export async function learnVectorstore(targetPath: string, namespace: string) {
  return {
    status: 'ok',
    message: `📚 Learned from "${targetPath}" into namespace "${namespace}" (stub response)`,
  };
}
