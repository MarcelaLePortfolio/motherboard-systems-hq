# Replace the existing file with the updated version that adds the new
# "For reply:" Summary Composition instructions while preserving the
# existing reply/durableInterpretation schema, workflow contract,
# and single Ollama invocation.
