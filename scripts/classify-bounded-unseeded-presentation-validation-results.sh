#!/usr/bin/env bash
set -euo pipefail

echo "=== CLASSIFY BOUNDED UNSEEDED PRESENTATION VALIDATION RESULTS ==="

test "$(git branch --show-current)" = "feature/support-source-references-runtime"
git merge-base --is-ancestor 54cff82c HEAD
test -z "$(git status --porcelain)"

latest="$(ls -t artifacts/unseeded-experimental-presentation-validation-*.log 2>/dev/null | head -1)"
test -n "$latest"

grep -q '^VALIDATION_CLASS=NON_PRODUCTION_PRODUCTION_EQUIVALENT_UNSEEDED_PRESENTATION_VALIDATION$' "$latest"
grep -q '^TEST_ARM=EXPERIMENTAL_PRESENTATION_ONLY$' "$latest"
grep -q '^PRESENTATION_VARIANT=EXPLICIT_PARENT_CHILD_SEPARATION$' "$latest"
grep -q '^GENERATION_SEED=ABSENT$' "$latest"
grep -q '^RUN_COUNT=10$' "$latest"
grep -q '^SEMANTIC_PASSES=10$' "$latest"
grep -q '^UNSUPPLIED_SUPPORT_FAILURES=0$' "$latest"
grep -q '^FAIL_CLOSED_OR_RUNTIME_REJECTIONS=0$' "$latest"
grep -q '^UNIQUE_EXACT_OUTPUT_FINGERPRINTS=9$' "$latest"
grep -q '^PRODUCTION_CHANGE=NONE$' "$latest"

cat <<MAP
PROGRAM=
MATILDA_CONVERSATION_ENGINE

MILESTONE=
CONVERSATION_ENGINE_RELIABLE_PRODUCTION_COLLABORATION

PHASE_2=
GENERATION_LAYER_INTERVENTION_INVESTIGATION

CORRIDOR_1=
PROMPT_AND_RESPONSE_CONTRACT_PRESENTATION

RESULT_SOURCE=
${latest}

VALIDATION_CLASS=
NON_PRODUCTION_PRODUCTION_EQUIVALENT_UNSEEDED_PRESENTATION_VALIDATION

TEST_ARM=
EXPERIMENTAL_PRESENTATION_ONLY

PRESENTATION_VARIANT=
EXPLICIT_PARENT_CHILD_SEPARATION

GENERATION_SEED=
ABSENT

RUN_COUNT=
10

SEMANTIC_RESULT=
10_OF_10_PASS

UNSUPPLIED_SUPPORT_FAILURE_RESULT=
0_OF_10

FAIL_CLOSED_OR_RUNTIME_REJECTION_RESULT=
0_OF_10

UNIQUE_EXACT_OUTPUT_FINGERPRINTS=
9

UNSEEDED_RELIABILITY_ON_TESTED_BOUNDED_SURFACE=
ESTABLISHED

EXACT_OUTPUT_REPEATABILITY=
NOT_REQUIRED_AND_NOT_OBSERVED

SEMANTIC_RELIABILITY_DESPITE_OUTPUT_VARIANCE=
ESTABLISHED_ON_TESTED_BOUNDED_SURFACE

PROMPT_PRESENTATION_STRUCTURE_AS_RELIABILITY_INTERVENTION_CANDIDATE=
STRONGLY_SUPPORTED

FIXED_SEED_AS_REQUIRED_REMEDY=
NOT_SUPPORTED

PRODUCTION_PROMPT_REPLACEMENT=
NOT_YET_AUTHORIZED

PRODUCTION_REGRESSION_VALIDATION=
STILL_REQUIRED

PRODUCTION_REMEDY_READINESS=
ADVANCED_BUT_NOT_COMPLETE

FAIL_CLOSED_VALIDATION=
PRESERVED

SUPPORT_PROVENANCE_RULES=
PRESERVED

SELECTED_CONTEXT_SEGMENT_RULES=
PRESERVED

MODEL=
UNCHANGED

RETRY_OR_SECOND_MODEL_CALL=
NONE

PRODUCTION_PROMPT_CHANGE=
NONE

PRODUCTION_GENERATION_POLICY_CHANGE=
NONE

VALIDATOR_CHANGE=
NONE

MODEL_CHANGE=
NONE

PRODUCTION_CHANGE=
NONE

CORRIDOR_1_STATUS=
ACTIVE

NEXT_ACTION=
CLASSIFY_EXPERIMENTAL_PRESENTATION_PRODUCTION_REGRESSION_VALIDATION_BOUNDARY
MAP
