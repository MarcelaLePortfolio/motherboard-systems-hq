/** * Simulates multiple task types through Matildas full runtime logic stack. * Logs outcomes to console and triggers all major memory systems. */
import fs from 'fs'
import path from 'path';

const memoryDir = path.resolve('memory');
