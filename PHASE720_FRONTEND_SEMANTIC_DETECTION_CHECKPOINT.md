
# Phase 720 Frontend Semantic Detection Checkpoint

## Status

Phase 720 frontend semantic envelope detection V2 is validated.

## Current HEAD

`d69fd737 Phase 720: validate frontend semantic detection v2`

## Verified Behavior

- Dashboard runtime healthy.

- Worker runtime healthy.

- Postgres healthy.

- Fresh task created successfully.

- Fresh worker artifact generated successfully.

- `MB_SEMANTIC_ARTIFACT_V1` envelope present in fresh artifact.

- Frontend detection helper served inside dashboard container.

- Semantic chip code served inside dashboard container.

- `/api/tasks` route still operational.

- Existing markdown fallback remains preserved.

## Preserved Contracts

- No DB schema mutation.

- No backend route mutation.

- No SSE contract mutation.

- No retry contract mutation.

- No worker mutation after semantic envelope validation.

- iframe/srcdoc corridor remains retired from active path.

## Stable Boundary

The system now supports:

1. Worker-authored additive semantic envelope.

2. Markdown artifact fallback.

3. Read-only frontend semantic envelope detection.

4. Visual semantic chip readiness.

5. Existing markdown section rendering.

## Next Safe Corridor

Browser-visible modal confirmation:

- Open the fresh Phase 720 frontend semantic detection task.

- Click Preview.

- Confirm `semantic v1.0` chip appears.

- Confirm Summary / Deliverable / Details / Recommendations / Next Steps / Outcome still render.

- Confirm no raw HTML comment is visible in the preview card.

- Confirm legacy Phase 719 artifacts still preview without semantic chip.

