-- Phase 40 — Acceptance checks (placeholder)
-- Must prove:
-- - No Tier B/C tasks transition to running via claim path
-- - run_view + task_events remain consistent
-- - Deterministic ordering is explicit in canonical claim SQL
SELECT 1 AS phase40_placeholder;
