services:
  worker:
    healthcheck:
      test: ["CMD-SHELL", "ps aux | grep phase26_task_worker | grep -v grep || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3
