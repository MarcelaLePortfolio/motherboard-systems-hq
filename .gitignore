# macOS
.DS_Store

# Node / build
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

dist/
build/
.coverage/
coverage/
.cache/
.esbuild/

# Logs / temp
logs/
*.log
tmp/
.tmp/
*.swp

# Env
.env
.env.*
!.env.example

# local-only Phase 16 scratch scripts
scripts/phase16_*

# allow committed Phase 16 helper script
!scripts/phase16_verify_sse_frontend.sh

# local recovery / backups (do not commit)
._recovery/
Backups/
public/dashboard.html.bak-*

!scripts/phase16_proofpack_connect_guard.sh

server/optional-sse.mjs.bak-*

.local_artifacts/
db/*.db
db/*.db-wal
db/*.db-shm
*.sqlite
docker-compose.yml.bak.*
drizzle.bak.*/
drizzle/*.busted.*

# local artifacts (do not commit)
.local_artifacts/
db/*.db
db/*.db-wal
db/*.db-shm
*.sqlite

# local backups / scratch
docker-compose.yml.bak.*
drizzle.bak.*/
drizzle/*.busted.*
