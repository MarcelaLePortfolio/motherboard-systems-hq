== Phase 62B Tasks Queued Discovery ==

-- repo root --
/Users/marcela-dev/Projects/Motherboard_Systems_HQ

-- branch --
phase65b-running-tasks-hydration

-- candidate telemetry files --
./public_broken_rollback_1762824556/dashboard-reflections.js
./public_broken_rollback_1762824556/dashboard-ops.js
./public_broken_rollback_1762824556/dashboard-chat.js
./public_broken_rollback_1762824556/agent-status-row.js
./cade_task_processor_clean.ts
./verify-dashboard-sync.ts
./drizzle_pg/0009_phase39_db_repair_notes_failedat_taskid.sql
./drizzle_pg/0001_create_task_events.sql
./drizzle_pg/0009_phase57_1_tasks_title.sql
./drizzle_pg/0006_phase32_1_task_events_contract.sql
./drizzle_pg/0008_phase57_run_snapshot.sql
./drizzle_pg/0001_create_tasks.sql
./snapshots/20251114-114821/public/dashboard.backup.js
./snapshots/20251114-114821/public/dashboard-logs.v3.js
./snapshots/20251114-114821/public/js/dashboard-broadcast.js
./snapshots/20251114-114821/public/js/dashboard-ops.js
./snapshots/20251114-114821/public/js/task-activity-graph.js
./snapshots/20251114-114821/public/js/ops-task-alerts.js
./snapshots/20251114-114821/public/js/agent-interaction-animation.js
./snapshots/20251114-114821/public/js/dashboard-graph-loader.js
./snapshots/20251114-114821/public/js/reflections-viewer.js
./snapshots/20251114-114821/public/js/dashboard-graph.js
./snapshots/20251114-114821/public/js/dashboard.js
./snapshots/20251114-114821/public/js/dashboard-stream.js
./snapshots/20251114-114821/public/dashboard-logs.js
./snapshots/20251114-114821/public/dashboard-status.js
./snapshots/20251114-114821/public/dashboard-tabs.backup.js
./snapshots/20251114-114821/public/dashboard.js
./snapshots/20251114-114821/public/dashboard-stream.js
./snapshots/20251114-114821/public/dashboard-tabs.js
./snapshots/20251114-114821/scripts/demo/verify-dashboard-sync.ts
./snapshots/20251114-114821/scripts/demo/run-live-delegation.ts
./snapshots/20251114-114821/scripts/demo/run-demo-sequence.ts
./snapshots/20251114-114821/scripts/reset-roundtrip.ts
./snapshots/20251114-114821/scripts/streams/reflection-sse-lite.ts
./snapshots/20251114-114821/scripts/agents/effie.ts
./snapshots/20251114-114821/scripts/agents/cade.ts
./snapshots/20251114-114821/scripts/agents/utils/ensurePaths.ts
./snapshots/20251114-114821/scripts/agents/utils/retryCreateAgent.ts
./snapshots/20251114-114821/scripts/agents/cade-delegation-watcher.ts
./snapshots/20251114-114821/scripts/agents/cade_status.ts
./snapshots/20251114-114821/scripts/agents/matilda/askRouter.ts
./snapshots/20251114-114821/scripts/agents/matilda/askRouter.runtime.ts
./snapshots/20251114-114821/scripts/utils/reflection-push.ts
./snapshots/20251114-114821/scripts/utils/ollamaChat.ts
./snapshots/20251114-114821/scripts/utils/ensure-task-events-main.ts
./snapshots/20251114-114821/scripts/utils/validateTaskEvent.ts
./snapshots/20251114-114821/scripts/utils/agentStatus.js
./snapshots/20251114-114821/scripts/utils/ollamaPlan.ts
./snapshots/20251114-114821/scripts/utils/agentWatchdog.ts
./snapshots/20251114-114821/scripts/utils/pruneDatabase.ts
./snapshots/20251114-114821/scripts/utils/log.ts
./snapshots/20251114-114821/scripts/utils/ensure-task-events.ts
./snapshots/20251114-114821/scripts/utils/runSkill.ts
./snapshots/20251114-114821/scripts/cli/matilda.ts
./snapshots/20251114-114821/scripts/agents_full/effie.ts
./snapshots/20251114-114821/scripts/agents_full/cade.ts
./snapshots/20251114-114821/scripts/agents_full/matilda.ts
./snapshots/20251114-114821/scripts/agents_stubs/effie.ts
./snapshots/20251114-114821/scripts/agents_stubs/cade.ts
./snapshots/20251114-114821/scripts/agents_stubs/matilda.ts
./snapshots/20251114-114821/scripts/run-create-atlas.js
./snapshots/20251114-114821/scripts/skills/createFile.ts
./snapshots/20251114-114821/scripts/skills/readFile.ts
./snapshots/20251114-114821/scripts/relay/agent-to-cade.ts
./snapshots/20251114-114821/scripts/relay/agent-to-effie.ts
./snapshots/20251114-114821/scripts/_archive/effie_delegate.js
./snapshots/20251114-114821/scripts/_archive/matilda_delegate.js
./snapshots/20251114-114821/scripts/loaders/reflection-loader.ts
./snapshots/20251114-114821/scripts/_local/test_runtime_simulation.ts
./snapshots/20251114-114821/scripts/_local/route-loader/ops-sse-bootstrap.ts
./snapshots/20251114-114821/scripts/_local/route-loader/reflection-sse-server.ts
./snapshots/20251114-114821/scripts/_local/route-loader/ops-sse-server.ts
./snapshots/20251114-114821/scripts/_local/utils/chain.ts
./snapshots/20251114-114821/scripts/_local/utils/log.ts
./snapshots/20251114-114821/scripts/_local/utils/chainState.ts
./snapshots/20251114-114821/scripts/_local/submit-task.ts
./snapshots/20251114-114821/scripts/_local/run-effie-maint.ts
./snapshots/20251114-114821/scripts/_local/test-cade-state.ts
./snapshots/20251114-114821/scripts/_local/dev/skip-ui-build.js
./snapshots/20251114-114821/scripts/_local/dev-server.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/cade-processor.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/matilda-auto-deploy-patch.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/output/welcome_kit.md
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/effie_task_processor.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/clean_settings_tab.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/clean_rogue_buttons.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/update_tab_switch.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/delegateToCade.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/merge_tracker_tasks.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/fix_settings_tab.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/submit-task.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/shared_utils.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/launch-cade.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/db-init/prepare-db.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/matilda-liaison.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/effie_runtime.js
./snapshots/20251114-114821/scripts/_local/agent-runtime/launch-effie.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/launch-matilda.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/handlers/handleTask.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/matilda_task_processor.ts
./snapshots/20251114-114821/scripts/_local/agent-runtime/run-cade-task-processor.js
./snapshots/20251114-114821/scripts/_local/handlers/install.ts
./snapshots/20251114-114821/scripts/_local/handlers/commit.js
./snapshots/20251114-114821/scripts/_local/handlers/taskRouter.ts
./snapshots/20251114-114821/scripts/_local/handlers/commit.ts
./snapshots/20251114-114821/scripts/_local/handlers/buildUI.ts
./snapshots/20251114-114821/scripts/servers/reflection-sse-server.ts
./snapshots/20251114-114821/scripts/servers/ops-sse-server.ts
./snapshots/20251114-114821/scripts/watchers/reflection-watcher.ts
./snapshots/20251114-114821/db/reflection_index.ts
./snapshots/20251114-114821/db/seeds/phase4_demo_seed.sql
./snapshots/20251114-114821/db/client.ts
./snapshots/20251114-114821/db/audit.ts
./snapshots/20251114-114821/index.ts
./snapshots/20251114-114821/server.ts
./snapshots/20251114-114821/routes/cade.ts
./snapshots/20251114-114821/routes/eventsAgents.ts
./snapshots/20251114-114821/routes/logs.ts
./snapshots/20251114-114821/routes/delegations.ts
./snapshots/20251114-114821/routes/agentsStatus.ts
./snapshots/20251114-114821/routes/reset.ts
./snapshots/20251114-114821/routes/diagnostics/autonomicAdaptation.ts
./snapshots/20251114-114821/routes/diagnostics/introspectiveSim.js
./snapshots/20251114-114821/routes/diagnostics/systemHealth.js
./snapshots/20251114-114821/routes/diagnostics/persistentInsight.ts
./snapshots/20251114-114821/routes/diagnostics/systemChronicle.ts
./snapshots/20251114-114821/routes/diagnostics/autonomicAdaptation.js
./snapshots/20251114-114821/routes/diagnostics/systemHealth.ts
./snapshots/20251114-114821/routes/diagnostics/persistentInsight.js
./snapshots/20251114-114821/routes/diagnostics/systemChronicle.js
./snapshots/20251114-114821/routes/diagnostics/introspectiveSim.ts
./snapshots/20251114-114821/routes/events.ts
./snapshots/20251114-114821/routes/api/logs.ts
./snapshots/20251114-114821/routes/api/delegate.ts
./snapshots/20251114-114821/routes/api/tasks.ts
./snapshots/20251114-114821/routes/reflections.ts
./snapshots/20251114-114821/routes/agentsStatus.js
./snapshots/20251114-114821/routes/tasks.ts
./snapshots/20251114-114821/routes/matilda.ts
./snapshots/20251114-114707/public/dashboard.backup.js
./snapshots/20251114-114707/public/dashboard-logs.v3.js
./snapshots/20251114-114707/public/js/dashboard-broadcast.js
./snapshots/20251114-114707/public/js/dashboard-ops.js
./snapshots/20251114-114707/public/js/task-activity-graph.js
./snapshots/20251114-114707/public/js/ops-task-alerts.js
./snapshots/20251114-114707/public/js/agent-interaction-animation.js
./snapshots/20251114-114707/public/js/dashboard-graph-loader.js
./snapshots/20251114-114707/public/js/reflections-viewer.js
./snapshots/20251114-114707/public/js/dashboard-graph.js
./snapshots/20251114-114707/public/js/dashboard.js
./snapshots/20251114-114707/public/js/dashboard-stream.js
./snapshots/20251114-114707/public/dashboard-logs.js
./snapshots/20251114-114707/public/dashboard-status.js
./snapshots/20251114-114707/public/dashboard-tabs.backup.js
./snapshots/20251114-114707/public/dashboard.js
./snapshots/20251114-114707/public/dashboard-stream.js
./snapshots/20251114-114707/public/dashboard-tabs.js
./snapshots/20251114-114707/scripts/demo/verify-dashboard-sync.ts
./snapshots/20251114-114707/scripts/demo/run-live-delegation.ts
./snapshots/20251114-114707/scripts/demo/run-demo-sequence.ts
./snapshots/20251114-114707/scripts/reset-roundtrip.ts
./snapshots/20251114-114707/scripts/streams/reflection-sse-lite.ts
./snapshots/20251114-114707/scripts/agents/effie.ts
./snapshots/20251114-114707/scripts/agents/cade.ts
./snapshots/20251114-114707/scripts/agents/utils/ensurePaths.ts
./snapshots/20251114-114707/scripts/agents/utils/retryCreateAgent.ts
./snapshots/20251114-114707/scripts/agents/cade-delegation-watcher.ts
./snapshots/20251114-114707/scripts/agents/cade_status.ts
./snapshots/20251114-114707/scripts/agents/matilda/askRouter.ts
./snapshots/20251114-114707/scripts/agents/matilda/askRouter.runtime.ts
./snapshots/20251114-114707/scripts/utils/reflection-push.ts
./snapshots/20251114-114707/scripts/utils/ollamaChat.ts
./snapshots/20251114-114707/scripts/utils/ensure-task-events-main.ts
./snapshots/20251114-114707/scripts/utils/validateTaskEvent.ts
./snapshots/20251114-114707/scripts/utils/agentStatus.js
./snapshots/20251114-114707/scripts/utils/ollamaPlan.ts
./snapshots/20251114-114707/scripts/utils/agentWatchdog.ts
./snapshots/20251114-114707/scripts/utils/pruneDatabase.ts
./snapshots/20251114-114707/scripts/utils/log.ts
./snapshots/20251114-114707/scripts/utils/ensure-task-events.ts
./snapshots/20251114-114707/scripts/utils/runSkill.ts
./snapshots/20251114-114707/scripts/cli/matilda.ts
./snapshots/20251114-114707/scripts/agents_full/effie.ts
./snapshots/20251114-114707/scripts/agents_full/cade.ts
./snapshots/20251114-114707/scripts/agents_full/matilda.ts
./snapshots/20251114-114707/scripts/agents_stubs/effie.ts
./snapshots/20251114-114707/scripts/agents_stubs/cade.ts
./snapshots/20251114-114707/scripts/agents_stubs/matilda.ts
./snapshots/20251114-114707/scripts/run-create-atlas.js
./snapshots/20251114-114707/scripts/skills/createFile.ts
./snapshots/20251114-114707/scripts/skills/readFile.ts
./snapshots/20251114-114707/scripts/relay/agent-to-cade.ts
./snapshots/20251114-114707/scripts/relay/agent-to-effie.ts
./snapshots/20251114-114707/scripts/_archive/effie_delegate.js
./snapshots/20251114-114707/scripts/_archive/matilda_delegate.js
./snapshots/20251114-114707/scripts/loaders/reflection-loader.ts
./snapshots/20251114-114707/scripts/_local/test_runtime_simulation.ts
./snapshots/20251114-114707/scripts/_local/route-loader/ops-sse-bootstrap.ts
./snapshots/20251114-114707/scripts/_local/route-loader/reflection-sse-server.ts
./snapshots/20251114-114707/scripts/_local/route-loader/ops-sse-server.ts
./snapshots/20251114-114707/scripts/_local/utils/chain.ts
./snapshots/20251114-114707/scripts/_local/utils/log.ts
./snapshots/20251114-114707/scripts/_local/utils/chainState.ts
./snapshots/20251114-114707/scripts/_local/submit-task.ts
./snapshots/20251114-114707/scripts/_local/run-effie-maint.ts
./snapshots/20251114-114707/scripts/_local/test-cade-state.ts
./snapshots/20251114-114707/scripts/_local/dev/skip-ui-build.js
./snapshots/20251114-114707/scripts/_local/dev-server.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/cade-processor.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/matilda-auto-deploy-patch.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/output/welcome_kit.md
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/effie_task_processor.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/clean_settings_tab.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/clean_rogue_buttons.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/update_tab_switch.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/delegateToCade.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/merge_tracker_tasks.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/fix_settings_tab.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/submit-task.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/shared_utils.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/launch-cade.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/db-init/prepare-db.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/matilda-liaison.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/effie_runtime.js
./snapshots/20251114-114707/scripts/_local/agent-runtime/launch-effie.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/launch-matilda.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/handlers/handleTask.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/matilda_task_processor.ts
./snapshots/20251114-114707/scripts/_local/agent-runtime/run-cade-task-processor.js
./snapshots/20251114-114707/scripts/_local/handlers/install.ts
./snapshots/20251114-114707/scripts/_local/handlers/commit.js
./snapshots/20251114-114707/scripts/_local/handlers/taskRouter.ts
./snapshots/20251114-114707/scripts/_local/handlers/commit.ts
./snapshots/20251114-114707/scripts/_local/handlers/buildUI.ts
./snapshots/20251114-114707/scripts/servers/reflection-sse-server.ts
./snapshots/20251114-114707/scripts/servers/ops-sse-server.ts
./snapshots/20251114-114707/scripts/watchers/reflection-watcher.ts
./snapshots/20251114-114707/db/reflection_index.ts
./snapshots/20251114-114707/db/seeds/phase4_demo_seed.sql
./snapshots/20251114-114707/db/client.ts
./snapshots/20251114-114707/db/audit.ts
./snapshots/20251114-114707/index.ts
./snapshots/20251114-114707/server.ts
./snapshots/20251114-114707/routes/cade.ts
./snapshots/20251114-114707/routes/eventsAgents.ts
./snapshots/20251114-114707/routes/logs.ts
./snapshots/20251114-114707/routes/delegations.ts
./snapshots/20251114-114707/routes/agentsStatus.ts
./snapshots/20251114-114707/routes/reset.ts
./snapshots/20251114-114707/routes/diagnostics/autonomicAdaptation.ts
./snapshots/20251114-114707/routes/diagnostics/introspectiveSim.js
./snapshots/20251114-114707/routes/diagnostics/systemHealth.js
./snapshots/20251114-114707/routes/diagnostics/persistentInsight.ts
./snapshots/20251114-114707/routes/diagnostics/systemChronicle.ts
./snapshots/20251114-114707/routes/diagnostics/autonomicAdaptation.js
./snapshots/20251114-114707/routes/diagnostics/systemHealth.ts
./snapshots/20251114-114707/routes/diagnostics/persistentInsight.js
./snapshots/20251114-114707/routes/diagnostics/systemChronicle.js
./snapshots/20251114-114707/routes/diagnostics/introspectiveSim.ts
./snapshots/20251114-114707/routes/events.ts
./snapshots/20251114-114707/routes/api/logs.ts
./snapshots/20251114-114707/routes/api/delegate.ts
./snapshots/20251114-114707/routes/api/tasks.ts
./snapshots/20251114-114707/routes/reflections.ts
./snapshots/20251114-114707/routes/agentsStatus.js
./snapshots/20251114-114707/routes/tasks.ts
./snapshots/20251114-114707/routes/matilda.ts
./add_sample_tasks.js
./ensure-task-events-main.ts
./routes_backup/diagnostics/autonomicAdaptation.ts
./routes_backup/diagnostics/introspectiveSim.js
./routes_backup/diagnostics/systemHealth.js
./routes_backup/diagnostics/persistentInsight.ts
./routes_backup/diagnostics/systemChronicle.ts
./routes_backup/diagnostics/autonomicAdaptation.js
./routes_backup/diagnostics/systemHealth.ts
./routes_backup/diagnostics/persistentInsight.js
./routes_backup/diagnostics/systemChronicle.js
./routes_backup/diagnostics/introspectiveSim.ts
./routes_backup/tasks.ts
./effie_task_processor.ts
./server/task-lifecycle.ts
./server/orchestration/operator-commands.ts
./server/orchestration/task-state-machine.ts
./server/orchestration/operator-commands.test.ts
./server/orchestration/task-state-machine.test.ts
./server/worker/phase34_claim_task_id_for_test.sql
./server/sql/phase40_6_shadow_audit_task_events.sql
./cade_task_processor.bak.ts
./scripts_backup/demo/verify-dashboard-sync.ts
./scripts_backup/agents/cade_status.ts
./scripts_backup/utils/ensure-task-events-main.ts
./scripts_backup/utils/ensure-task-events.ts
./scripts_backup/scripts/demo/verify-dashboard-sync.ts
./scripts_backup/scripts/agents/cade_status.ts
./scripts_backup/scripts/utils/ensure-task-events-main.ts
./scripts_backup/scripts/utils/ensure-task-events.ts
./scripts_backup/scripts/diagnostics/test-rehydration.ts
./scripts_backup/scripts/_local/submit-task.ts
./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
./scripts_backup/scripts/_local/agent-runtime/utils/effie_task_processor.ts
./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor.ts
./scripts_backup/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
./scripts_backup/scripts/_local/agent-runtime/utils/merge_tracker_tasks.js
./scripts_backup/scripts/_local/agent-runtime/submit-task.ts
./scripts_backup/scripts/_local/agent-runtime/matilda_task_processor.ts
./scripts_backup/scripts/_local/agent-runtime/run-cade-task-processor.js
./scripts_backup/scripts/_local/handlers/taskRouter.ts
./scripts_backup/diagnostics/test-rehydration.ts
./scripts_backup/_local/submit-task.ts
./scripts_backup/_local/agent-runtime/utils/cade_task_processor_clean.ts
./scripts_backup/_local/agent-runtime/utils/effie_task_processor.ts
./scripts_backup/_local/agent-runtime/utils/cade_task_processor.bak.ts
./scripts_backup/_local/agent-runtime/utils/cade_task_processor.ts
./scripts_backup/_local/agent-runtime/utils/matilda_task_processor.ts
./scripts_backup/_local/agent-runtime/utils/merge_tracker_tasks.js
./scripts_backup/_local/agent-runtime/submit-task.ts
./scripts_backup/_local/agent-runtime/matilda_task_processor.ts
./scripts_backup/_local/agent-runtime/run-cade-task-processor.js
./scripts_backup/_local/handlers/taskRouter.ts
./docs/v11-dashboard-bundle-handoff-v2.md
./docs/v11-dashboard-bundle-handoff-v3.md
./docs/v11-dashboard-bundle-next-steps.md
./docs/v11-dashboard-bundle-thread4-placeholder.md
./docs/v11-dashboard-bundle-status.md
./docs/v11-dashboard-bundle-todo.md
./docs/v11-dashboard-bundle-handoff-thread3.md
./docs/v11-dashboard-bundle-next-thread-anchor.md
./docs/v11-dashboard-bundle-thread-summary-2025-11-27.md
./docs/telemetry/EVENT_SCHEMA_CONTRACT.md
./docs/PHASE46_operator_grade_run_lifecycle.md
./taskRouter.ts
./submit-task.ts
./exports/dashboard_phase6.8/js/dashboard-broadcast.js
./exports/dashboard_phase6.8/js/dashboard-ops.js
./exports/dashboard_phase6.8/js/task-activity-graph.js
./exports/dashboard_phase6.8/js/ops-task-alerts.js
./exports/dashboard_phase6.8/js/agent-interaction-animation.js
./exports/dashboard_phase6.8/js/dashboard-graph-loader.js
./exports/dashboard_phase6.8/js/reflections-viewer.js
./exports/dashboard_phase6.8/js/dashboard-graph.js
./exports/dashboard_phase6.8/js/dashboard-stream.js
./dashboard/src/operator/runbooks/operatorRunbookResolver.ts
./dashboard/src/operator/runbooks/operatorRunbookTypes.ts
./dashboard/src/operator/runbooks/operatorRunbookFormatter.ts
./dashboard/src/operator/runbooks/operatorRunbookCatalog.ts
./public/dashboard.backup.js
./public/dashboard-logs.v3.js
./public/js/dashboard-tasks-widget.js
./public/js/sse-tasks-singleton.js
./public/js/dashboard-broadcast.js
./public/js/phase13_5_operator_ux.js
./public/js/task-activity-graph.js
./public/js/telemetry_queue_depth_reducer.js
./public/js/dashboard-graph-loader.js
./public/js/dashboard-status.js
./public/js/dashboard-bundle-entry.js
./public/js/task-completion.js
./public/js/dashboard-delegation.js
./public/js/ops-status-widget.js
./public/js/task-delegation.js
./public/js/task-activity.js
./public/js/telemetry_failed_tasks_reducer.js
./public/js/dashboard-ops-reflections-ui.js
./public/js/agent-status-row.js
./public/js/phase31_6_seed_task_button.js
./public/js/phase16_sse_status_indicator.js
./public/js/task-events-sse-client.js
./public/js/agent-status.js
./public/js/telemetry/success_rate_metric.js
./public/js/telemetry/latency_metric.js
./public/js/telemetry/running_tasks_metric.js
./public/js/telemetry/phase65b_metric_ownership_guard.js
./public/js/telemetry/phase65b_metric_bootstrap.js
./public/js/phase23_matilda_task_events_bridge.js
./public/js/dashboard-graph.js
./public/js/reflections-sse-dashboard.js
./public/js/phase22_task_delegation_live_bindings.js
./public/js/phase23_matilda_delegate_taskspec.js
./public/dashboard-logs.js
./public/dashboard-status.js
./public/scripts/dashboard-reflections.js
./public/scripts/dashboard-ops.js
./public/scripts/dashboard-chat.js
./public/scripts/agent-status-row.js
./public/dashboard-tabs.backup.js
./public/dashboard.js
./public/dashboard-stream.js
./public/dashboard-tabs.js
./cade_status.ts
./scripts/demo/verify-dashboard-sync.ts
./scripts/phase19_postgres_apply_task_events.sql
./scripts/dashboard-reflections.js
./scripts/phase35_pg_fix_tasks_actor_run_id.sql
./scripts/dashboard-ops.js
./scripts/dashboard-chat.js
./scripts/agents/cade_status.ts
./scripts/utils/ensure-task-events-main.ts
./scripts/utils/ensure-task-events.ts
./scripts/operator-runbook-smoke.ts
./scripts/diagnostics/test-rehydration.ts
./scripts/agent-status-row.js
./scripts/operator-runbook-assert.ts
./scripts/build-dashboard-bundle.js
./scripts/_local/phase74_operator_workflow_helpers_smoke.ts
./scripts/_local/phase76_operator_playbooks.ts
./scripts/_local/phase75_operator_ranked_helpers.ts
./scripts/_local/phase73_operator_safety_gates_smoke.ts
./scripts/_local/phase75_operator_helper_priority.ts
./scripts/_local/phase72_operator_guidance_schema.ts
./scripts/_local/phase76_operator_playbook_selection.ts
./scripts/_local/submit-task.ts
./scripts/_local/phase75_operator_helper_priority_smoke.ts
./scripts/_local/phase72_operator_guidance_command.ts
./scripts/_local/phase72_operator_guidance_interpreter.ts
./scripts/_local/phase72_operator_safety_classifier.ts
./scripts/_local/phase72_operator_guidance_smoke.ts
./scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
./scripts/_local/agent-runtime/utils/effie_task_processor.ts
./scripts/_local/agent-runtime/utils/cade_task_processor.ts
./scripts/_local/agent-runtime/utils/matilda_task_processor.ts
./scripts/_local/agent-runtime/utils/merge_tracker_tasks.js
./scripts/_local/agent-runtime/submit-task.ts
./scripts/_local/agent-runtime/matilda_task_processor.ts
./scripts/_local/agent-runtime/run-cade-task-processor.js
./scripts/_local/phase73_operator_safety_gates.ts
./scripts/_local/phase74_operator_workflow_helpers.ts
./scripts/_local/handlers/taskRouter.ts
./db/task_events.schema.ts
./cade_task_processor.ts
./ensure-task-events.ts
./ts-backup/cade_task_processor_clean.ts
./ts-backup/verify-dashboard-sync.ts
./ts-backup/ensure-task-events-main.ts
./ts-backup/effie_task_processor.ts
./ts-backup/cade_task_processor.bak.ts
./ts-backup/taskRouter.ts
./ts-backup/submit-task.ts
./ts-backup/cade_status.ts
./ts-backup/cade_task_processor.ts
./ts-backup/ensure-task-events.ts
./ts-backup/tasks.ts
./ts-backup/test-rehydration.ts
./ts-backup/matilda_task_processor.ts
./tasks.ts
./test-rehydration.ts
./routes/diagnostics/autonomicAdaptation.ts
./routes/diagnostics/introspectiveSim.js
./routes/diagnostics/systemHealth.js
./routes/diagnostics/persistentInsight.ts
./routes/diagnostics/systemChronicle.ts
./routes/diagnostics/autonomicAdaptation.js
./routes/diagnostics/systemHealth.ts
./routes/diagnostics/persistentInsight.js
./routes/diagnostics/systemChronicle.js
./routes/diagnostics/introspectiveSim.ts
./routes/api/tasks.ts
./routes/tasks.ts
./routes/routes/diagnostics/autonomicAdaptation.ts
./routes/routes/diagnostics/introspectiveSim.js
./routes/routes/diagnostics/systemHealth.js
./routes/routes/diagnostics/persistentInsight.ts
./routes/routes/diagnostics/systemChronicle.ts
./routes/routes/diagnostics/autonomicAdaptation.js
./routes/routes/diagnostics/systemHealth.ts
./routes/routes/diagnostics/persistentInsight.js
./routes/routes/diagnostics/systemChronicle.js
./routes/routes/diagnostics/introspectiveSim.ts
./routes/routes/tasks.ts
./scripts_backup_2/demo/verify-dashboard-sync.ts
./scripts_backup_2/agents/cade_status.ts
./scripts_backup_2/utils/ensure-task-events-main.ts
./scripts_backup_2/utils/ensure-task-events.ts
./scripts_backup_2/diagnostics/test-rehydration.ts
./scripts_backup_2/_local/submit-task.ts
./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor_clean.ts
./scripts_backup_2/_local/agent-runtime/utils/effie_task_processor.ts
./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor.bak.ts
./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor.ts
./scripts_backup_2/_local/agent-runtime/utils/matilda_task_processor.ts
./scripts_backup_2/_local/agent-runtime/utils/merge_tracker_tasks.js
./scripts_backup_2/_local/agent-runtime/submit-task.ts
./scripts_backup_2/_local/agent-runtime/matilda_task_processor.ts
./scripts_backup_2/_local/agent-runtime/run-cade-task-processor.js
./scripts_backup_2/_local/handlers/taskRouter.ts
./matilda_task_processor.ts
./drizzle/0003_phase32_tasks_actor_run_id.sql

-- existing hydrated metrics references --
./docs/MATILDA_CHAT_COLUMN_SPLIT_ISSUE.md:7:  above the metrics row (Active Agents, Tasks Running, etc.).
./docs/contracts/PHASE62B_METRICS_HYDRATION_PLAN.md:12:2 Tasks Running
./docs/contracts/PHASE62B_METRICS_HYDRATION_PLAN.md:52:Tasks Running
./docs/contracts/PHASE62_METRICS_DATA_CONTRACT.md:92:Tasks Running
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:14:- `/events/task-events` for Tasks Running
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:22:Tasks Running uses:
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:92:- Tasks Running does not treat `complete`, `done`, `success`, or `error` as terminal
./docs/checkpoints/PHASE65C_QUEUE_DEPTH_ANCHOR_AUDIT_20260315.md:9:metric-tasks    → Tasks Running
./docs/checkpoints/PHASE62B_ACTIVE_AGENTS_HYDRATION_COMPLETE_20260311.md:86:Tasks Running
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_LOCAL_VERIFICATION_20260316.md:8:Verify bounded Tasks Completed derivation behavior locally using deterministic event sequences.
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_LOCAL_VERIFICATION_20260316.md:86:Tasks Completed derivation behavior is deterministically verified across bounded local cases.
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:2:Tasks Running Metric Implementation Plan
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:7:Hydrate the "Tasks Running" metric tile using existing task lifecycle data without altering layout or script mount order.
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:88:Tasks Running reflects live runtime state
./docs/checkpoints/PHASE65_SHARED_METRICS_PATCH_VERIFIED_20260314.md:45:Expected Tasks Running display should be non-blank and greater than zero after bootstrap.
./docs/checkpoints/PHASE65_SHARED_METRICS_PATCH_VERIFIED_20260314.md:59:3. Confirm Tasks Running is not blank
./docs/checkpoints/PHASE62B_RUNNING_TASKS_SURFACE_SELECTION_20260316.md:16:It is the narrowest existing telemetry derivation surface for adding a runningTasks count without mutating:
./docs/checkpoints/PHASE62B_RUNNING_TASKS_SURFACE_SELECTION_20260316.md:34:Do not implement first-pass runningTasks derivation here if it can be completed inside the existing UI derivation layer.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_SURFACE_SELECTION_20260316.md:44:• add isolated runningTasks derivation inside public/js/phase22_task_delegation_live_bindings.js
./docs/checkpoints/PHASE65_TASKS_RUNNING_EXECUTION_START_20260314.md:2:Tasks Running Hydration
./docs/checkpoints/PHASE65_TASKS_RUNNING_EXECUTION_START_20260314.md:13:Hydrate the "Tasks Running" metric without:
./docs/checkpoints/PHASE65_TASKS_RUNNING_EXECUTION_START_20260314.md:65:"Tasks Running" becomes live while all Phase 64 protections remain green.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1326:./docs/MATILDA_CHAT_COLUMN_SPLIT_ISSUE.md:7:  above the metrics row (Active Agents, Tasks Running, etc.).
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1637:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:107:runningTasks = size(activeTaskIds)
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1638:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:115:• runningTasks >= 0 always
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1639:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:116:• runningTasks deterministic under replay
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1807:./docs/PHASE62B_RUNNING_TASKS_IMPLEMENTATION_START.md:26:STEP 3 — Add isolated runningTasks derivation
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:2327:./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:14:- `/events/task-events` for Tasks Running
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4017:./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:7:The Tasks Running, Success Rate, and Latency telemetry binder already exists.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4022:./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:47:1. idle dashboard shows 0 Tasks Running
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4147:./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_STARTED_20260312.md:21:- Tasks Running derives from `/events/task-events`
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5245:./scripts/_local/phase66_metric_collision_check.sh:18:FAILED_SYMBOL_COLLISIONS="$(grep -R --line-number --exclude-dir=.git --exclude='telemetry_failed_tasks_reducer.js' 'failedTasksTelemetry\|getFailedTaskCount' public/js || true)"
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5589:./scripts/_local/phase65c_inspect_queue_depth_target.sh:11:rg -n 'metric-|Tasks Running|Success Rate|Latency|System metrics|tasks|success|latency' public/dashboard.html || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5662:./scripts/_local/phase62b_running_tasks_discovery.sh:40:      'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5663:./scripts/_local/phase62b_running_tasks_discovery.sh:42:    grep -RInE 'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . \
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5666:./scripts/_local/phase62b_running_tasks_discovery.sh:70:  echo "2. Add an isolated runningTasks derivation helper only in that surface."
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5751:./scripts/_local/phase66a_determinism_check.sh:51:  return reducer.context.window.failedTasksTelemetry.getFailedTaskCount();
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:6410:./scripts/_local/phase65_2_tasks_running_focus_inspect.sh:8:nl -ba public/dashboard.html | sed -n '1,260p' | grep -nE 'Tasks Running|Success Rate|Latency|task-count|success-rate|latency|metric|tile' || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:6488:./scripts/_local/phase65_1_tasks_running_inspect.sh:13:rg -n 'Tasks Running|Success Rate|Latency|metric|tile' public/dashboard.html public/js public/css || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:6491:./scripts/_local/phase65_1_tasks_running_inspect.sh:37:echo "Inspect whether Tasks Running tile already has a stable DOM hook in public/dashboard.html."
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:6913:2. Add an isolated runningTasks derivation helper only in that surface.
./docs/checkpoints/PHASE62B_FINAL_VERIFIED_STABLE_STATE_20260311.md:35:- Tasks Running
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_SEMANTICS_FINDINGS_20260312.md:6:Continue Phase 63.3 with a narrow finding pass on exact Tasks Running membership semantics from the verified Phase 63.2 golden baseline.
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_SEMANTICS_FINDINGS_20260312.md:43:Current Tasks Running metric is:
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_SEMANTICS_FINDINGS_20260312.md:60:Current Tasks Running semantics are internally coherent for this audit pass:
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:634:./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:14:- `/events/task-events` for Tasks Running
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:641:./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_STARTED_20260312.md:21:- Tasks Running derives from `/events/task-events`
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:919:./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:47:1. idle dashboard shows 0 Tasks Running
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:920:./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:7:The Tasks Running, Success Rate, and Latency telemetry binder already exists.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:1484:./docs/MATILDA_CHAT_COLUMN_SPLIT_ISSUE.md:7:  above the metrics row (Active Agents, Tasks Running, etc.).
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:1650:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:107:runningTasks = size(activeTaskIds)
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:1651:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:115:• runningTasks >= 0 always
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:1652:./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:116:• runningTasks deterministic under replay
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:1684:./docs/PHASE62B_RUNNING_TASKS_IMPLEMENTATION_START.md:26:STEP 3 — Add isolated runningTasks derivation
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4994:./scripts/_local/phase62b_running_tasks_discovery.sh:40:      'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4995:./scripts/_local/phase62b_running_tasks_discovery.sh:42:    grep -RInE 'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . \
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4998:./scripts/_local/phase62b_running_tasks_discovery.sh:70:  echo "2. Add an isolated runningTasks derivation helper only in that surface."
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5360:./scripts/_local/phase65_1_tasks_running_inspect.sh:13:rg -n 'Tasks Running|Success Rate|Latency|metric|tile' public/dashboard.html public/js public/css || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5363:./scripts/_local/phase65_1_tasks_running_inspect.sh:37:echo "Inspect whether Tasks Running tile already has a stable DOM hook in public/dashboard.html."
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5366:./scripts/_local/phase65_2_tasks_running_focus_inspect.sh:8:nl -ba public/dashboard.html | sed -n '1,260p' | grep -nE 'Tasks Running|Success Rate|Latency|task-count|success-rate|latency|metric|tile' || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5507:./scripts/_local/phase65c_inspect_queue_depth_target.sh:11:rg -n 'metric-|Tasks Running|Success Rate|Latency|System metrics|tasks|success|latency' public/dashboard.html || true
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5576:./scripts/_local/phase66_metric_collision_check.sh:18:FAILED_SYMBOL_COLLISIONS="$(grep -R --line-number --exclude-dir=.git --exclude='telemetry_failed_tasks_reducer.js' 'failedTasksTelemetry\|getFailedTaskCount' public/js || true)"
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5589:./scripts/_local/phase66a_determinism_check.sh:51:  return reducer.context.window.failedTasksTelemetry.getFailedTaskCount();
./docs/checkpoints/PHASE62B_TASKS_FAILED_LOCAL_VERIFICATION_20260316.md:8:Verify bounded Tasks Failed derivation behavior locally using deterministic event sequences.
./docs/checkpoints/PHASE62B_TASKS_FAILED_LOCAL_VERIFICATION_20260316.md:86:Tasks Failed derivation behavior is deterministically verified across bounded local cases.
./docs/checkpoints/PHASE62B_TASKS_FAILED_STATUS_20260316.md:8:Tasks Failed hardening is now:
./docs/checkpoints/PHASE62B_TASKS_FAILED_STATUS_20260316.md:76:Record a clean handoff for Tasks Failed and preserve the corridor as controlled before selecting any further metric.
./docs/checkpoints/PHASE62B_TASKS_FAILED_STATUS_20260316.md:93:Tasks Failed status is recorded truthfully:
./docs/checkpoints/PHASE63_1_METRIC_SOURCE_AUDIT_FINDINGS_20260312.md:35:### Tasks Running
./docs/checkpoints/PHASE62B_NEXT_TARGET_SELECTION_20260316.md:42:• Tasks Completed (terminal success count)
./docs/checkpoints/PHASE62B_NEXT_TARGET_SELECTION_20260316.md:43:• Tasks Failed (terminal failure count)
./docs/checkpoints/PHASE62B_NEXT_TARGET_SELECTION_20260316.md:51:Tasks Completed
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_METRIC_FINDINGS_20260312.md:6:Initial Phase 63.3 Tasks Running metric audit findings captured from the verified Phase 63.2 golden baseline.
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_METRIC_FINDINGS_20260312.md:10:Primary browser-side Tasks Running metric corridor is:
./docs/checkpoints/PHASE63_3_SEMANTIC_VERIFIER_CANDIDATES_FINDINGS_20260312.md:17:### Candidate 2 — Tasks Running deterministic source
./docs/checkpoints/PHASE62B_SAFE_LABEL_REHYDRATION_STABLE_20260311.md:13:- Tasks Running
./docs/checkpoints/PHASE63_1_COMPLETE_20260312.md:24:  - Tasks Running
./docs/checkpoints/PHASE67C_METRIC_READOUT_BINDING_PLAN_20260316.md:95:window.failedTasksTelemetry?.getFailedTaskCount?.()
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_METRIC_AUDIT_STARTED_20260312.md:6:Continue Phase 63.3 from the verified Phase 63.2 golden baseline with a narrow audit of the Tasks Running metric provenance and idle/null semantics.
./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:7:The Tasks Running, Success Rate, and Latency telemetry binder already exists.
./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:39:Do not implement a new Tasks Running binder.
./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:47:1. idle dashboard shows 0 Tasks Running
./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:48:2. starting a task increments Tasks Running
./docs/checkpoints/PHASE65_EXISTING_METRICS_BINDER_DISCOVERY_20260314.md:49:3. terminal completion decrements Tasks Running
./docs/checkpoints/PHASE62B_CORE_TELEMETRY_HYDRATION_COMPLETE_20260311.md:44:### 2. Tasks Running
./docs/checkpoints/PHASE62B_NEXT_METRIC_SELECTION_20260316_B.md:11:Tasks Completed — COMPLETE pattern
./docs/checkpoints/PHASE62B_NEXT_METRIC_SELECTION_20260316_B.md:12:Tasks Failed — COMPLETE pattern
./docs/checkpoints/STATE_HANDOFF_PHASE63_1_COMPLETE_TO_63_2_20260312.md:93:Tasks Running
./docs/checkpoints/STATE_HANDOFF_PHASE63_1_COMPLETE_TO_63_2_20260312.md:115:  - Tasks Running
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_STARTED_20260312.md:21:- Tasks Running derives from `/events/task-events`
./docs/checkpoints/PHASE62B_DUPLICATE_METRIC_LABELS_REMOVED_20260312.md:12:- Tasks Running
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_EXECUTION_PROTOCOL_20260316.md:8:Harden Tasks Completed derivation using the existing task-events-derived telemetry surface.
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_EXECUTION_PROTOCOL_20260316.md:42:Tasks Completed must count terminal success only.
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_EXECUTION_PROTOCOL_20260316.md:126:Tasks Completed derivation is hardened deterministically inside the existing telemetry surface with no layout, transport, or authority change.
./docs/checkpoints/PHASE63_1_METRIC_SOURCE_AUDIT_STARTED_20260312.md:11:- Tasks Running
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_STATUS_20260316.md:8:Tasks Completed hardening is now:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_STATUS_20260316.md:76:Record a clean handoff for Tasks Completed and preserve the corridor as controlled before selecting any further metric.
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_STATUS_20260316.md:93:Tasks Completed status is recorded truthfully:
./docs/checkpoints/PHASE63_4A_VERIFIER_CANDIDATE_FINDINGS_20260312.md:33:- Tasks Running
./docs/checkpoints/PHASE62B_TASKS_FAILED_EXECUTION_PROTOCOL_20260316.md:8:Harden Tasks Failed derivation using the existing task-events-derived telemetry surface.
./docs/checkpoints/PHASE62B_TASKS_FAILED_EXECUTION_PROTOCOL_20260316.md:25:• preserve current Running Tasks and Tasks Completed derivations unchanged unless required for consistency
./docs/checkpoints/PHASE62B_TASKS_FAILED_EXECUTION_PROTOCOL_20260316.md:42:Tasks Failed must count terminal failure only.
./docs/checkpoints/PHASE62B_TASKS_FAILED_EXECUTION_PROTOCOL_20260316.md:128:Tasks Failed derivation is hardened deterministically inside the existing telemetry surface with no layout, transport, or authority change.
./docs/checkpoints/PHASE62B_FORCE_RESTORE_TO_B03D8B99_20260311.md:18:- Tasks Running still rendered `0`
./docs/checkpoints/PHASE62B_IMPLEMENTATION_STATUS.md:17:Tasks Running:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_HANDOFF_20260316.md:8:Tasks Completed hardening has been implemented in the bounded telemetry surface:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_HANDOFF_20260316.md:64:Tasks Completed may now be treated as:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_HANDOFF_20260316.md:70:Tasks Completed may NOT yet be treated as:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_HANDOFF_20260316.md:108:Record this state as the current handoff anchor for Tasks Completed and preserve the corridor as:
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_HANDOFF_20260316.md:120:Tasks Completed status is preserved truthfully for thread continuation:
./docs/checkpoints/PHASE63_1_SHARED_TASK_EVENTS_METRIC_CORRIDOR_NORMALIZED_20260312.md:12:- Tasks Running hydration
./docs/checkpoints/PHASE63_1_SHARED_TASK_EVENTS_METRIC_CORRIDOR_NORMALIZED_20260312.md:22:  - Tasks Running
./docs/checkpoints/PHASE63_3_SEMANTIC_VERIFIER_CANDIDATES_AUDIT_STARTED_20260312.md:26:- Tasks Running idle semantics
./docs/checkpoints/PHASE62B_NEXT_METRIC_SELECTION_20260316.md:11:Tasks Completed — COMPLETE pattern
./docs/checkpoints/PHASE62B_NEXT_METRIC_SELECTION_20260316.md:28:• Tasks Failed
./docs/checkpoints/PHASE63_3_METRIC_NULL_STATE_CONSISTENCY_FINDINGS_20260312.md:17:### Tasks Running
./docs/checkpoints/PHASE63_3_METRIC_NULL_STATE_CONSISTENCY_FINDINGS_20260312.md:37:  - Tasks Running → `0`
./docs/checkpoints/PHASE62B_TASKS_FAILED_HANDOFF_20260316.md:8:Tasks Failed hardening has been implemented in the bounded telemetry surface:
./docs/checkpoints/PHASE62B_TASKS_FAILED_HANDOFF_20260316.md:64:Tasks Failed may now be treated as:
./docs/checkpoints/PHASE62B_TASKS_FAILED_HANDOFF_20260316.md:70:Tasks Failed may NOT yet be treated as:
./docs/checkpoints/PHASE62B_TASKS_FAILED_HANDOFF_20260316.md:108:Record this state as the current handoff anchor for Tasks Failed and preserve the corridor as:
./docs/checkpoints/PHASE62B_TASKS_FAILED_HANDOFF_20260316.md:120:Tasks Failed status is preserved truthfully for thread continuation:
./docs/checkpoints/STATE_HANDOFF_PHASE63_3_CLOSED_20260312.md:120:2 Tasks Running
./docs/checkpoints/STATE_HANDOFF_PHASE63_3_CLOSED_20260312.md:306:  ← Active Agents, Tasks Running, Success Rate, Latency browser-side corridor
./docs/checkpoints/PHASE62B_METRIC_LABEL_DISAPPEARANCE_FIXED_AT_BINDING_LAYER_20260312.md:12:- Tasks Running
./docs/PHASE62B_RUNNING_TASKS_IMPLEMENTATION_START.md:26:STEP 3 — Add isolated runningTasks derivation
./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:107:runningTasks = size(activeTaskIds)
./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:115:• runningTasks >= 0 always
./docs/PHASE62B_RUNNING_TASKS_EXECUTION_PROTOCOL.md:116:• runningTasks deterministic under replay
./docs/MATILDA_CHAT_LAYOUT_AFTER_REFACTOR.md:21:  - Tasks Running (#metric-tasks)
./public/index.html:83:        <p class="text-sm text-gray-400">Tasks Running</p>
./public/js/task-activity.js:13:                label: 'Tasks Completed',
./public/js/telemetry_failed_tasks_reducer.js:51:  window.failedTasksTelemetry = {
./public/dashboard.html:462:        <p class="phase62-metric-label text-xs uppercase tracking-wide text-gray-400">Tasks Running</p>
./public/dashboard.html.phase61_agent_emoji_backup:390:        <p class="text-sm text-gray-400">Tasks Running</p>
./scripts/_local/phase62b_tasks_failed_local_verification.sh:150:console.log("Verify bounded Tasks Failed derivation behavior locally using deterministic event sequences.");
./scripts/_local/phase62b_tasks_failed_local_verification.sh:181:console.log("Tasks Failed derivation behavior is deterministically verified across bounded local cases.");
./scripts/_local/phase66_metric_collision_check.sh:18:FAILED_SYMBOL_COLLISIONS="$(grep -R --line-number --exclude-dir=.git --exclude='telemetry_failed_tasks_reducer.js' 'failedTasksTelemetry\|getFailedTaskCount' public/js || true)"
./scripts/_local/phase66a_reducer_replay_check.sh:103:  failedReducer.context.window.failedTasksTelemetry.getFailedTaskCount(),
./scripts/_local/repair_phase61_layout_now.sh:148:          <p class="text-sm text-gray-400">Tasks Running</p>
./scripts/_local/phase65c_inspect_queue_depth_target.sh:11:rg -n 'metric-|Tasks Running|Success Rate|Latency|System metrics|tasks|success|latency' public/dashboard.html || true
./scripts/_local/phase62b_running_tasks_candidate_extract.sh:39:    grep -Ei 'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|runningTasks|tasksRunning|telemetry|metric|reducer|dashboard|EventSource|SSE' "$SRC" \
./scripts/_local/phase62b_running_tasks_candidate_extract.sh:53:    grep -Ei 'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|runningTasks|tasksRunning|telemetry|metric|reducer|dashboard|EventSource|SSE' "$SRC" \
./scripts/_local/phase62b_tasks_completed_local_verification.sh:150:console.log("Verify bounded Tasks Completed derivation behavior locally using deterministic event sequences.");
./scripts/_local/phase62b_tasks_completed_local_verification.sh:181:console.log("Tasks Completed derivation behavior is deterministically verified across bounded local cases.");
./scripts/_local/phase62b_running_tasks_surface_shortlist.sh:40:      'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|runningTasks|tasksRunning|telemetry|metric|reducer|EventSource|SSE|dashboard' . \
./scripts/_local/phase62b_running_tasks_surface_shortlist.sh:48:      'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|runningTasks|tasksRunning|telemetry|metric|reducer|EventSource|SSE|dashboard' . \
./scripts/_local/phase66a_determinism_check.sh:51:  return reducer.context.window.failedTasksTelemetry.getFailedTaskCount();
./scripts/_local/phase62b_running_tasks_discovery.sh:40:      'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . || true
./scripts/_local/phase62b_running_tasks_discovery.sh:42:    grep -RInE 'task-events|task\.created|task\.started|task\.running|task\.completed|task\.failed|task\.cancelled|EventSource|SSE|telemetry|metrics|reducer|runningTasks|tasksRunning|dashboard' . \
./scripts/_local/phase62b_running_tasks_discovery.sh:70:  echo "2. Add an isolated runningTasks derivation helper only in that surface."
./scripts/_local/phase65_3_metrics_runtime_verify.sh:176:  echo "2. Confirm Tasks Running shows 0 when idle."
./scripts/_local/phase65_3_metrics_runtime_verify.sh:179:  echo "5. Confirm Tasks Running increments during active lifecycle."
./scripts/_local/phase65_3_metrics_runtime_verify.sh:180:  echo "6. Confirm terminal event decrements Tasks Running."
./scripts/_local/phase62b_tasks_queued_discovery.sh:34:  "Tasks Running|Tasks Completed|Tasks Failed|runningTasks|completedTasks|failedTasks|tasksRunning|tasksCompleted|tasksFailed" \
./scripts/_local/phase62b_bind_metric_renderers_to_dedicated_value_nodes.sh:26:    "Tasks Running": "metric-tasks",
./scripts/_local/phase65_2_tasks_running_focus_inspect.sh:4:echo "== Phase 65 focused inspection: Tasks Running / Success Rate hooks =="
./scripts/_local/phase65_2_tasks_running_focus_inspect.sh:8:nl -ba public/dashboard.html | sed -n '1,260p' | grep -nE 'Tasks Running|Success Rate|Latency|task-count|success-rate|latency|metric|tile' || true
./scripts/_local/phase65_1_tasks_running_inspect.sh:4:echo "== Phase 65 Tasks Running inspection =="
./scripts/_local/phase65_1_tasks_running_inspect.sh:13:rg -n 'Tasks Running|Success Rate|Latency|metric|tile' public/dashboard.html public/js public/css || true
./scripts/_local/phase65_1_tasks_running_inspect.sh:37:echo "Inspect whether Tasks Running tile already has a stable DOM hook in public/dashboard.html."
./scripts/_local/fix_phase62_safe_metric_label_rehydration.sh:26:    'Tasks Running',
./scripts/_local/fix_phase62_metrics_tile_labels.sh:26:    "Tasks Running",
./scripts/_local/phase65_5_post_patch_verify.sh:39:  echo "3. Confirm Tasks Running is not blank"
./scripts/_local/fix_phase62_metric_label_binding_layer.sh:20:    "Tasks Running": "metric-tasks",
./scripts/_local/remove_phase62_duplicate_metric_labels.sh:15:    "Tasks Running",
./.git/logs/HEAD:75:f52d98b914ad6ff1d767f2fc6df1f8ca81042d01 616da310534bf425337d9cba4982215d774ec320 Marcela Gama Le <marcelagamale@gmail.com> 1773288024 -0700	commit: Hydrate Tasks Running metric from task-events stream
./.git/logs/HEAD:195:a9f09dae2cf469406360b73fd1f5e37649c1bc58 0653fab102b177fdc8d3091b6051aca28312742e Marcela Gama Le <marcelagamale@gmail.com> 1773370678 -0700	commit: Phase 62B: hydrate Tasks Running metric from task-events
./.git/logs/HEAD:274:80ae1909d48299c68ae519e711ee33370a676119 63e686a602266a8d0f59f61b486c7c1c036095db Marcela Gama Le <marcelagamale@gmail.com> 1773506823 -0700	commit: Phase 65: Tasks Running telemetry hydration plan (data binding only, protection corridor preserved)
./.git/logs/HEAD:275:63e686a602266a8d0f59f61b486c7c1c036095db 28122f571c9f92a688b3ea79b177c80605aba2d2 Marcela Gama Le <marcelagamale@gmail.com> 1773506882 -0700	commit: Phase 65: lock Tasks Running execution start and protection constraints
./.git/logs/HEAD:276:28122f571c9f92a688b3ea79b177c80605aba2d2 7076dbd3ca09f0de27deaeb3a5c6b2af90c76cf7 Marcela Gama Le <marcelagamale@gmail.com> 1773506971 -0700	commit: Phase 65: add Tasks Running inspection runner before telemetry binding
./.git/logs/refs/heads/phase63-telemetry-enrichment:87:a9f09dae2cf469406360b73fd1f5e37649c1bc58 0653fab102b177fdc8d3091b6051aca28312742e Marcela Gama Le <marcelagamale@gmail.com> 1773370678 -0700	commit: Phase 62B: hydrate Tasks Running metric from task-events
./.git/logs/refs/heads/phase63-telemetry-enrichment:166:80ae1909d48299c68ae519e711ee33370a676119 63e686a602266a8d0f59f61b486c7c1c036095db Marcela Gama Le <marcelagamale@gmail.com> 1773506823 -0700	commit: Phase 65: Tasks Running telemetry hydration plan (data binding only, protection corridor preserved)
./.git/logs/refs/heads/phase63-telemetry-enrichment:167:63e686a602266a8d0f59f61b486c7c1c036095db 28122f571c9f92a688b3ea79b177c80605aba2d2 Marcela Gama Le <marcelagamale@gmail.com> 1773506882 -0700	commit: Phase 65: lock Tasks Running execution start and protection constraints
./.git/logs/refs/heads/phase63-telemetry-enrichment:168:28122f571c9f92a688b3ea79b177c80605aba2d2 7076dbd3ca09f0de27deaeb3a5c6b2af90c76cf7 Marcela Gama Le <marcelagamale@gmail.com> 1773506971 -0700	commit: Phase 65: add Tasks Running inspection runner before telemetry binding
./.git/logs/refs/heads/phase62-dashboard-layout:37:f52d98b914ad6ff1d767f2fc6df1f8ca81042d01 616da310534bf425337d9cba4982215d774ec320 Marcela Gama Le <marcelagamale@gmail.com> 1773288024 -0700	commit: Hydrate Tasks Running metric from task-events stream

-- queued/pending status references --
./PHASE39_PROJECT_INDEX_UPDATE.md:5:Status: Complete (pending PR merge + golden tag)
./public_broken_rollback_1762824556/agent-status-row.js:45:    console.warn("⚠️ Could not find Dashboard v2.0.3 anchor — appending to body instead");
./PHASE33_SUPERVISOR_SUMMARY.md:4:Enable dockerized workers (workerA/workerB) to claim tasks created with status `delegated`, without breaking queued-task behavior, and keep `task_events.task_id` stable as the string task id (e.g., `t51`) rather than numeric row ids.
./PHASE33_SUPERVISOR_SUMMARY.md:8:  - `WHERE status IN ('queued','delegated')`
./ops-sse-bootstrap.ts:13:    status TEXT DEFAULT 'pending',
./PHASE40_NEXT.md:11:     - status = 'queued'
./PHASE40_NEXT.md:20:     - inserts Tier A + Tier B queued tasks
./PHASE40_NEXT.md:22:     - proves Tier B remains queued
./PHASE11_OPS_EVENT_STREAM_VERIFICATION.md:126:  * Pill should eventually move to **STALE** or **NO SIGNAL** depending on your existing logic (unchanged from baseline).
./memory/archive_desktop/matilda_task_log.txt:11:[2025-07-30T23:19:18.952Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:12:[2025-07-30T23:19:48.953Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:13:[2025-07-30T23:20:18.954Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:14:[2025-07-30T23:20:48.955Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:15:[2025-07-30T23:21:18.957Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:16:[2025-07-30T23:21:48.958Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:17:[2025-07-30T23:22:18.959Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:18:[2025-07-30T23:22:48.960Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:19:[2025-07-30T23:23:18.961Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:20:[2025-07-30T23:23:48.962Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:21:[2025-07-30T23:24:18.963Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:26:[2025-07-30T23:25:18.965Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:27:[2025-07-30T23:32:12.999Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:28:[2025-07-30T23:32:42.999Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:29:[2025-07-30T23:33:57.437Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:34:[2025-07-30T23:34:27.437Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:35:[2025-07-30T23:34:57.439Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:40:[2025-07-30T23:36:18.050Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:41:[2025-07-30T23:36:48.051Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:42:[2025-07-30T23:37:18.053Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:43:[2025-07-30T23:37:48.054Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:44:[2025-07-30T23:38:18.055Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:45:[2025-07-30T23:38:48.057Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:46:[2025-07-30T23:48:56.963Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:47:[2025-07-30T23:49:26.969Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:48:[2025-07-30T23:49:56.971Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:49:[2025-07-30T23:50:26.972Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:50:[2025-07-30T23:50:56.974Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:51:[2025-07-30T23:51:26.975Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:52:[2025-07-30T23:51:56.976Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:53:[2025-07-30T23:52:26.977Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:54:[2025-07-30T23:52:56.978Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:55:[2025-07-30T23:53:26.979Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:56:[2025-07-30T23:53:56.980Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:57:[2025-07-30T23:54:26.981Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:58:[2025-07-30T23:54:56.983Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:59:[2025-07-30T23:55:26.984Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:60:[2025-07-30T23:55:56.986Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:61:[2025-07-30T23:56:26.986Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:62:[2025-07-30T23:56:56.988Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:63:[2025-07-30T23:57:26.989Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:64:[2025-07-30T23:57:56.990Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:65:[2025-07-31T00:13:40.618Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:66:[2025-07-31T00:14:10.613Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:67:[2025-07-31T00:32:06.498Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:68:[2025-07-31T00:48:22.836Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:69:[2025-07-31T01:15:29.992Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:70:[2025-07-31T01:30:55.921Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:71:[2025-07-31T02:03:06.939Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:72:[2025-07-31T02:16:55.210Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:73:[2025-07-31T02:17:25.220Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:74:[2025-07-31T02:17:55.223Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:75:[2025-07-31T02:18:25.224Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:76:[2025-07-31T02:18:55.226Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:77:[2025-07-31T02:23:13.253Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:78:[2025-07-31T02:40:10.786Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:79:[2025-07-31T03:00:42.217Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:80:[2025-07-31T03:01:23.617Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:81:[2025-07-31T03:01:53.618Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:86:[2025-07-31T03:02:53.620Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:87:[2025-07-31T03:03:37.849Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:98:[2025-07-31T03:04:37.850Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:99:[2025-07-31T03:05:07.852Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:108:[2025-07-31T03:06:07.854Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:109:[2025-07-31T03:06:37.855Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:110:[2025-07-31T03:07:07.856Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:111:[2025-07-31T03:07:37.857Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:112:[2025-07-31T03:08:07.858Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:113:[2025-07-31T03:41:51.479Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:122:[2025-07-31T03:42:51.452Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:131:[2025-07-31T03:43:51.454Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:132:[2025-07-31T03:44:21.454Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:133:[2025-07-31T03:44:51.455Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:134:[2025-07-31T03:45:21.455Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:135:[2025-07-31T03:45:51.457Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:136:[2025-07-31T03:47:01.713Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:137:[2025-07-31T03:49:23.197Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:138:[2025-07-31T03:49:53.199Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:155:[2025-07-31T03:51:23.201Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:156:[2025-07-31T03:51:53.201Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:157:[2025-07-31T03:52:23.202Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:158:[2025-07-31T03:53:13.330Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:159:[2025-07-31T03:53:43.331Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:160:[2025-07-31T03:54:13.332Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:161:[2025-07-31T03:54:43.333Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:162:[2025-07-31T03:55:13.333Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:163:[2025-07-31T03:55:43.334Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:164:[2025-07-31T03:56:13.336Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:165:[2025-07-31T04:06:14.813Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:166:[2025-07-31T04:06:44.826Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:167:[2025-07-31T04:07:14.830Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:168:[2025-07-31T04:07:48.082Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:169:[2025-07-31T04:08:18.083Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:170:[2025-07-31T04:08:48.083Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:171:[2025-07-31T04:09:18.085Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:172:[2025-07-31T04:18:47.761Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:173:[2025-07-31T04:28:14.464Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:174:[2025-07-31T04:28:44.477Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:175:[2025-07-31T04:29:34.107Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:176:[2025-07-31T04:30:08.166Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:177:[2025-07-31T04:30:46.638Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:178:[2025-07-31T04:31:23.038Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:179:[2025-07-31T04:31:53.039Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:180:[2025-07-31T04:32:23.041Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:181:[2025-07-31T04:32:53.041Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:182:[2025-07-31T04:33:23.043Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:183:[2025-07-31T04:33:53.044Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:194:[2025-07-31T04:34:53.046Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:195:[2025-07-31T04:35:23.047Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:196:[2025-07-31T04:35:53.048Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:197:[2025-07-31T04:36:23.049Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:198:[2025-07-31T04:37:55.106Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:199:[2025-07-31T04:38:25.106Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:220:[2025-07-31T04:39:25.109Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:221:[2025-07-31T04:39:55.110Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:222:[2025-07-31T04:40:25.111Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:223:[2025-07-31T04:40:55.112Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:224:[2025-07-31T04:41:25.114Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:225:[2025-07-31T04:41:55.115Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:226:[2025-07-31T04:42:25.115Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:227:[2025-07-31T04:42:55.116Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:228:[2025-07-31T04:43:25.118Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:229:[2025-07-31T04:43:55.119Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:230:[2025-07-31T04:44:25.120Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:231:[2025-07-31T04:44:55.122Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:232:[2025-07-31T04:45:25.122Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:233:[2025-07-31T04:45:55.123Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:234:[2025-07-31T04:46:25.125Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:235:[2025-07-31T04:46:55.125Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:236:[2025-07-31T04:47:25.127Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:237:[2025-07-31T04:47:55.128Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:238:[2025-07-31T04:48:25.130Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:239:[2025-07-31T04:48:55.132Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:240:[2025-07-31T04:49:25.132Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:241:[2025-07-31T04:49:55.133Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:242:[2025-07-31T04:50:25.134Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:243:[2025-07-31T04:50:55.136Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:244:[2025-07-31T04:51:25.136Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:245:[2025-07-31T04:51:55.138Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:246:[2025-07-31T04:52:25.139Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:247:[2025-07-31T04:52:55.140Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:248:[2025-07-31T04:53:25.141Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:249:[2025-07-31T04:53:55.142Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:250:[2025-07-31T04:54:25.143Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:251:[2025-07-31T04:54:55.144Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:252:[2025-07-31T04:55:25.145Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:253:[2025-07-31T04:55:55.145Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:254:[2025-07-31T04:56:25.147Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:255:[2025-07-31T04:56:55.148Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:256:[2025-07-31T04:57:25.149Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:257:[2025-07-31T04:57:55.150Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:258:[2025-07-31T04:58:25.152Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:259:[2025-07-31T04:58:55.152Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:260:[2025-07-31T04:59:25.154Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:261:[2025-07-31T04:59:55.154Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:262:[2025-07-31T05:00:25.156Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:263:[2025-07-31T05:00:55.157Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:264:[2025-07-31T05:01:25.158Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:265:[2025-07-31T05:01:55.159Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:266:[2025-07-31T05:02:25.160Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:267:[2025-07-31T05:02:55.161Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:268:[2025-07-31T05:07:56.902Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:269:[2025-07-31T05:10:23.821Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:270:[2025-07-31T05:10:53.816Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:271:[2025-07-31T05:11:23.817Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:272:[2025-07-31T05:11:53.817Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:273:[2025-07-31T05:12:23.819Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:274:[2025-07-31T05:12:53.819Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:275:[2025-07-31T05:13:23.820Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:276:[2025-07-31T05:13:53.821Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:277:[2025-07-31T05:14:23.821Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:278:[2025-07-31T05:14:53.822Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:279:[2025-07-31T05:15:23.824Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:280:[2025-07-31T05:15:53.824Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:281:[2025-07-31T05:16:23.825Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:282:[2025-07-31T05:16:53.826Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:283:[2025-07-31T05:17:23.826Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:284:[2025-07-31T05:17:53.827Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:285:[2025-07-31T05:34:51.370Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:286:[2025-07-31T05:52:49.205Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:287:[2025-07-31T06:10:09.729Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:288:[2025-07-31T06:25:31.585Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:289:[2025-07-31T06:47:27.768Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:290:[2025-07-31T06:47:57.769Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:291:[2025-07-31T06:48:27.770Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:292:[2025-07-31T06:48:57.769Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:293:[2025-07-31T06:49:27.770Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:294:[2025-07-31T06:49:57.770Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:295:[2025-07-31T06:50:27.771Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:296:[2025-07-31T06:50:57.771Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:297:[2025-07-31T06:51:27.772Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:298:[2025-07-31T06:51:57.772Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:299:[2025-07-31T18:08:35.421Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:300:[2025-07-31T18:09:34.776Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:301:[2025-07-31T18:10:04.777Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:302:[2025-07-31T18:10:34.778Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:303:[2025-07-31T18:11:04.780Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:304:[2025-07-31T18:11:34.781Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:305:[2025-07-31T18:12:04.781Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:306:[2025-07-31T18:12:34.783Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:307:[2025-07-31T18:13:04.784Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:308:[2025-07-31T18:13:34.785Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:309:[2025-07-31T18:14:04.786Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:310:[2025-07-31T18:14:34.788Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:311:[2025-07-31T18:15:04.788Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:312:[2025-07-31T18:15:34.789Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:313:[2025-07-31T18:16:04.790Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:314:[2025-07-31T18:16:34.791Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:315:[2025-07-31T18:17:04.792Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:316:[2025-07-31T18:17:34.794Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:317:[2025-07-31T18:18:04.795Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:318:[2025-07-31T18:18:34.797Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:319:[2025-07-31T18:19:04.798Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:320:[2025-07-31T18:19:34.800Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:321:[2025-07-31T18:20:04.783Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:322:[2025-07-31T18:20:34.780Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:323:[2025-07-31T18:21:04.781Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:324:[2025-07-31T18:21:34.782Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:325:[2025-07-31T18:22:04.782Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:326:[2025-07-31T18:22:34.783Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:327:[2025-07-31T18:23:04.784Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:328:[2025-07-31T18:23:34.785Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:329:[2025-07-31T18:24:04.786Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:330:[2025-07-31T18:24:34.787Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:331:[2025-07-31T18:25:04.788Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:332:[2025-07-31T18:25:34.789Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:333:[2025-07-31T18:26:04.789Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:334:[2025-07-31T18:26:34.791Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:335:[2025-07-31T18:27:04.793Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:336:[2025-07-31T18:27:34.794Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:337:[2025-07-31T18:28:04.795Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:338:[2025-07-31T18:28:34.796Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:339:[2025-07-31T18:29:04.796Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:340:[2025-07-31T18:29:34.798Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:341:[2025-07-31T18:30:04.798Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:342:[2025-07-31T18:30:34.799Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:343:[2025-07-31T18:31:04.800Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:344:[2025-07-31T18:31:34.801Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:345:[2025-07-31T18:32:04.802Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:346:[2025-07-31T18:32:34.803Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:347:[2025-07-31T18:33:04.803Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:348:[2025-07-31T18:33:34.805Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:349:[2025-07-31T18:34:04.805Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:350:[2025-07-31T18:34:34.807Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:351:[2025-07-31T18:35:04.808Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:352:[2025-07-31T18:35:34.808Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:353:[2025-07-31T18:36:04.809Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:354:[2025-07-31T18:36:34.810Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:355:[2025-07-31T18:37:04.811Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:356:[2025-07-31T18:37:34.811Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:357:[2025-07-31T18:38:04.812Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:358:[2025-07-31T18:38:34.814Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:359:[2025-07-31T18:39:04.815Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:360:[2025-07-31T18:39:34.815Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:361:[2025-07-31T18:40:04.817Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:362:[2025-07-31T18:40:34.817Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:363:[2025-07-31T18:41:04.818Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:364:[2025-07-31T18:41:34.819Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:365:[2025-07-31T18:42:04.820Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:366:[2025-07-31T18:42:34.821Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:367:[2025-07-31T18:43:04.822Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:368:[2025-07-31T18:43:34.822Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:369:[2025-07-31T18:44:04.824Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:370:[2025-07-31T18:44:34.824Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:371:[2025-07-31T18:45:04.826Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:372:[2025-07-31T18:45:34.827Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:373:[2025-07-31T18:46:04.829Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:374:[2025-07-31T18:46:34.830Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:375:[2025-07-31T18:47:04.831Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:376:[2025-07-31T18:47:34.832Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:377:[2025-07-31T18:48:04.833Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:378:[2025-07-31T18:48:34.833Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:379:[2025-07-31T18:49:04.835Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:380:[2025-07-31T18:49:34.842Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:381:[2025-07-31T18:50:04.851Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:382:[2025-07-31T18:50:34.853Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:383:[2025-07-31T18:51:04.854Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:384:[2025-07-31T18:51:34.855Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:385:[2025-07-31T18:52:04.856Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:386:[2025-07-31T18:52:34.856Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:387:[2025-07-31T18:53:04.858Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:388:[2025-07-31T18:53:34.858Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:389:[2025-07-31T18:54:04.859Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:390:[2025-07-31T18:54:34.861Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:391:[2025-07-31T18:55:04.861Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:392:[2025-07-31T18:55:34.862Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:393:[2025-07-31T18:56:04.864Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:394:[2025-07-31T18:56:34.864Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:395:[2025-07-31T18:57:04.865Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:396:[2025-07-31T18:57:34.866Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:397:[2025-07-31T18:58:04.867Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:398:[2025-07-31T18:58:34.868Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:399:[2025-07-31T18:59:04.869Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:400:[2025-07-31T18:59:34.870Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:401:[2025-07-31T19:00:04.871Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:402:[2025-07-31T19:00:34.872Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:403:[2025-07-31T19:01:04.873Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:404:[2025-07-31T19:01:34.874Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:405:[2025-07-31T19:02:04.875Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:406:[2025-07-31T19:02:34.877Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:407:[2025-07-31T19:03:04.878Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:408:[2025-07-31T19:03:34.878Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:409:[2025-07-31T19:04:04.880Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:410:[2025-07-31T19:04:34.880Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:411:[2025-07-31T19:05:04.881Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:412:[2025-07-31T19:05:34.883Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:413:[2025-07-31T19:06:04.884Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:414:[2025-07-31T19:06:34.885Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:415:[2025-07-31T19:07:04.885Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:416:[2025-07-31T19:07:34.887Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:417:[2025-07-31T19:08:04.888Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:418:[2025-07-31T19:08:34.889Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:419:[2025-07-31T19:09:04.889Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:420:[2025-07-31T19:09:34.891Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:421:[2025-07-31T19:10:04.892Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:422:[2025-07-31T19:10:34.893Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:423:[2025-07-31T19:11:04.894Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:424:[2025-07-31T19:11:34.896Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:425:[2025-07-31T19:12:04.896Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:426:[2025-07-31T19:12:34.898Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:427:[2025-07-31T19:13:04.900Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:428:[2025-07-31T19:13:34.900Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:429:[2025-07-31T19:14:04.901Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:430:[2025-07-31T19:14:34.903Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:431:[2025-07-31T19:15:04.903Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:432:[2025-07-31T19:15:34.905Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:433:[2025-07-31T19:16:04.906Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:434:[2025-07-31T19:16:34.908Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:435:[2025-07-31T19:17:04.909Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:436:[2025-07-31T19:17:34.911Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:437:[2025-07-31T19:18:04.912Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:438:[2025-07-31T19:18:34.913Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:439:[2025-07-31T19:19:04.914Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:440:[2025-07-31T19:19:34.927Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:441:[2025-07-31T19:20:04.934Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:442:[2025-07-31T19:20:34.936Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:443:[2025-07-31T19:21:04.937Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:444:[2025-07-31T19:21:34.938Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:445:[2025-07-31T19:22:04.939Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:446:[2025-07-31T19:22:34.940Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:447:[2025-07-31T19:23:04.941Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:448:[2025-07-31T19:23:34.942Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:449:[2025-07-31T19:24:04.944Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:450:[2025-07-31T19:24:34.946Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:451:[2025-07-31T19:25:04.947Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:452:[2025-07-31T19:25:34.948Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:453:[2025-07-31T19:26:04.949Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:454:[2025-07-31T19:26:34.951Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:455:[2025-07-31T19:27:04.952Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:456:[2025-07-31T19:27:34.953Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:457:[2025-07-31T19:28:04.955Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:458:[2025-07-31T19:28:34.956Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:459:[2025-07-31T19:29:04.957Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:460:[2025-07-31T19:29:34.957Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:461:[2025-07-31T19:30:04.958Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:462:[2025-07-31T19:30:34.960Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:463:[2025-07-31T19:31:04.960Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:464:[2025-07-31T19:31:34.962Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:465:[2025-07-31T19:32:04.964Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:466:[2025-07-31T19:32:34.965Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:467:[2025-07-31T19:33:04.967Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:468:[2025-07-31T19:33:34.968Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:469:[2025-07-31T19:34:04.969Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:470:[2025-07-31T19:34:34.970Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:471:[2025-07-31T19:35:04.971Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:472:[2025-07-31T19:35:34.972Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:473:[2025-07-31T19:36:04.973Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:474:[2025-07-31T19:36:34.974Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:475:[2025-07-31T19:37:04.975Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:476:[2025-07-31T19:37:34.977Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:477:[2025-07-31T19:38:04.978Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:478:[2025-07-31T19:38:34.979Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:479:[2025-07-31T19:39:04.981Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:480:[2025-07-31T19:39:34.981Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:481:[2025-07-31T19:40:04.984Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:482:[2025-07-31T19:40:34.987Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:483:[2025-07-31T19:41:04.989Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:484:[2025-07-31T19:41:34.992Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:485:[2025-07-31T19:42:04.993Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:486:[2025-07-31T19:42:34.994Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:487:[2025-07-31T19:43:04.997Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:488:[2025-07-31T19:43:35.000Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:489:[2025-07-31T19:44:05.002Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:490:[2025-07-31T19:44:35.003Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:491:[2025-07-31T19:45:05.005Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:492:[2025-07-31T20:01:59.897Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:493:[2025-07-31T20:02:29.899Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:494:[2025-07-31T20:02:59.901Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:495:[2025-07-31T20:03:29.903Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:496:[2025-07-31T20:03:59.904Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:497:[2025-07-31T20:04:29.906Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:498:[2025-07-31T20:04:59.907Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:499:[2025-07-31T20:11:53.220Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:500:[2025-07-31T20:12:23.221Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:501:[2025-07-31T20:12:53.222Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:502:[2025-07-31T20:13:23.222Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:503:[2025-07-31T20:13:53.223Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:504:[2025-07-31T20:14:23.224Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:505:[2025-07-31T20:14:53.225Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:506:[2025-07-31T20:15:23.227Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:507:[2025-07-31T20:15:53.227Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:508:[2025-07-31T20:16:23.228Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:509:[2025-07-31T20:16:53.229Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:510:[2025-07-31T20:17:23.229Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:511:[2025-07-31T20:17:53.230Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:512:[2025-07-31T20:18:23.230Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:513:[2025-07-31T20:18:53.232Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:514:[2025-07-31T20:19:23.233Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:515:[2025-07-31T20:19:53.233Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:516:[2025-07-31T20:20:23.234Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:517:[2025-07-31T20:20:53.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:518:[2025-07-31T20:21:23.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:519:[2025-07-31T20:21:53.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:520:[2025-07-31T20:22:23.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:521:[2025-07-31T20:22:53.234Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:522:[2025-07-31T20:23:23.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:523:[2025-07-31T20:23:53.235Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:524:[2025-07-31T20:24:23.236Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:525:[2025-07-31T20:24:53.236Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:526:[2025-07-31T20:25:23.237Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:527:[2025-07-31T20:25:53.237Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:528:[2025-07-31T20:26:23.238Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:529:[2025-07-31T20:26:53.239Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:530:[2025-07-31T20:27:23.240Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:531:[2025-07-31T20:27:53.241Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:532:[2025-07-31T20:28:23.241Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:533:[2025-07-31T20:28:53.242Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:534:[2025-07-31T20:29:23.243Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:535:[2025-07-31T20:29:53.243Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:536:[2025-07-31T20:30:23.244Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:537:[2025-07-31T20:30:53.244Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:538:[2025-07-31T20:31:23.245Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:539:[2025-07-31T20:32:11.124Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:540:[2025-07-31T20:32:41.125Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:541:[2025-07-31T20:33:11.125Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:542:[2025-07-31T20:33:41.191Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:543:[2025-07-31T20:34:11.192Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:544:[2025-07-31T20:34:41.194Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:545:[2025-07-31T20:35:38.757Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:546:[2025-07-31T20:36:08.758Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:547:[2025-07-31T20:36:38.759Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:548:[2025-07-31T20:37:08.761Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:549:[2025-07-31T20:37:38.762Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:550:[2025-07-31T20:38:08.763Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:551:[2025-07-31T20:38:38.764Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:552:[2025-07-31T20:39:08.765Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:553:[2025-07-31T20:39:38.767Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:554:[2025-07-31T20:40:08.768Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:555:[2025-07-31T20:40:38.769Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:556:[2025-07-31T20:41:08.771Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:557:[2025-07-31T20:41:38.772Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:558:[2025-07-31T20:42:08.774Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:559:[2025-07-31T20:42:38.775Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:560:[2025-07-31T20:43:08.775Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:561:[2025-07-31T20:43:38.777Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:562:[2025-07-31T20:44:08.777Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:563:[2025-07-31T20:44:38.779Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:564:[2025-07-31T20:45:08.780Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:565:[2025-07-31T20:45:38.781Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:566:[2025-07-31T20:46:08.782Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:567:[2025-07-31T20:46:38.783Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:568:[2025-07-31T20:47:08.784Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:569:[2025-07-31T20:47:38.785Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:570:[2025-07-31T20:48:08.786Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:571:[2025-07-31T20:48:38.787Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:572:[2025-07-31T20:49:08.789Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:573:[2025-07-31T20:49:38.789Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:574:[2025-07-31T20:50:08.791Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:575:[2025-07-31T20:50:38.792Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:576:[2025-07-31T20:51:08.793Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:577:[2025-07-31T20:51:38.795Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:578:[2025-07-31T20:52:08.795Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:579:[2025-07-31T20:52:38.797Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:580:[2025-07-31T20:53:08.799Z] ℹ️ No pending tasks.
./memory/archive_desktop/matilda_task_log.txt:581:[2025-07-31T20:53:38.800Z] ℹ️ No pending tasks.
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:6:Run the queued-metric discovery script.
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:7:Capture exact queued-state definition.
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:12:./scripts/_local/phase62b_tasks_queued_discovery.sh | tee /tmp/phase62b_tasks_queued_discovery.out
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:17:2 Exact queued or pending status literal
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:25:queued-state definition is explicit
./PHASE62B_TASKS_QUEUED_DISCOVERY_RUNBOOK.md:37:/tmp/phase62b_tasks_queued_discovery.out
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:42:- public/js/phase23_matilda_delegate_taskspec.js:22:      try { fn(`📨 delegated → task.queued [${tid}]`); } catch (_) {}
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:67:- public/js/phase22_task_delegation_live_bindings.js:32:      (ev?.kind === "task.created" ? "queued" : null);
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:68:- public/js/phase22_task_delegation_live_bindings.js:73:    let queued = 0, done = 0, failed = 0;
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:110:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:133:- public/bundle.js:1309:      const status = ev?.status ?? ev?.payload?.status ?? t?.status ?? (ev?.kind === "task.created" ? "queued" : null);
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:134:- public/bundle.js:1340:      let queued = 0, done = 0, failed = 0;
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:148:- public/js/phase23_matilda_task_events_bridge.js:59:    if (status in { queued: 1, running: 1, started: 1 }) return { kind: "task.progress", task: merged, id };
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:176:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:215:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:225:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./snapshots/20251114-114821/public/js/dashboard-stream.js:31:      status: "pending"
./snapshots/20251114-114821/scripts/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./snapshots/20251114-114821/scripts/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./snapshots/20251114-114821/scripts/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./snapshots/20251114-114821/scripts/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./snapshots/20251114-114821/scripts/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./snapshots/20251114-114821/scripts/_local/route-loader/ops-sse-bootstrap.ts:13:    status TEXT DEFAULT 'pending',
./snapshots/20251114-114821/routes/delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./snapshots/20251114-114707/public/js/dashboard-stream.js:31:      status: "pending"
./snapshots/20251114-114707/scripts/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./snapshots/20251114-114707/scripts/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./snapshots/20251114-114707/scripts/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./snapshots/20251114-114707/scripts/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./snapshots/20251114-114707/scripts/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./snapshots/20251114-114707/scripts/_local/route-loader/ops-sse-bootstrap.ts:13:    status TEXT DEFAULT 'pending',
./snapshots/20251114-114707/routes/delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./PHASE25_FOLD_FUNCTION_SPEC.md:31:- NEW -> QUEUED (on "task.created" or "task.queued")
./PHASE11_DASHBOARD_VISUAL_CHECK_STATUS.md:9:* Visual check in the browser is **still pending**.
./PHASE11_DASHBOARD_VISUAL_CHECK_STATUS.md:35:* Change “Visual check in the browser is **still pending**” to:
./ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./routes_backup/delegations.ts:15:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./PHASE33_HANDOFF_ONE_LINER.txt:1:We are on feature/phase26-task-worker-min (pushed). Phase33 fixed docker workers claiming delegated tasks: phase32_claim_one.sql now claims WHERE status IN ('queued','delegated') ... FOR UPDATE SKIP LOCKED; docker-compose.worker.phase32.yml bind-mounts ./server/worker:/app/server/worker:ro for workerA+workerB; phase26_task_worker.mjs emits task_id as (row.task_id || String(row.id)) to keep stable string ids in task_events. Verified: sha256 parity host==workerA==workerB; 20 delegated → completed with clean task.created→task.running→task.completed, no duplicate terminal events, no numeric task_id drift. Commits in lane include 4bede779, 9543798b, ec70766e, plus 9c6c4251 adding PHASE33_SUPERVISOR_SUMMARY.md.
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:21:tasks where status = 'queued'
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:23:tasks where run_state = 'pending'
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:30:WHERE status = 'queued';
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:36:1 Identify exact queued definition from tasks table.
./PHASE25_UI_INFERENCE_AUDIT.md:79:- public/bundle.js:1340:      let queued = 0, done = 0, failed = 0;
./PHASE25_UI_INFERENCE_AUDIT.md:91:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT.md:130:- public/js/phase22_task_delegation_live_bindings.js:73:    let queued = 0, done = 0, failed = 0;
./PHASE25_UI_INFERENCE_AUDIT.md:143:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT.md:180:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./PHASE25_UI_INFERENCE_AUDIT.md:186:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./patent/unique-claims.md:19:Additional claims pending UI layer, remote access gateway, and modular scheduler.
./PHASE62B_TASKS_QUEUED_DISCOVERY_CAPTURE.md:6:Execute the queued-metric discovery pass and persist the output for review.
./PHASE62B_TASKS_QUEUED_DISCOVERY_CAPTURE.md:10:./scripts/_local/phase62b_tasks_queued_discovery.sh | tee /tmp/phase62b_tasks_queued_discovery.out
./PHASE62B_TASKS_QUEUED_DISCOVERY_CAPTURE.md:14:cp /tmp/phase62b_tasks_queued_discovery.out ./PHASE62B_TASKS_QUEUED_DISCOVERY_OUTPUT.txt
./PHASE62B_TASKS_QUEUED_DISCOVERY_CAPTURE.md:18:Confirm exact queued-state literal.
./PHASE62B_TASKS_QUEUED_DISCOVERY_CAPTURE.md:25:If queued-state definition is ambiguous:
./delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./server/taskContract.mjs:11:  "queued",
./server/taskContract.mjs:32:  // tolerate empty/unknown -> queued
./server/taskContract.mjs:33:  return "queued";
./server/taskContract.mjs:85:    queued: ["delegated", "canceled", "failed"],
./server/tasks-mutations.mjs:34:  const status = "queued";
./server/task-events.mjs:13:  "task.queued",
./server/task-events.mjs:153:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./server/worker/phase27_claim_one.sql:1:-- phase27: claim one queued task
./server/worker/phase27_claim_one.sql:7:  where status = 'queued'
./server/worker/phase32_claim_one.sql:10:  WHERE status IN ('queued','delegated')
./server/worker/phase27_mark_failure.sql:16:    status = case when row.attempt < row.max_attempts then 'queued' else 'failed' end,
./server/worker/phase28_claim_one.sql:8:  where status = 'queued'
./server/worker/phase28_mark_failure.sql:3:-- $2 status ('queued' | 'failed')
./server/routes/api-tasks-postgres.mjs:112:        b.status ?? "queued",
./server/routes/phase48_policy_probe.mjs:37:      values ($1, $2, 'queued', $3, 'A', $4)
./server/orchestrator/phase18_tick.mjs:18:    // Minimal “work”: drain at most one queued item per tick (future hook).
./server/sql/phase39_claim_one_with_value_gate.sql:4:--  - If a queued Tier B/C task exists, return it as REFUSE (no mutation).
./server/sql/phase39_claim_one_with_value_gate.sql:5:--  - Otherwise, claim a queued Tier A (or NULL) task via UPDATE ... RETURNING.
./server/sql/phase39_claim_one_with_value_gate.sql:22:  WHERE t.status = 'queued'
./server/sql/phase39_claim_one_with_value_gate.sql:31:  WHERE t.status = 'queued'
./scripts_backup/demo/run-create-atlas.ts:12:  const status = "queued";
./scripts_backup/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./scripts_backup/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./scripts_backup/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./scripts_backup/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./scripts_backup/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./scripts_backup/scripts/demo/run-create-atlas.ts:12:  const status = "queued";
./scripts_backup/scripts/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./scripts_backup/scripts/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./scripts_backup/scripts/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./scripts_backup/scripts/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./scripts_backup/scripts/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./scripts_backup/scripts/_local/route-loader/ops-sse-bootstrap.ts:17:    status TEXT DEFAULT 'pending',
./scripts_backup/_local/route-loader/ops-sse-bootstrap.ts:17:    status TEXT DEFAULT 'pending',
./docs/PHASE47_SCHEMA_SNAPSHOT.txt:7: status                | text                     |           | not null | 'queued'::text                    | extended |             |              | 
./docs/PHASE39_VALUE_ALIGNMENT.md:34:- **Read** queued tasks.
./docs/PHASE39_VALUE_ALIGNMENT.md:46:1) A Tier B (or C) queued task triggers `VALUE_GATE_REFUSE` in worker logs.
./docs/checkpoints/PHASE63_3_LATENCY_SAMPLING_SEMANTICS_FINDINGS_20260312.md:13:- `queued`
./docs/checkpoints/PHASE79_CHANGE_IMPACT_PREVIEW_COMPLETE.md:11:pending changes before they enter the protected history.
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:26:  - `queued`
./docs/checkpoints/PHASE63_1_CALCULATION_PATH_AUDIT_FINDINGS_20260312.md:66:  - `queued`
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:28:queued
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:36:Running Tasks = count(tasks where state = running OR queued)
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:44:task.queued
./docs/checkpoints/PHASE62B_ACTIVE_AGENTS_SOURCE_AUDIT_20260311.md:51:• pending
./docs/checkpoints/PHASE66A_QUEUE_DEPTH_REDUCER_DESIGN_20260316.md:23:task.queued
./docs/checkpoints/PHASE66A_QUEUE_DEPTH_REDUCER_DESIGN_20260316.md:45:task.queued
./docs/checkpoints/PHASE65_SHARED_METRICS_PATCH_VERIFIED_20260314.md:40:- multiple queued tasks
./docs/checkpoints/PHASE66_OBSERVABILITY_EXPANSION_PLAN_20260316.md:78:queued
./docs/checkpoints/PHASE66_OBSERVABILITY_EXPANSION_PLAN_20260316.md:96:task.queued
./docs/checkpoints/PHASE65_TASKS_RUNNING_EXECUTION_START_20260314.md:39:- add on running/started/queued transition
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:268:./PHASE25_FOLD_FUNCTION_SPEC.md:31:- NEW -> QUEUED (on "task.created" or "task.queued")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:573:./PHASE33_HANDOFF_ONE_LINER.txt:1:We are on feature/phase26-task-worker-min (pushed). Phase33 fixed docker workers claiming delegated tasks: phase32_claim_one.sql now claims WHERE status IN ('queued','delegated') ... FOR UPDATE SKIP LOCKED; docker-compose.worker.phase32.yml bind-mounts ./server/worker:/app/server/worker:ro for workerA+workerB; phase26_task_worker.mjs emits task_id as (row.task_id || String(row.id)) to keep stable string ids in task_events. Verified: sha256 parity host==workerA==workerB; 20 delegated → completed with clean task.created→task.running→task.completed, no duplicate terminal events, no numeric task_id drift. Commits in lane include 4bede779, 9543798b, ec70766e, plus 9c6c4251 adding PHASE33_SUPERVISOR_SUMMARY.md.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:599:./PHASE28_HANDOFF.md:27:- Expect OK; task ends queued with attempt=1 and future available_at; task_events includes task.running, task.failed, task.retry_scheduled.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:717:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:67:- public/js/phase22_task_delegation_live_bindings.js:32:      (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:747:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:110:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:760:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:133:- public/bundle.js:1309:      const status = ev?.status ?? ev?.payload?.status ?? t?.status ?? (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:777:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:176:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:778:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:215:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:786:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:225:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1052:./PHASE25_UI_INFERENCE_AUDIT.md:91:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1081:./PHASE25_UI_INFERENCE_AUDIT.md:143:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1082:./PHASE25_UI_INFERENCE_AUDIT.md:180:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1087:./PHASE25_UI_INFERENCE_AUDIT.md:186:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:1207:./server/task-events.mjs:153:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:2144:./PHASE27_SUPERVISOR_SUMMARY.txt:9:- End-to-end success path: queued -> running -> completed, with task.running + task.completed events written to task_events.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:2825:./public/scripts/agent-status-row.js:19:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:3571:./public/bundle.js:20:    `,document.head.appendChild(a)}function v(a,d,x){if(w(),!a){let t=document.getElementById("phase16-sse-tray");t||(t=c("div",{id:"phase16-sse-tray",style:["position:fixed","left:10px","bottom:10px","display:flex","flex-direction:column","gap:6px","z-index:9999","pointer-events:none"].join(";")}),document.body.appendChild(t)),a=t}let f=document.getElementById(d);if(f)return f;f=c("span",{id:d,class:"sse-indicator","data-connected":"false"}),f.append(c("span",{class:"dot","aria-hidden":"true"}),c("span",{class:"meta"},`${x}: disconnected \xB7 last: \u2014`));try{if(a.matches&&a.matches("header,h1,h2,h3,h4,strong")){let t=c("span",{style:"margin-left:8px"});t.appendChild(f),a.appendChild(t)}else{let t=c("div",{style:"margin-top:4px"});t.appendChild(f),a.appendChild(t)}}catch{a.appendChild(f)}return f}function k(a,d,x,f){if(!a)return;a.dataset.connected=x?"true":"false";let t=a.querySelector(".meta");t&&(t.textContent=`${d}: ${x?"connected":"disconnected"} \xB7 last: ${f?i(b()-f):"\u2014"}`)}function l(){return window.__MB_STREAMS||(window.__MB_STREAMS={ops:{connected:!1,lastAt:0,state:{},es:null},reflections:{connected:!1,lastAt:0,state:{},es:null}}),window.__MB_STREAMS}function I(a,d){return(!a||typeof a!="object")&&(a={}),!d||typeof d!="object"?a:Object.assign(a,d)}function y(a,d){(!a||typeof a!="object")&&(a={});let x=d&&typeof d.path=="string"?d.path:"";if(!x)return a;let f=x.split(".").filter(Boolean);if(!f.length)return a;let t=a;for(let n=0;n<f.length-1;n++){let o=f[n];(!t[o]||typeof t[o]!="object")&&(t[o]={}),t=t[o]}return t[f[f.length-1]]=d.value,a}function s(a,d){return!!(typeof a=="string"&&a.endsWith(".state")||d&&typeof d=="object"&&(d.state&&typeof d.state=="object"||typeof d.type=="string"&&d.type.includes("state")||typeof d.event=="string"&&d.event.includes("state")))}function r(a){return!a||typeof a!="object"?a:a.payload!==void 0?a.payload:a.data!==void 0?a.data:a.delta!==void 0?a.delta:a.patch!==void 0?a.patch:a.state!==void 0?a.state:a}function h(a,d,x,f){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return null;let t=l();try{t[a].es&&t[a].es.close()}catch{}t[a].es=null;let n=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(x);if(t[a].es=n,!n)return null;let o=()=>k(f,d,t[a].connected,t[a].lastAt);n.onopen=()=>{t[a].connected=!0,o()},n.onerror=()=>{t[a].connected=!1,o()};let g=(m,_)=>{t[a].lastAt=b();let C=S(_&&_.data?_.data:""),L=r(C);s(m,C)?t[a].state=L&&typeof L=="object"?L:{value:L}:L&&typeof L=="object"?typeof L.path=="string"&&"value"in L?t[a].state=y(t[a].state,L):t[a].state=I(t[a].state,L):L!=null&&(t[a].state=I(t[a].state,{lastValue:L})),o();try{window.dispatchEvent(new CustomEvent(`mb:${a}:update`,{detail:{event:m,state:t[a].state,raw:C}}))}catch{}};n.onmessage=m=>g("message",m);let e=["hello",`${a}.state`,`${a}.update`,`${a}.patch`,`${a}.delta`,"state","update","patch","delta"];for(let m of e)try{n.addEventListener(m,_=>g(m,_))}catch{}o()}function E(){return document.getElementById("ops-pill")||document.querySelector("[data-widget='ops-pill']")||document.querySelector(".ops-pill")||document.querySelector("#ops")||null}function T(){return document.getElementById("reflections-header")||document.getElementById("reflections")||document.querySelector("[data-panel='reflections']")||document.querySelector(".reflections")||Array.from(document.querySelectorAll("h1,h2,h3,h4,header,strong")).find(d=>(d.textContent||"").toLowerCase().includes("reflections"))||null}function A(){let a=v(E(),"ops-sse-indicator","OPS SSE"),d=v(T(),"reflections-sse-indicator","Reflections SSE");h("ops","OPS SSE",p,a),h("reflections","Reflections SSE",u,d),setInterval(()=>{let x=l();k(a,"OPS SSE",x.ops.connected,x.ops.lastAt),k(d,"Reflections SSE",x.reflections.connected,x.reflections.lastAt)},1e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",A,{once:!0}):A()})();(()=>{let p=document.getElementById("agent-status-container");if(!p){console.warn("agent-status-row.js: #agent-status-container not found.");return}let u=p.querySelector("h2");p.innerHTML="",u&&p.appendChild(u);let b=["Matilda","Atlas","Cade","Effie"],S={matilda:"\u{1F5E3}\uFE0F",atlas:"\u{1F9ED}",cade:"\u{1F4BB}",effie:"\u{1F4CA}"},i={},c=Object.create(null),w=60*1e3,v=document.getElementById("metric-agents");function k(t,n){t&&(t.textContent=n)}function l(t){if(typeof t=="number"&&Number.isFinite(t))return t;if(typeof t=="string"&&t.trim()){let n=Number(t);if(Number.isFinite(n))return n;let o=Date.parse(t);if(Number.isFinite(o))return o}return null}function I(){if(!v)return;let t=Date.now(),n=0;for(let o of b){let g=o.toLowerCase(),e=l(c[g]);e!=null&&t-e<=w&&(n+=1)}k(v,String(n))}k(v,"\u2014");let y=document.createElement("div");y.className="w-full flex flex-col gap-0.5",p.appendChild(y),b.forEach(t=>{let n=t.toLowerCase(),o=document.createElement("div");o.className="w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm";let g=document.createElement("div");g.className="flex items-center gap-3 min-w-0 h-[18px]";let e=document.createElement("span");e.className="inline-flex items-center justify-center shrink-0",e.textContent=S[n]||"\u2022",e.style.width="18px",e.style.minWidth="18px",e.style.height="18px",e.style.minHeight="18px",e.style.fontSize="14px",e.style.lineHeight="1",e.style.background="transparent",e.style.borderRadius="0",e.style.boxShadow="none",e.style.marginRight="0";let m=document.createElement("span");m.className="text-[13px] font-semibold tracking-tight text-slate-100/95 truncate",m.textContent=t;let _=document.createElement("span");_.className="text-[12px] font-medium text-slate-200/90 truncate",_.textContent="unknown",g.append(e,m),o.append(g,_),y.appendChild(o),i[n]={row:o,emoji:e,label:m,status:_}});let s="/events/ops",r=(typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0;function h(t){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return"unknown";let n=(t||"").toLowerCase();return n?n.includes("error")||n.includes("failed")||n.includes("offline")?"error":n.includes("online")||n.includes("ready")||n.includes("ok")?"online":n.includes("queued")||n.includes("pending")||n.includes("init")||n.includes("running")?"pending":"unknown":"unknown"}function E(t,n){let o=i[t];if(!o)return;let g=h(n),e=n||"unknown";switch(o.status.textContent=e,o.row.className="w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm",o.emoji.className="inline-flex items-center justify-center shrink-0",o.emoji.textContent=S[t]||"\u2022",o.emoji.style.display="inline-flex",o.emoji.style.width="18px",o.emoji.style.minWidth="18px",o.emoji.style.height="18px",o.emoji.style.minHeight="18px",o.emoji.style.fontSize="14px",o.emoji.style.lineHeight="1",o.emoji.style.background="transparent",o.emoji.style.borderRadius="0",o.emoji.style.boxShadow="none",o.emoji.style.marginRight="0",o.label.className="text-[13px] font-semibold tracking-tight truncate",o.status.className="text-[11px] font-medium truncate",g){case"online":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-emerald-300/90");break;case"error":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-rose-300/90");break;case"pending":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-amber-200/90");break;default:o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-slate-300/75");break}}if(Object.keys(i).forEach(t=>E(t,"unknown")),r){console.warn("[agent-status-row] Optional SSE disabled:",s);return}function T(t){try{return JSON.parse(t)}catch{return null}}function A(t){return!t||typeof t!="object"?null:t.payload&&typeof t.payload=="object"?t.payload:t.data&&typeof t.data=="object"?t.data:t.state&&typeof t.state=="object"?t.state:t}function a(t){if(!t||typeof t!="object")return!1;let n=t.agents;if(!n||typeof n!="object")return!1;let o=!1;for(let[g,e]of Object.entries(n)){let m=String(g||"").toLowerCase();if(!i[m])continue;let _="unknown";if(typeof e=="string")_=e;else if(e&&typeof e=="object"){_=e.status??e.state??e.level??e.health??e.mode??"unknown";let C=e.at??e.ts??e.last_activity??e.lastActivity??e.last_seen??e.lastSeen??null;C!=null&&(c[m]=C)}E(m,String(_||"unknown")),o=!0}return I(),o}function d(t){if(!t||typeof t!="object")return!1;let n=(t.agent||t.actor||t.source||t.worker||t.name||"").toString().toLowerCase();if(!n||!i[n])return!1;let o=t.status??t.state??t.level??t.health??t.mode??"unknown",g=t.at??t.ts??t.last_activity??t.lastActivity??t.last_seen??t.lastSeen??null;return g!=null&&(c[n]=g),E(n,String(o||"unknown")),I(),!0}function x(t,n){let o=T(n.data);if(!o)return;let g=A(o);t==="ops.state"&&a(g)||a(o)||a(g)||(d(g),d(o))}let f;try{f=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(s)}catch(t){console.error("agent-status-row.js: Failed to open OPS SSE connection:",t);return}f&&(f.onmessage=t=>x("message",t),f.addEventListener("hello",t=>x("hello",t)),f.addEventListener("ops.state",t=>x("ops.state",t)),f.addEventListener("state",t=>x("state",t)),f.addEventListener("update",t=>x("update",t)),f.onerror=t=>{console.warn("agent-status-row.js: OPS SSE error:",t),Object.keys(i).forEach(n=>E(n,"unknown")),k(v,"\u2014")})})();var D="__broadcastVisualizationInited",j=["Matilda","Cade","Effie"];function R(){let p=document.getElementById("broadcast-visual");if(!p)return;let u=[];for(let b=0;b<j.length;b++){let S=j[b];u.push('<div class="node" id="node-'+S+'">'+S+"</div>"),b<j.length-1&&u.push('<div class="arrow">\u279C</div>')}p.innerHTML=u.join("")}function $(){let p=0;setInterval(()=>{document.querySelectorAll(".node").forEach(i=>i.classList.remove("active"));let b="node-"+j[p],S=document.getElementById(b);S&&S.classList.add("active"),p=(p+1)%j.length},1500)}function H(){if(typeof window>"u"||typeof document>"u"||window[D])return;window[D]=!0;let p=()=>{R(),$()};document.readyState==="loading"?window.addEventListener("DOMContentLoaded",p):p()}typeof window<"u"&&(window.initBroadcastVisualization=H);(function(){if(!(typeof document>"u")){var p=document.getElementById("ops-dashboard-pill");if(!p){var u=document.querySelector("[data-ops-pill]");u&&(u.id="ops-dashboard-pill")}}})();(()=>{if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED||typeof window>"u"||typeof EventSource>"u"||window.__opsGlobalsBridgeInitialized)return;window.__opsGlobalsBridgeInitialized=!0,typeof window.lastOpsHeartbeat>"u"&&(window.lastOpsHeartbeat=null),typeof window.lastOpsStatusSnapshot>"u"&&(window.lastOpsStatusSnapshot=null);let p="/events/ops";if((typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0){console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):",p);return}let b=S=>{try{let i=JSON.parse(S.data||"null");if(!i)return;window.lastOpsHeartbeat=Math.floor(Date.now()/1e3),window.lastOpsStatusSnapshot=i}catch(i){console.warn("[ops-globals-bridge] Failed to parse OPS event:",i)}try{window.dispatchEvent(new CustomEvent("mb:ops:update",{detail:{event:"message",state:window.lastOpsStatusSnapshot}}))}catch{}};try{let S=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(p);if(!S)return null;if(S.onmessage=b,S.addEventListener("hello",b),!S)return;S.onerror=i=>{console.warn("[ops-globals-bridge] EventSource error:",i)}}catch(S){console.warn("[ops-globals-bridge] Failed to init EventSource:",S)}})();(function(){if(typeof window>"u"||typeof document>"u"||window.location.pathname!=="/dashboard")return;var p=5e3,u="ops-dashboard-pill";function b(){var i=document.getElementById(u);return i||(i=document.createElement("span"),i.id=u,i.className="ops-pill ops-pill-unknown",i.textContent="OPS: Unknown",i.style.display="inline-block",document.body.firstChild?document.body.insertBefore(i,document.body.firstChild):document.body.appendChild(i),i)}function S(){var i=document.getElementById("ops-status-pill");i&&(i.style.display="none");var c=b();if(c){var w=typeof window.lastOpsHeartbeat=="number",v=w?"OPS: Online":"OPS: Unknown",k=w?"ops-pill-online":"ops-pill-unknown";c.classList.remove("ops-pill-unknown","ops-pill-online","ops-pill-stale","ops-pill-error"),c.classList.add(k),c.textContent=v}}S(),setInterval(S,p)})();(function(){function p(i){console.log("[matilda-chat]",i)}function u(i,c,w){if(i){var v=document.createElement("p");v.className="mb-1 text-sm";var k=c?c+": ":"";v.textContent=k+w,i.appendChild(v),i.scrollTop=i.scrollHeight}}function b(i,c,w){i&&(i.disabled=w,i.classList.toggle("opacity-60",w),i.textContent=w?"Sending...":"Send"),c&&(c.disabled=w)}async function S(){var i=document.getElementById("matilda-chat-root");if(!i){p("No #matilda-chat-root found; skipping wiring.");return}var c=document.getElementById("matilda-chat-transcript"),w=document.getElementById("matilda-chat-input"),v=document.getElementById("matilda-chat-send");if(window.appendMessage=window.appendMessage||function(y){try{var s=y&&y.role?String(y.role):"system",r=y&&(y.content??y.text??y.message)?String(y.content??y.text??y.message):"",h=s==="user"?"You":s==="matilda"?"Matilda":"System";u(c,h,r)}catch{}},window.__appendMessage=window.__appendMessage||window.appendMessage,!c||!w||!v){p("Missing one or more Matilda chat elements; aborting wiring.");return}function k(y){return(y||"").toString().trim()}async function l(){var y=k(w.value);if(y){u(c,"You",y),w.value="",b(v,w,!0);try{var s=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:y,agent:"matilda"})});if(!s.ok){u(c,"Matilda","(error talking to /api/chat)");return}var r=await s.json(),h=r&&(r.reply||r.message||r.response)||"(no reply)";u(c,"Matilda",h)}catch(E){console.error(E),u(c,"Matilda","(network error)")}finally{b(v,w,!1)}}}v.addEventListener("click",l);var I=document.getElementById("matilda-chat-quick-check");I&&I.addEventListener("click",function(){w.value="Quick systems check from dashboard Phase 11.4.",l()}),w.addEventListener("keydown",function(y){y.key==="Enter"&&!y.shiftKey&&(y.preventDefault(),l())}),p("Matilda chat wiring complete.")}document.addEventListener("DOMContentLoaded",S)})();(function(){console.log("[dashboard-delegation] module loaded");function p(s){return document.getElementById(s)}function u(){var s=window.fetch,r=typeof s;return console.log("[dashboard-delegation] detected fetch type:",r),r!=="function"?(console.error("[dashboard-delegation] fetch is not a function; value:",s),null):s.bind(window)}function b(s){return String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function S(s){return'<pre class="mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300">'+b(JSON.stringify(s,null,2))+"</pre>"}function i(s,r){var h=p("delegation-response"),E=p("delegation-status-panel");h&&(h.innerHTML=r,E&&(E.classList.remove("border-gray-700","border-teal-600","border-green-600","border-red-600","border-amber-500"),s==="sending"?E.classList.add("border-teal-600"):s==="success"?E.classList.add("border-green-600"):s==="error"?E.classList.add("border-red-600"):s==="waiting"?E.classList.add("border-amber-500"):E.classList.add("border-gray-700")))}function c(){i("idle","Awaiting operator input.<br>Results from delegation requests will appear here.")}function w(s){i("sending",'<div class="text-teal-300 font-medium">Sending delegation\u2026</div><div class="mt-2 text-gray-400">Preparing request for the orchestration layer.</div>'+(s?'<div class="mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words">'+b(s)+"</div>":""))}function v(){i("waiting",'<div class="text-amber-300 font-medium">Still waiting on delegation response\u2026</div><div class="mt-2 text-gray-400">The request may still be processing.</div>')}function k(s){var r="Delegation accepted.";s&&typeof s=="object"&&(r=s.message||s.status||s.result||s.reply||s.ok&&"Delegation accepted."||r),i("success",'<div class="text-green-300 font-medium">'+b(r)+'</div><div class="mt-2 text-gray-400">Request completed successfully.</div>'+(s&&typeof s=="object"?S(s):""))}function l(s,r){i("error",'<div class="text-red-300 font-medium">Delegation failed.</div><div class="mt-2 text-gray-300">'+b(s||"Unknown error")+"</div>"+(r?'<div class="mt-3 text-xs text-gray-400 break-words">'+b(r)+"</div>":""))}async function I(){var s=p("delegation-input"),r=p("delegation-submit");if(!s){console.warn("[dashboard-delegation] delegation input not found at click time"),l("Delegation input field was not found.");return}var h=String(s.value||"").trim();if(!h){console.warn("[dashboard-delegation] empty delegation input; skipping"),l("Please enter a delegation request before submitting.");return}console.log("[dashboard-delegation] sending delegation:",h);var E=u();if(!E){console.error("[dashboard-delegation] aborting delegation because fetch is unavailable or invalid"),l("Browser fetch is unavailable.");return}var T=r&&r.textContent||"Submit Delegation",A=null;try{r&&(r.disabled=!0,r.textContent="Sending...",r.classList.add("opacity-70","cursor-not-allowed")),w(h),A=window.setTimeout(v,4e3);var a;try{a=await E("/api/delegate-task",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({prompt:h,message:h,text:h,task:h})})}catch(f){throw console.error("[dashboard-delegation] fetch threw before response:",f),f}console.log("[dashboard-delegation] fetch returned:",{ok:a&&a.ok,status:a&&a.status,statusText:a&&a.statusText});var d=null,x="";try{x=await a.text(),d=x?JSON.parse(x):{}}catch(f){console.error("[dashboard-delegation] error parsing JSON response:",f),d={error:"Non-JSON response from /api/delegate-task",raw:x||""}}if(console.log("[dashboard-delegation] delegation response:",d),!a.ok){l(d&&(d.error||d.message||d.statusText)||"HTTP "+a.status+" "+(a.statusText||""),x);return}k(d)}catch(f){l(f&&f.message?f.message:String(f))}finally{A&&window.clearTimeout(A),r&&(r.disabled=!1,r.textContent=T,r.classList.remove("opacity-70","cursor-not-allowed"))}}function y(){var s=p("delegation-submit"),r=p("delegation-input");if(!s||!r){console.warn("[dashboard-delegation] delegation button or input not found in init");return}if(c(),s.dataset.delegationWired==="true"){console.log("[dashboard-delegation] Task Delegation wiring already active");return}s.dataset.delegationWired="true",s.addEventListener("click",I),r.addEventListener("keydown",function(h){(h.metaKey||h.ctrlKey)&&h.key==="Enter"&&(h.preventDefault(),I())}),console.log("[dashboard-delegation] Task Delegation wiring active")}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();(()=>{let p="/events/task-events",u="mb-task-events-panel",b="mb-task-events-feed",S="mb-task-events-counts",i="mb-task-events-panel-anchor";function c(){let n=document.getElementById(i);return n||document.body}function w(){if(document.getElementById(u))return;let n=c(),o=document.createElement("div");o.id=u;let g=n&&n.id===i;o.style.width=g?"100%":"360px",o.style.maxWidth=g?"100%":"calc(100vw - 24px)",o.style.maxHeight=g?"260px":"40vh",o.style.overflow="hidden",o.style.zIndex="9999",o.style.border="1px solid rgba(255,255,255,0.12)",o.style.borderRadius="14px",o.style.background="rgba(10,10,14,0.92)",o.style.backdropFilter="blur(10px)",o.style.boxShadow="0 10px 30px rgba(0,0,0,0.35)",o.style.color="rgba(255,255,255,0.92)",o.style.fontFamily="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial",g?o.style.marginTop="12px":(o.style.position="fixed",o.style.right="12px",o.style.bottom="12px");let e=document.createElement("div");e.style.display="flex",e.style.alignItems="center",e.style.justifyContent="space-between",e.style.gap="8px",e.style.padding="10px 12px",e.style.borderBottom="1px solid rgba(255,255,255,0.10)";let m=document.createElement("div");m.textContent="Task Events",m.style.fontSize="12px",m.style.letterSpacing="0.06em",m.style.opacity="0.9";let _=document.createElement("div");_.style.display="flex",_.style.alignItems="center",_.style.gap="10px";let C=document.createElement("div");C.id=S,C.textContent="created:0  completed:0  failed:0",C.style.fontSize="11px",C.style.opacity="0.85",C.style.fontVariantNumeric="tabular-nums";let L=document.createElement("span");L.setAttribute("aria-label","task-events connection"),L.title="task-events connection",L.style.display="inline-block",L.style.width="10px",L.style.height="10px",L.style.borderRadius="999px",L.style.background="rgba(255,255,255,0.25)",L.style.boxShadow="0 0 0 2px rgba(255,255,255,0.08) inset";let O=document.createElement("button");O.type="button",O.textContent="\xD7",O.title="hide",O.style.cursor="pointer",O.style.border="1px solid rgba(255,255,255,0.14)",O.style.background="transparent",O.style.color="rgba(255,255,255,0.85)",O.style.borderRadius="10px",O.style.width="28px",O.style.height="24px",O.style.lineHeight="22px",O.style.fontSize="14px",O.onclick=()=>o.remove(),_.appendChild(L),_.appendChild(C),_.appendChild(O),e.appendChild(m),e.appendChild(_);let N=document.createElement("div");N.id=b,N.style.padding="10px 12px",N.style.overflow="auto",N.style.maxHeight=g?"200px":"calc(40vh - 46px)",o.appendChild(e),o.appendChild(N),n.appendChild(o),window.__MB_TASK_EVENTS_PANEL={dot:L,feed:N,counts:C},console.log("[phase22] task-events panel mounted (anchored=%s)",g)}function v(n){w();let o=window.__MB_TASK_EVENTS_PANEL?.dot;o&&(n==="open"?o.style.background="rgba(80,200,120,0.85)":n==="error"?o.style.background="rgba(240,90,90,0.85)":o.style.background="rgba(255,255,255,0.25)")}let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\.created|task\.started|task\.running|task\.queued|task\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\.completed|task\.failed|task\.cancelled|task\.canceled|task\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}function y(n){if(typeof n=="number")return new Date(n).toISOString();if(typeof n=="string"&&n.trim()){let o=new Date(n);return Number.isNaN(o.getTime())?n.trim():o.toISOString()}return new Date().toISOString()}function s(n,o,g){let e=`${n||""} ${o||""} ${g||""}`.toLowerCase();return/fail|error|cancel|timeout/.test(e)?"terminal-error":/complete|done|success/.test(e)?"terminal-success":/queue|pending|retry|wait|hold|sleep/.test(e)?"waiting":/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(e)?"active":"neutral"}function r(n){return n==="terminal-error"?"rgba(240,90,90,0.95)":n==="terminal-success"?"rgba(80,200,120,0.95)":n==="waiting"?"rgba(250,204,21,0.95)":n==="active"?"rgba(96,165,250,0.95)":"rgba(255,255,255,0.18)"}function h(n,o){let g=y(n.ts),e=String(n.kind??o??"event"),m=n.task_id??n.taskId??"unknown",_=n.run_id??n.runId??"",C=n.actor??n.meta?.actor??n.meta?.owner??"",L=n.status??n.meta?.status??"",O=typeof n.cursor=="number"||typeof n.cursor=="string"?String(n.cursor):"",N=String(n.msg??n.message??"").trim(),B=[];B.push(`task=${m}`),_&&B.push(`run=${_}`),C&&B.push(`actor=${C}`),L&&B.push(`status=${L}`),O&&B.push(`cursor=${O}`),N&&B.push(N);let M=B.join(" \u2022 "),P=s(e,N,L);return{ts:g,kind:e,detail:M,tone:P}}function E(n,o){w();let g=document.getElementById(b);if(!g)return;let e=h(n,o),m=document.createElement("div");m.className=`phase61-task-event phase61-task-event-${e.tone}`,m.dataset.eventKind=e.kind,m.dataset.eventTone=e.tone,m.style.position="relative",m.style.display="grid",m.style.gridTemplateColumns="minmax(112px, 132px) minmax(150px, 170px) 1fr",m.style.gap="10px",m.style.alignItems="start",m.style.padding="10px 12px 10px 14px",m.style.marginBottom="8px",m.style.border="1px solid rgba(255,255,255,0.10)",m.style.borderRadius="12px",m.style.background="rgba(255,255,255,0.03)",m.style.lineHeight="1.35",m.style.fontSize="12px";let _=document.createElement("span");_.setAttribute("aria-hidden","true"),_.style.position="absolute",_.style.left="0",_.style.top="10px",_.style.bottom="10px",_.style.width="3px",_.style.borderRadius="999px",_.style.background=r(e.tone);let C=document.createElement("div");C.textContent=e.ts,C.style.color="rgba(255,255,255,0.68)",C.style.fontVariantNumeric="tabular-nums",C.style.whiteSpace="nowrap";let L=document.createElement("div");L.textContent=e.kind,L.style.color="rgba(255,255,255,0.92)",L.style.fontWeight="600",L.style.letterSpacing="0.01em",L.style.wordBreak="break-word";let O=document.createElement("div");O.textContent=e.detail||"\u2014",O.style.color="rgba(255,255,255,0.78)",O.style.wordBreak="break-word",m.appendChild(_),m.appendChild(C),m.appendChild(L),m.appendChild(O),g.prepend(m);let N=Array.from(g.children);if(N.length>60)for(let B=60;B<N.length;B++)N[B].remove()}function T(n){try{window.dispatchEvent(new CustomEvent("mb.task.event",{detail:n}))}catch{}}function A(n){try{return JSON.parse(n)}catch{return null}}function a(n,o){let g=typeof o=="string"?A(o):o,e=g&&typeof g=="object"?g:{kind:n,raw:o};n==="task.event"&&(e.actor==null&&e.meta&&typeof e.meta=="object"&&(e.actor=e.meta.actor??e.meta.owner??null),!e.kind&&e.type&&(e.kind=e.type),e.kind==="task.event"&&e.type&&(e.kind=e.type),e.task_id==null&&e.taskId!=null&&(e.task_id=e.taskId),e.run_id==null&&e.runId!=null&&(e.run_id=e.runId),e&&e.meta&&typeof e.meta=="object"&&(e.task_id==null&&e.meta.task_id!=null&&(e.task_id=e.meta.task_id),e.run_id==null&&e.meta.run_id!=null&&(e.run_id=e.meta.run_id),e.actor==null&&e.meta.actor!=null&&(e.actor=e.meta.actor),e.actor==null&&e.meta.owner!=null&&(e.actor=e.meta.owner),e.status==null&&e.meta.status!=null&&(e.status=e.meta.status))),e.kind||(e.kind=n);let m=`${n}|${e.kind}|${e.ts??""}|${e.task_id??""}|${e.run_id??""}|${e.cursor??""}`;if(!k.has(m)){if(k.add(m),(e.kind==="task.created"||e.kind==="task.completed"||e.kind==="task.failed")&&I(String(e.kind)),G(e,n),E(e,n),window.__UI_DEBUG)try{console.log("[task-events] mb.task.event",e)}catch{}T(e)}}let d=null,x=0;function f(){if(w(),d){try{d.close()}catch{}d=null}let n=p;d=new EventSource(n),d.onopen=()=>{x=0,v("open"),E({ts:Date.now(),kind:"sse.open",message:`url=${n}`},"sse.open"),console.log("[phase22] task-events SSE open",n)},d.onerror=()=>{v("error");try{d.close()}catch{}d=null,x+=1;let g=Math.min(15e3,500*Math.pow(2,Math.min(6,x)));E({ts:Date.now(),kind:"sse.error",message:`reconnect_in=${g}ms`},"sse.error"),console.log("[phase22] task-events SSE error; reconnect in",g),setTimeout(f,g)},d.onmessage=g=>a("message",g.data);let o=["hello","heartbeat","task.event","task.created","task.completed","task.failed","task.updated","task.status"];for(let g of o)d.addEventListener(g,e=>a(g,e.data))}function t(){f(),setInterval(()=>{if(!document.getElementById(u))try{f()}catch{}},2e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",t,{once:!0}):t(),window.__MB_TASK_EVENTS={url:p,reconnect:()=>f()}})();(()=>{let p="mb.task.event",u=new Map,b={queued:"task-status-queued",done:"task-status-done",failed:"task-status-failed"};function S(s){let r=String(s??"").toLowerCase();return r==="queued"||r==="pending"?"queued":r==="done"||r==="complete"||r==="completed"?"done":r==="failed"||r==="error"?"failed":r||"unknown"}function i(s){return s?.task_id??s?.taskId??s?.task?.id??null}function c(s){let r=s?.task&&typeof s.task=="object"?s.task:null,h=i(s),E=s?.status??s?.payload?.status??r?.status??(s?.kind==="task.created"?"queued":null);return{id:h!=null?String(h):null,status:E!=null?S(E):null,title:r?.title??s?.title??null,agent:r?.agent??s?.agent??null,error:s?.error??s?.payload?.error??r?.error??null,updated_at:r?.updated_at??s?.ts??Date.now()}}function w(s,r){if(!s)return;let h=S(r);s.setAttribute("data-task-status",h),s.classList?.remove(...Object.values(b)),b[h]&&s.classList?.add(b[h]);let E=s.querySelector?.("[data-task-field='status']")||s.querySelector?.(".task-status")||s.querySelector?.(".status")||null;E&&(E.textContent=h)}function v(s){if(!s?.id)return;let r=String(s.id),h=[document.getElementById(`task-${r}`),document.getElementById(`task_${r}`),document.querySelector?.(`[data-task-id="${CSS.escape(r)}"]`),document.querySelector?.(`[data-taskid="${CSS.escape(r)}"]`)].filter(Boolean);for(let E of h)w(E,s.status)}function k(){let s=0,r=0,h=0;for(let T of u.values()){let A=S(T.status);A==="queued"?s++:A==="done"?r++:A==="failed"&&h++}let E=[["queued",s],["done",r],["failed",h]];for(let[T,A]of E){let a=document.getElementById(`task-count-${T}`)||document.getElementById(`tasks-${T}-count`)||document.querySelector?.(`[data-task-count="${T}"]`)||null;a&&(a.textContent=String(A))}}function l(s){if(!s?.id)return;let r=String(s.id),h=u.get(r)||{},E={...h,...s,id:r,status:s.status??h.status};u.set(r,E),v(E),k()}function I(s){let r=c(s);!r.id&&s?.kind&&(s.kind==="task.completed"&&(r.status="done"),s.kind==="task.failed"&&(r.status="failed")),r.id&&l(r)}function y(){window.__PHASE22_TASK_UI_BOUND||(window.__PHASE22_TASK_UI_BOUND=!0,window.addEventListener(p,s=>{try{(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&console.log("[phase22] mb.task.event",s.detail),I(s.detail)}catch{}}),window.__PHASE22_TASK_UI={tasks:u},console.log("[phase22] bindings attached"))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();typeof window<"u"&&typeof window.__DISABLE_OPTIONAL_SSE>"u"&&(window.__DISABLE_OPTIONAL_SSE=!1);})();
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:3695:./docs/checkpoints/PHASE61_5_5N_LOCATE_OPS_STATE_EMITTER_20260310_172424.txt:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:3863:./public/js/phase22_task_delegation_live_bindings.js:32:      (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4040:./scripts/_local/phase65_5_post_patch_verify.sh:32:  grep -nE 'base_url=|dashboard_up=|container_http_up=|metric-tasks|metric-success|metric-success-rate|metric-latency|policy.probe.task|task_status|status":"running"|status":"queued"|Recv failure|Connection reset|error|failed' \
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4267:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:136:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4305:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:219:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4324:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:331:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4377:./public/js/ops-globals-bridge.js:26:    console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):", opsUrl);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4423:./public/js/telemetry_queue_depth_reducer.js:11:    return state === "task.created" || state === "task.queued";
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4536:./scripts/phase35_fix_appendTaskEvent_pool_signature.sh:113:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4651:./public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance with role emojis.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Atlas\", \"Cade\", \"Effie\"];\n\n  const AGENT_EMOJI = {\n    matilda: \"\uD83D\uDDE3\uFE0F\",\n    atlas: \"\uD83E\uDDED\",\n    cade: \"\uD83D\uDCBB\",\n    effie: \"\uD83D\uDCCA\",\n  };\n\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const key = name.toLowerCase();\n\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const emoji = document.createElement(\"span\");\n    emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    emoji.textContent = AGENT_EMOJI[key] || \"\u2022\";\n    emoji.style.width = \"18px\";\n    emoji.style.minWidth = \"18px\";\n    emoji.style.height = \"18px\";\n    emoji.style.minHeight = \"18px\";\n    emoji.style.fontSize = \"14px\";\n    emoji.style.lineHeight = \"1\";\n    emoji.style.background = \"transparent\";\n    emoji.style.borderRadius = \"0\";\n    emoji.style.boxShadow = \"none\";\n    emoji.style.marginRight = \"0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(emoji, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[key] = { row, emoji, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    indicator.emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    indicator.emoji.textContent = AGENT_EMOJI[agentKey] || \"\u2022\";\n    indicator.emoji.style.display = \"inline-flex\";\n    indicator.emoji.style.width = \"18px\";\n    indicator.emoji.style.minWidth = \"18px\";\n    indicator.emoji.style.height = \"18px\";\n    indicator.emoji.style.minHeight = \"18px\";\n    indicator.emoji.style.fontSize = \"14px\";\n    indicator.emoji.style.lineHeight = \"1\";\n    indicator.emoji.style.background = \"transparent\";\n    indicator.emoji.style.borderRadius = \"0\";\n    indicator.emoji.style.boxShadow = \"none\";\n    indicator.emoji.style.marginRight = \"0\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  function parseJson(raw) {\n    try {\n      return JSON.parse(raw);\n    } catch {\n      return null;\n    }\n  }\n\n  function extractPayload(data) {\n    if (!data || typeof data !== \"object\") return null;\n    if (data.payload && typeof data.payload === \"object\") return data.payload;\n    if (data.data && typeof data.data === \"object\") return data.data;\n    if (data.state && typeof data.state === \"object\") return data.state;\n    return data;\n  }\n\n  function applyAgentMap(payload) {\n    if (!payload || typeof payload !== \"object\") return false;\n    const agents = payload.agents;\n    if (!agents || typeof agents !== \"object\") return false;\n\n    let applied = false;\n    for (const [name, value] of Object.entries(agents)) {\n      const key = String(name || \"\").toLowerCase();\n      if (!indicators[key]) continue;\n\n      let status = \"unknown\";\n      if (typeof value === \"string\") {\n        status = value;\n      } else if (value && typeof value === \"object\") {\n        status =\n          value.status ??\n          value.state ??\n          value.level ??\n          value.health ??\n          value.mode ??\n          \"unknown\";\n      }\n\n      applyVisual(key, String(status || \"unknown\"));\n      applied = true;\n    }\n    return applied;\n  }\n\n  function applySingleAgent(data) {\n    if (!data || typeof data !== \"object\") return false;\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || data.name || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return false;\n\n    const status =\n      data.status ??\n      data.state ??\n      data.level ??\n      data.health ??\n      data.mode ??\n      \"unknown\";\n\n    applyVisual(agentName, String(status || \"unknown\"));\n    return true;\n  }\n\n  function handleOpsEvent(eventName, event) {\n    const data = parseJson(event.data);\n    if (!data) return;\n\n    const payload = extractPayload(data);\n\n    if (eventName === \"ops.state\") {\n      if (applyAgentMap(payload)) return;\n    }\n\n    if (applyAgentMap(data)) return;\n    if (applyAgentMap(payload)) return;\n    applySingleAgent(payload);\n    applySingleAgent(data);\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => handleOpsEvent(\"message\", event);\n  source.addEventListener(\"hello\", (event) => handleOpsEvent(\"hello\", event));\n  source.addEventListener(\"ops.state\", (event) => handleOpsEvent(\"ops.state\", event));\n  source.addEventListener(\"state\", (event) => handleOpsEvent(\"state\", event));\n  source.addEventListener(\"update\", (event) => handleOpsEvent(\"update\", event));\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4665:./scripts/phase14_5_patch_dashboard_no_cache.mjs:104:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4925:./scripts/phase22_wire_task_events_ui.sh:293:      echo "Appending import for task-events-sse-client into: $f"
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5151:./PHASE26_HANDOFF_ONE_LINER.txt:1:Start from tag v25.0-task-events-single-writer-ok; on feature/phase26-task-worker-min we added server/worker/phase26_task_worker.mjs + scripts/phase26_run_worker.sh; worker claims tasks where tasks.status='queued' via FOR UPDATE SKIP LOCKED, sets running/completed(or failed), inserts task_events (kind + JSON payload-as-text) so /events/task-events streams task.created + task.running + task.completed/failed; smoke verified end-to-end with task t86 (events 261–263) and tasks.id=86 completed.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:5648:./scripts/_local/phase62b_hydrate_tasks_running.sh:15:new = 'let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\\.created|task\\.started|task\\.running|task\\.queued|task\\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\\.completed|task\\.failed|task\\.cancelled|task\\.canceled|task\\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}'
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:6489:./scripts/_local/phase65_1_tasks_running_inspect.sh:17:rg -n 'task\.|task-events|EventSource|SSE|recent history|recent tasks|running|queued|completed|failed|cancelled|started' \
./docs/checkpoints/PHASE61_5_5Z_PROVE_AGENT_STATUS_ROUTE_UPDATES_OPS_SNAPSHOT_20260310_190755.txt:10:{"ok":true,"agent":"Cade","state":"queued","at":1773194875292}
./docs/checkpoints/PHASE61_5_5Z_PROVE_AGENT_STATUS_ROUTE_UPDATES_OPS_SNAPSHOT_20260310_190755.txt:19:data: {"status":"live","lastHeartbeatAt":null,"agents":{"Matilda":{"state":"running","at":1773194875284,"meta":{"source":"phase61_5_5z_probe"}},"Cade":{"state":"queued","at":1773194875292,"meta":{"source":"phase61_5_5z_probe"}},"Effie":{"state":"ready","at":1773194875299,"meta":{"source":"phase61_5_5z_probe"}},"Atlas":{"state":"ready","at":1773194875306,"meta":{"source":"phase61_5_5z_probe"}}}}
./docs/checkpoints/PHASE61_5_5Z_PROVE_AGENT_STATUS_ROUTE_UPDATES_OPS_SNAPSHOT_20260310_190755.txt:31:5:data: {"status":"live","lastHeartbeatAt":null,"agents":{"Matilda":{"state":"running","at":1773194875284,"meta":{"source":"phase61_5_5z_probe"}},"Cade":{"state":"queued","at":1773194875292,"meta":{"source":"phase61_5_5z_probe"}},"Effie":{"state":"ready","at":1773194875299,"meta":{"source":"phase61_5_5z_probe"}},"Atlas":{"state":"ready","at":1773194875306,"meta":{"source":"phase61_5_5z_probe"}}}}
./docs/checkpoints/PHASE63_3_TASKS_RUNNING_SEMANTICS_FINDINGS_20260312.md:13:- `queued`
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:161:./docs/checkpoints/PHASE61_5_5N_LOCATE_OPS_STATE_EMITTER_20260310_172424.txt:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:338:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:136:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:377:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:219:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:397:./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:331:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3098:./PHASE25_FOLD_FUNCTION_SPEC.md:31:- NEW -> QUEUED (on "task.created" or "task.queued")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3112:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:110:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3125:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:133:- public/bundle.js:1309:      const status = ev?.status ?? ev?.payload?.status ?? t?.status ?? (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3144:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:176:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3149:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:215:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3158:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:225:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3193:./PHASE25_UI_INFERENCE_AUDIT_SCOPED.md:67:- public/js/phase22_task_delegation_live_bindings.js:32:      (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3250:./PHASE25_UI_INFERENCE_AUDIT.md:143:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3255:./PHASE25_UI_INFERENCE_AUDIT.md:180:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3260:./PHASE25_UI_INFERENCE_AUDIT.md:186:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3314:./PHASE25_UI_INFERENCE_AUDIT.md:91:- public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 21: Task Events SSE listener\n * - Parallel to ops/reflections (does not modify their ownership)\n * - Opens EventSource(\"/events/task-events\")\n * - Buffers events on window.__TASK_EVENTS_FEED and emits DOM event \"task-events:append\"\n * - Tiny debug panel is ALWAYS clickable (collapsed by default)\n *   - Auto-expands when window.__UI_DEBUG or window.__PHASE21_SHOW_TASK_EVENTS is true\n * - Exposes window.__TASK_EVENTS snapshot for quick inspection\n */\n(function () {\n  const URL = \"/events/task-events\";\n  const MAX_ITEMS = 200;\n\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.__TASK_EVENTS_SSE_INITED) return;\n  window.__TASK_EVENTS_SSE_INITED = true;\n\n  function nowIso() {\n    try { return new Date().toISOString(); } catch { return String(Date.now()); }\n  }\n\n  function ensureBuffer() {\n    if (!window.__TASK_EVENTS_FEED) window.__TASK_EVENTS_FEED = [];\n    return window.__TASK_EVENTS_FEED;\n  }\n\n  function ensureSnapshot() {\n    if (!window.__TASK_EVENTS) {\n      window.__TASK_EVENTS = {\n        url: URL,\n        connected: false,\n        lastAt: 0,\n        lastEvent: null,\n        cursor: null,\n        readyState: null,\n      };\n    }\n    return window.__TASK_EVENTS;\n  }\n\n  function pushItem(item) {\n    const buf = ensureBuffer();\n    buf.push(item);\n    if (buf.length > MAX_ITEMS) buf.splice(0, buf.length - MAX_ITEMS);\n\n    const snap = ensureSnapshot();\n    snap.lastAt = item.ts || Date.now();\n    snap.lastEvent = item.event || item.kind || null;\n    if (item?.data && typeof item.data === \"object\" && \"cursor\" in item.data) {\n      snap.cursor = item.data.cursor ?? snap.cursor;\n    }\n    try { snap.readyState = window.__taskEventsES?.readyState ?? null; } catch {}\n\n    try {\n      window.dispatchEvent(new CustomEvent(\"task-events:append\", { detail: item }));\n    } catch {}\n  }\n\n  function safeJson(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function ensureMiniPanel() {\n    if (document.getElementById(\"task-events-log\")) return;\n\n    const wrap = document.createElement(\"div\");\n    wrap.id = \"task-events-log\";\n    wrap.style.position = \"fixed\";\n    wrap.style.right = \"14px\";\n    wrap.style.bottom = \"14px\";\n    wrap.style.width = \"440px\";\n    wrap.style.maxHeight = \"240px\";\n    wrap.style.overflow = \"hidden\"; // body scrolls, header stays\n    wrap.style.padding = \"10px 12px\";\n    wrap.style.borderRadius = \"12px\";\n    wrap.style.fontFamily = \"ui-monospace, Menlo, Monaco, Consolas, monospace\";\n    wrap.style.fontSize = \"12px\";\n    wrap.style.lineHeight = \"1.35\";\n    wrap.style.zIndex = \"99999\";\n    wrap.style.background = \"rgba(10,10,14,0.72)\";\n    wrap.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    wrap.style.boxShadow = \"0 10px 24px rgba(0,0,0,0.35)\";\n    wrap.style.backdropFilter = \"blur(6px)\";\n    wrap.style.display = \"block\"; // ALWAYS clickable\n\n    const hdr = document.createElement(\"div\");\n    hdr.style.display = \"flex\";\n    hdr.style.alignItems = \"center\";\n    hdr.style.justifyContent = \"space-between\";\n    hdr.style.gap = \"10px\";\n    hdr.style.marginBottom = \"8px\";\n\n    const title = document.createElement(\"div\");\n    title.id = \"task-events-log-title\";\n    title.textContent = \"TASK EVENTS \u00B7 disconnected\";\n    title.style.letterSpacing = \"0.12em\";\n    title.style.fontWeight = \"700\";\n    title.style.opacity = \"0.9\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"expand\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.15)\";\n    btn.style.background = \"rgba(255,255,255,0.08)\";\n    btn.style.color = \"rgba(255,255,255,0.9)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.padding = \"4px 8px\";\n    btn.style.fontSize = \"12px\";\n\n    const body = document.createElement(\"div\");\n    body.id = \"task-events-log-body\";\n    body.style.maxHeight = \"190px\";\n    body.style.overflow = \"auto\";\n    body.style.display = \"none\"; // collapsed by default\n\n    btn.onclick = () => {\n      const on = body.style.display === \"none\";\n      body.style.display = on ? \"block\" : \"none\";\n      btn.textContent = on ? \"collapse\" : \"expand\";\n    };\n\n    hdr.appendChild(title);\n    hdr.appendChild(btn);\n\n    wrap.appendChild(hdr);\n    wrap.appendChild(body);\n    document.body.appendChild(wrap);\n\n    // Auto-expand when debugging\n    if (window.__UI_DEBUG || window.__PHASE21_SHOW_TASK_EVENTS) {\n      body.style.display = \"block\";\n      btn.textContent = \"collapse\";\n    }\n  }\n\n  function appendLine(text) {\n    const body = document.getElementById(\"task-events-log-body\");\n    if (!body) return;\n    const div = document.createElement(\"div\");\n    div.style.whiteSpace = \"pre-wrap\";\n    div.style.wordBreak = \"break-word\";\n    div.textContent = text;\n    body.appendChild(div);\n    while (body.childNodes.length > 140) body.removeChild(body.firstChild);\n    body.scrollTop = body.scrollHeight;\n  }\n\n  function setHeaderStatus(text) {\n    const t = document.getElementById(\"task-events-log-title\");\n    if (t) t.textContent = text;\n  }\n\n  function start() {\n    ensureMiniPanel();\n    ensureSnapshot();\n\n    const es = new EventSource(URL);\n    window.__taskEventsES = es;\n\n    es.onopen = () => {\n      const snap = ensureSnapshot();\n      snap.connected = true;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.open\", event: \"open\", url: URL };\n      pushItem(item);\n      setHeaderStatus(\"TASK EVENTS \u00B7 connected\");\n      appendLine(`[${item.iso}] open ${URL}`);\n    };\n\n    es.onerror = () => {\n      const snap = ensureSnapshot();\n      snap.connected = false;\n      snap.readyState = es.readyState;\n\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"sse.error\", event: \"error\", url: URL, readyState: es.readyState };\n      pushItem(item);\n      setHeaderStatus(`TASK EVENTS \u00B7 error (readyState=${es.readyState})`);\n      appendLine(`[${item.iso}] error readyState=${es.readyState}`);\n    };\n\n    // default \"message\"\n    es.onmessage = (ev) => {\n      const payload = safeJson(ev.data);\n      const item = { ts: Date.now(), iso: nowIso(), kind: \"message\", event: \"message\", data: payload ?? ev.data };\n      pushItem(item);\n      appendLine(`[${item.iso}] message :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n    };\n\n    // named events (include what server ACTUALLY emits: task.event)\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.lifecycle\",\n      \"task.created\",\n      \"task.updated\",\n      \"task.completed\",\n      \"task.failed\",\n      \"error\",\n    ];\n\n    for (const name of names) {\n      try {\n        es.addEventListener(name, (ev) => {\n          const payload = safeJson(ev.data);\n          const item = { ts: Date.now(), iso: nowIso(), kind: \"event\", event: name, data: payload ?? ev.data };\n          pushItem(item);\n          appendLine(`[${item.iso}] ${name} :: ${payload ? JSON.stringify(payload) : String(ev.data)}`);\n        });\n      } catch {}\n    }\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", start, { once: true });\n  } else {\n    start();\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// <0001fb02> Agent Status Row \u2013 live heartbeats from OPS SSE\n// - Renders status pills for Matilda, Cade, Effie, Atlas\n// - Listens to OPS SSE on port 3201\n// - Updates colors/text based on incoming status\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  // Clear placeholder text (e.g., \"Loading agents...\")\n  container.innerHTML = \"\";\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const row = document.createElement(\"div\");\n  row.className = \"flex flex-wrap gap-4 items-center\";\n  container.appendChild(row);\n\n  AGENTS.forEach((name) => {\n    const pill = document.createElement(\"div\");\n    pill.className =\n      \"px-3 py-1 rounded-full bg-gray-700 text-sm flex items-center gap-2 shadow\";\n\n    const dot = document.createElement(\"span\");\n    dot.className = \"w-2 h-2 rounded-full bg-yellow-400\";\n\n    const label = document.createElement(\"span\");\n    label.textContent = `${name}: \u23F3`;\n\n    pill.dataset.agent = name.toLowerCase();\n    pill.append(dot, label);\n    row.appendChild(pill);\n\n    indicators[name.toLowerCase()] = { pill, dot, label };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled (Phase 16 pending):\", OPS_SSE_URL);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n    return;\n  }\nlet source;\n\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  function classifyStatus(statusString) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) {\n      return \"error\";\n    }\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) {\n      return \"online\";\n    }\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\")) {\n      return \"pending\";\n    }\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const { pill, dot, label } = indicator;\n\n    // Reset base classes\n    dot.className = \"w-2 h-2 rounded-full\";\n    pill.classList.remove(\"border\", \"border-red-400\", \"border-green-400\", \"border-yellow-300\");\n\n    switch (kind) {\n      case \"online\":\n        dot.classList.add(\"bg-green-400\");\n        pill.classList.add(\"border\", \"border-green-400\");\n        break;\n      case \"error\":\n        dot.classList.add(\"bg-red-400\");\n        pill.classList.add(\"border\", \"border-red-400\");\n        break;\n      case \"pending\":\n        dot.classList.add(\"bg-yellow-300\");\n        pill.classList.add(\"border\", \"border-yellow-300\");\n        break;\n      case \"unknown\":\n      default:\n        dot.classList.add(\"bg-gray-500\");\n        break;\n    }\n\n    const prettyName = agentKey.charAt(0).toUpperCase() + agentKey.slice(1);\n    const finalStatus = statusString || \"unknown\";\n    label.textContent = `${prettyName}: ${finalStatus}`;\n  }\n\n  // Phase16: guard null EventSource before handlers\n  if (!source) return null;\n  source.onmessage = (event) => {\n    let payloadRaw = event.data;\n    let data;\n\n    try {\n      data = JSON.parse(payloadRaw);\n    } catch {\n      // If it's not JSON, ignore for agent-status purposes\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString();\n    if (!agentName) return;\n\n    const key = agentName.toLowerCase();\n    if (!indicators[key]) {\n      // Ignore agents we don't show in the row\n      return;\n    }\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(key, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    // On error, show all as unknown (neutral gray)\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "/**\n * Tasks Widget (stable, no SSE)\n * - GET /api/tasks\n * - POST /api/complete-task\n * - No optimistic removal (prevents list blinking)\n */\n(() => {\n  const API = {\n    list: \"/api/tasks\",\n    complete: \"/api/complete-task\",\n  };\n\n  const SELECTORS = [\n    \"#tasks-widget\",\n    \"#tasksWidget\",\n    \"[data-tasks-widget]\",\n    \"[data-widget='tasks']\",\n  ];\n\n  const state = {\n    tasks: [],\n    loading: false,\n    lastError: null,\n    inflightComplete: new Set(),\n  };\n\n  function $(sel, root = document) {\n    return root.querySelector(sel);\n  }\n\n  function findMount() {\n    for (const sel of SELECTORS) {\n      const el = $(sel);\n      if (el) return el;\n    }\n    return null;\n  }\n\n  function esc(s) {\n    return String(s ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#39;\");\n  }\n\n  async function apiJson(url, opts = {}) {\n    const res = await fetch(url, {\n      method: opts.method || \"GET\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: opts.body ? JSON.stringify(opts.body) : undefined,\n    });\n    const json = await res.json();\n    if (!res.ok) throw new Error(json?.error || \"Request failed\");\n    return json;\n  }\n\n  async function fetchTasks() {\n    state.loading = true;\n    render();\n    try {\n      const data = await apiJson(API.list);\n      state.tasks = (data.tasks || []).map(t => ({\n        id: String(t.id),\n        title: t.title || \"\",\n        status: t.status || \"\",\n      }));\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.loading = false;\n      render();\n    }\n  }\n\n  async function completeTask(taskId) {\n    if (state.inflightComplete.has(taskId)) return;\n    state.inflightComplete.add(taskId);\n    render();\n\n    try {\n      await apiJson(API.complete, {\n        method: \"POST\",\n        body: { taskId },\n      });\n    } catch (e) {\n      state.lastError = e.message;\n    } finally {\n      state.inflightComplete.delete(taskId);\n      await fetchTasks();\n    }\n  }\n\n  function render() {\n    const mount = findMount();\n    if (!mount) return;\n\n    mount.innerHTML = `\n      <div>\n        \n        ${state.lastError ? `<div style=\"color:red\">${esc(state.lastError)}</div>` : \"\"}\n        <div>\n          ${state.tasks.map(t => `\n            <div style=\"display:flex;justify-content:space-between;gap:8px\">\n              <span>${esc(t.title)}</span>\n              ${\n                ([\"complete\",\"completed\",\"done\"].includes(String(t.status||\"\").toLowerCase()))\n                  ? `<span style=\"opacity:.5;font-size:12px\">Completed</span>`\n                  : `<button data-id=\"${t.id}\">Complete</button>`\n              }\n            </div>\n          `).join(\"\")}\n        </div>\n      </div>\n    `;\n\n    mount.querySelectorAll(\"button[data-id]\").forEach(btn => {\n      btn.onclick = () => completeTask(btn.dataset.id);\n    });\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", fetchTasks);\n\n  // Phase22: SSE-driven refresh (ignore heartbeats)\n  window.addEventListener(\"mb.task.event\", (e) => {\n    const k = String(e?.detail?.kind || e?.detail?.type || \"\");\n    if (k === \"heartbeat\") return;\n    fetchTasks();\n  });\n\n// Phase22: SSE-driven refresh\n  window.addEventListener(\"mb.task.event\", () => { fetchTasks(); });\n\n// Auto-refresh (no SSE): keep widget feeling live\n  setInterval(() => { fetchTasks(); }, 5000);\n\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    // If anchored, behave like an in-page card; otherwise float.\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"TASK EVENTS (live)\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.08em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function formatLine(ev, fallbackKind) {\n    const ts = typeof ev.ts === \"number\" ? new Date(ev.ts).toISOString() : new Date().toISOString();\n    const tid = ev.task_id ?? ev.id ?? ev.taskId ?? \"unknown\";\n    const run = ev.run_id ?? ev.runId ?? \"\";\n    const msg = ev.msg ?? ev.message ?? \"\";\n    const extras = [];\n    if (run) extras.push(`run=${run}`);\n    if (ev.actor) extras.push(`actor=${ev.actor}`);\n    if (ev.status) extras.push(`status=${ev.status}`);\n    if (typeof ev.cursor === \"number\") extras.push(`cursor=${ev.cursor}`);\n    const extraStr = extras.length ? ` (${extras.join(\" \")})` : \"\";\n    return `${ts}  ${(ev.kind ?? fallbackKind ?? \"event\")}  task=${tid}${extraStr}${msg ? \" \u2014 \" + msg : \"\"}`;\n  }\n\n  function appendLine(text, kind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const row = document.createElement(\"div\");\n    row.style.whiteSpace = \"pre-wrap\";\n    row.style.wordBreak = \"break-word\";\n    row.style.fontSize = \"11px\";\n    row.style.lineHeight = \"1.35\";\n    row.style.padding = \"6px 8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.marginBottom = \"8px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n\n    if (kind === \"task.completed\") row.style.borderColor = \"rgba(80,200,120,0.35)\";\n    if (kind === \"task.failed\") row.style.borderColor = \"rgba(240,90,90,0.35)\";\n    if (kind === \"heartbeat\") row.style.opacity = \"0.65\";\n\n    row.textContent = text;\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n      // Phase22 normalization:\n      // server sends event: task.event with payload { type:\"task.created|task.completed|task.failed\", taskId:\"...\" }\n      if (eventName === \"task.event\") {\n        if (!ev.kind && ev.type) ev.kind = ev.type;\n        if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n        if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n        if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? ev.id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendLine(formatLine(ev, eventName), String(ev.kind ?? eventName));\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendLine(`${new Date().toISOString()}  sse.open  url=${url}`, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendLine(`${new Date().toISOString()}  sse.error  reconnect_in=${delay}ms`, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    // Guard: if something removes the panel, re-mount + reconnect.\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.id ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\nimport \"./task-events-sse-listener.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\nimport \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3319:./PHASE26_HANDOFF_ONE_LINER.txt:1:Start from tag v25.0-task-events-single-writer-ok; on feature/phase26-task-worker-min we added server/worker/phase26_task_worker.mjs + scripts/phase26_run_worker.sh; worker claims tasks where tasks.status='queued' via FOR UPDATE SKIP LOCKED, sets running/completed(or failed), inserts task_events (kind + JSON payload-as-text) so /events/task-events streams task.created + task.running + task.completed/failed; smoke verified end-to-end with task t86 (events 261–263) and tasks.id=86 completed.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3331:./PHASE27_SUPERVISOR_SUMMARY.txt:9:- End-to-end success path: queued -> running -> completed, with task.running + task.completed events written to task_events.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3333:./PHASE28_HANDOFF.md:27:- Expect OK; task ends queued with attempt=1 and future available_at; task_events includes task.running, task.failed, task.retry_scheduled.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3336:./PHASE33_HANDOFF_ONE_LINER.txt:1:We are on feature/phase26-task-worker-min (pushed). Phase33 fixed docker workers claiming delegated tasks: phase32_claim_one.sql now claims WHERE status IN ('queued','delegated') ... FOR UPDATE SKIP LOCKED; docker-compose.worker.phase32.yml bind-mounts ./server/worker:/app/server/worker:ro for workerA+workerB; phase26_task_worker.mjs emits task_id as (row.task_id || String(row.id)) to keep stable string ids in task_events. Verified: sha256 parity host==workerA==workerB; 20 delegated → completed with clean task.created→task.running→task.completed, no duplicate terminal events, no numeric task_id drift. Commits in lane include 4bede779, 9543798b, ec70766e, plus 9c6c4251 adding PHASE33_SUPERVISOR_SUMMARY.md.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3474:./public/bundle.js:20:    `,document.head.appendChild(a)}function v(a,d,x){if(w(),!a){let t=document.getElementById("phase16-sse-tray");t||(t=c("div",{id:"phase16-sse-tray",style:["position:fixed","left:10px","bottom:10px","display:flex","flex-direction:column","gap:6px","z-index:9999","pointer-events:none"].join(";")}),document.body.appendChild(t)),a=t}let f=document.getElementById(d);if(f)return f;f=c("span",{id:d,class:"sse-indicator","data-connected":"false"}),f.append(c("span",{class:"dot","aria-hidden":"true"}),c("span",{class:"meta"},`${x}: disconnected \xB7 last: \u2014`));try{if(a.matches&&a.matches("header,h1,h2,h3,h4,strong")){let t=c("span",{style:"margin-left:8px"});t.appendChild(f),a.appendChild(t)}else{let t=c("div",{style:"margin-top:4px"});t.appendChild(f),a.appendChild(t)}}catch{a.appendChild(f)}return f}function k(a,d,x,f){if(!a)return;a.dataset.connected=x?"true":"false";let t=a.querySelector(".meta");t&&(t.textContent=`${d}: ${x?"connected":"disconnected"} \xB7 last: ${f?i(b()-f):"\u2014"}`)}function l(){return window.__MB_STREAMS||(window.__MB_STREAMS={ops:{connected:!1,lastAt:0,state:{},es:null},reflections:{connected:!1,lastAt:0,state:{},es:null}}),window.__MB_STREAMS}function I(a,d){return(!a||typeof a!="object")&&(a={}),!d||typeof d!="object"?a:Object.assign(a,d)}function y(a,d){(!a||typeof a!="object")&&(a={});let x=d&&typeof d.path=="string"?d.path:"";if(!x)return a;let f=x.split(".").filter(Boolean);if(!f.length)return a;let t=a;for(let n=0;n<f.length-1;n++){let o=f[n];(!t[o]||typeof t[o]!="object")&&(t[o]={}),t=t[o]}return t[f[f.length-1]]=d.value,a}function s(a,d){return!!(typeof a=="string"&&a.endsWith(".state")||d&&typeof d=="object"&&(d.state&&typeof d.state=="object"||typeof d.type=="string"&&d.type.includes("state")||typeof d.event=="string"&&d.event.includes("state")))}function r(a){return!a||typeof a!="object"?a:a.payload!==void 0?a.payload:a.data!==void 0?a.data:a.delta!==void 0?a.delta:a.patch!==void 0?a.patch:a.state!==void 0?a.state:a}function h(a,d,x,f){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return null;let t=l();try{t[a].es&&t[a].es.close()}catch{}t[a].es=null;let n=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(x);if(t[a].es=n,!n)return null;let o=()=>k(f,d,t[a].connected,t[a].lastAt);n.onopen=()=>{t[a].connected=!0,o()},n.onerror=()=>{t[a].connected=!1,o()};let g=(m,_)=>{t[a].lastAt=b();let C=S(_&&_.data?_.data:""),L=r(C);s(m,C)?t[a].state=L&&typeof L=="object"?L:{value:L}:L&&typeof L=="object"?typeof L.path=="string"&&"value"in L?t[a].state=y(t[a].state,L):t[a].state=I(t[a].state,L):L!=null&&(t[a].state=I(t[a].state,{lastValue:L})),o();try{window.dispatchEvent(new CustomEvent(`mb:${a}:update`,{detail:{event:m,state:t[a].state,raw:C}}))}catch{}};n.onmessage=m=>g("message",m);let e=["hello",`${a}.state`,`${a}.update`,`${a}.patch`,`${a}.delta`,"state","update","patch","delta"];for(let m of e)try{n.addEventListener(m,_=>g(m,_))}catch{}o()}function E(){return document.getElementById("ops-pill")||document.querySelector("[data-widget='ops-pill']")||document.querySelector(".ops-pill")||document.querySelector("#ops")||null}function T(){return document.getElementById("reflections-header")||document.getElementById("reflections")||document.querySelector("[data-panel='reflections']")||document.querySelector(".reflections")||Array.from(document.querySelectorAll("h1,h2,h3,h4,header,strong")).find(d=>(d.textContent||"").toLowerCase().includes("reflections"))||null}function A(){let a=v(E(),"ops-sse-indicator","OPS SSE"),d=v(T(),"reflections-sse-indicator","Reflections SSE");h("ops","OPS SSE",p,a),h("reflections","Reflections SSE",u,d),setInterval(()=>{let x=l();k(a,"OPS SSE",x.ops.connected,x.ops.lastAt),k(d,"Reflections SSE",x.reflections.connected,x.reflections.lastAt)},1e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",A,{once:!0}):A()})();(()=>{let p=document.getElementById("agent-status-container");if(!p){console.warn("agent-status-row.js: #agent-status-container not found.");return}let u=p.querySelector("h2");p.innerHTML="",u&&p.appendChild(u);let b=["Matilda","Atlas","Cade","Effie"],S={matilda:"\u{1F5E3}\uFE0F",atlas:"\u{1F9ED}",cade:"\u{1F4BB}",effie:"\u{1F4CA}"},i={},c=Object.create(null),w=60*1e3,v=document.getElementById("metric-agents");function k(t,n){t&&(t.textContent=n)}function l(t){if(typeof t=="number"&&Number.isFinite(t))return t;if(typeof t=="string"&&t.trim()){let n=Number(t);if(Number.isFinite(n))return n;let o=Date.parse(t);if(Number.isFinite(o))return o}return null}function I(){if(!v)return;let t=Date.now(),n=0;for(let o of b){let g=o.toLowerCase(),e=l(c[g]);e!=null&&t-e<=w&&(n+=1)}k(v,String(n))}k(v,"\u2014");let y=document.createElement("div");y.className="w-full flex flex-col gap-0.5",p.appendChild(y),b.forEach(t=>{let n=t.toLowerCase(),o=document.createElement("div");o.className="w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm";let g=document.createElement("div");g.className="flex items-center gap-3 min-w-0 h-[18px]";let e=document.createElement("span");e.className="inline-flex items-center justify-center shrink-0",e.textContent=S[n]||"\u2022",e.style.width="18px",e.style.minWidth="18px",e.style.height="18px",e.style.minHeight="18px",e.style.fontSize="14px",e.style.lineHeight="1",e.style.background="transparent",e.style.borderRadius="0",e.style.boxShadow="none",e.style.marginRight="0";let m=document.createElement("span");m.className="text-[13px] font-semibold tracking-tight text-slate-100/95 truncate",m.textContent=t;let _=document.createElement("span");_.className="text-[12px] font-medium text-slate-200/90 truncate",_.textContent="unknown",g.append(e,m),o.append(g,_),y.appendChild(o),i[n]={row:o,emoji:e,label:m,status:_}});let s="/events/ops",r=(typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0;function h(t){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return"unknown";let n=(t||"").toLowerCase();return n?n.includes("error")||n.includes("failed")||n.includes("offline")?"error":n.includes("online")||n.includes("ready")||n.includes("ok")?"online":n.includes("queued")||n.includes("pending")||n.includes("init")||n.includes("running")?"pending":"unknown":"unknown"}function E(t,n){let o=i[t];if(!o)return;let g=h(n),e=n||"unknown";switch(o.status.textContent=e,o.row.className="w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm",o.emoji.className="inline-flex items-center justify-center shrink-0",o.emoji.textContent=S[t]||"\u2022",o.emoji.style.display="inline-flex",o.emoji.style.width="18px",o.emoji.style.minWidth="18px",o.emoji.style.height="18px",o.emoji.style.minHeight="18px",o.emoji.style.fontSize="14px",o.emoji.style.lineHeight="1",o.emoji.style.background="transparent",o.emoji.style.borderRadius="0",o.emoji.style.boxShadow="none",o.emoji.style.marginRight="0",o.label.className="text-[13px] font-semibold tracking-tight truncate",o.status.className="text-[11px] font-medium truncate",g){case"online":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-emerald-300/90");break;case"error":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-rose-300/90");break;case"pending":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-amber-200/90");break;default:o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-slate-300/75");break}}if(Object.keys(i).forEach(t=>E(t,"unknown")),r){console.warn("[agent-status-row] Optional SSE disabled:",s);return}function T(t){try{return JSON.parse(t)}catch{return null}}function A(t){return!t||typeof t!="object"?null:t.payload&&typeof t.payload=="object"?t.payload:t.data&&typeof t.data=="object"?t.data:t.state&&typeof t.state=="object"?t.state:t}function a(t){if(!t||typeof t!="object")return!1;let n=t.agents;if(!n||typeof n!="object")return!1;let o=!1;for(let[g,e]of Object.entries(n)){let m=String(g||"").toLowerCase();if(!i[m])continue;let _="unknown";if(typeof e=="string")_=e;else if(e&&typeof e=="object"){_=e.status??e.state??e.level??e.health??e.mode??"unknown";let C=e.at??e.ts??e.last_activity??e.lastActivity??e.last_seen??e.lastSeen??null;C!=null&&(c[m]=C)}E(m,String(_||"unknown")),o=!0}return I(),o}function d(t){if(!t||typeof t!="object")return!1;let n=(t.agent||t.actor||t.source||t.worker||t.name||"").toString().toLowerCase();if(!n||!i[n])return!1;let o=t.status??t.state??t.level??t.health??t.mode??"unknown",g=t.at??t.ts??t.last_activity??t.lastActivity??t.last_seen??t.lastSeen??null;return g!=null&&(c[n]=g),E(n,String(o||"unknown")),I(),!0}function x(t,n){let o=T(n.data);if(!o)return;let g=A(o);t==="ops.state"&&a(g)||a(o)||a(g)||(d(g),d(o))}let f;try{f=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(s)}catch(t){console.error("agent-status-row.js: Failed to open OPS SSE connection:",t);return}f&&(f.onmessage=t=>x("message",t),f.addEventListener("hello",t=>x("hello",t)),f.addEventListener("ops.state",t=>x("ops.state",t)),f.addEventListener("state",t=>x("state",t)),f.addEventListener("update",t=>x("update",t)),f.onerror=t=>{console.warn("agent-status-row.js: OPS SSE error:",t),Object.keys(i).forEach(n=>E(n,"unknown")),k(v,"\u2014")})})();var D="__broadcastVisualizationInited",j=["Matilda","Cade","Effie"];function R(){let p=document.getElementById("broadcast-visual");if(!p)return;let u=[];for(let b=0;b<j.length;b++){let S=j[b];u.push('<div class="node" id="node-'+S+'">'+S+"</div>"),b<j.length-1&&u.push('<div class="arrow">\u279C</div>')}p.innerHTML=u.join("")}function $(){let p=0;setInterval(()=>{document.querySelectorAll(".node").forEach(i=>i.classList.remove("active"));let b="node-"+j[p],S=document.getElementById(b);S&&S.classList.add("active"),p=(p+1)%j.length},1500)}function H(){if(typeof window>"u"||typeof document>"u"||window[D])return;window[D]=!0;let p=()=>{R(),$()};document.readyState==="loading"?window.addEventListener("DOMContentLoaded",p):p()}typeof window<"u"&&(window.initBroadcastVisualization=H);(function(){if(!(typeof document>"u")){var p=document.getElementById("ops-dashboard-pill");if(!p){var u=document.querySelector("[data-ops-pill]");u&&(u.id="ops-dashboard-pill")}}})();(()=>{if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED||typeof window>"u"||typeof EventSource>"u"||window.__opsGlobalsBridgeInitialized)return;window.__opsGlobalsBridgeInitialized=!0,typeof window.lastOpsHeartbeat>"u"&&(window.lastOpsHeartbeat=null),typeof window.lastOpsStatusSnapshot>"u"&&(window.lastOpsStatusSnapshot=null);let p="/events/ops";if((typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0){console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):",p);return}let b=S=>{try{let i=JSON.parse(S.data||"null");if(!i)return;window.lastOpsHeartbeat=Math.floor(Date.now()/1e3),window.lastOpsStatusSnapshot=i}catch(i){console.warn("[ops-globals-bridge] Failed to parse OPS event:",i)}try{window.dispatchEvent(new CustomEvent("mb:ops:update",{detail:{event:"message",state:window.lastOpsStatusSnapshot}}))}catch{}};try{let S=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(p);if(!S)return null;if(S.onmessage=b,S.addEventListener("hello",b),!S)return;S.onerror=i=>{console.warn("[ops-globals-bridge] EventSource error:",i)}}catch(S){console.warn("[ops-globals-bridge] Failed to init EventSource:",S)}})();(function(){if(typeof window>"u"||typeof document>"u"||window.location.pathname!=="/dashboard")return;var p=5e3,u="ops-dashboard-pill";function b(){var i=document.getElementById(u);return i||(i=document.createElement("span"),i.id=u,i.className="ops-pill ops-pill-unknown",i.textContent="OPS: Unknown",i.style.display="inline-block",document.body.firstChild?document.body.insertBefore(i,document.body.firstChild):document.body.appendChild(i),i)}function S(){var i=document.getElementById("ops-status-pill");i&&(i.style.display="none");var c=b();if(c){var w=typeof window.lastOpsHeartbeat=="number",v=w?"OPS: Online":"OPS: Unknown",k=w?"ops-pill-online":"ops-pill-unknown";c.classList.remove("ops-pill-unknown","ops-pill-online","ops-pill-stale","ops-pill-error"),c.classList.add(k),c.textContent=v}}S(),setInterval(S,p)})();(function(){function p(i){console.log("[matilda-chat]",i)}function u(i,c,w){if(i){var v=document.createElement("p");v.className="mb-1 text-sm";var k=c?c+": ":"";v.textContent=k+w,i.appendChild(v),i.scrollTop=i.scrollHeight}}function b(i,c,w){i&&(i.disabled=w,i.classList.toggle("opacity-60",w),i.textContent=w?"Sending...":"Send"),c&&(c.disabled=w)}async function S(){var i=document.getElementById("matilda-chat-root");if(!i){p("No #matilda-chat-root found; skipping wiring.");return}var c=document.getElementById("matilda-chat-transcript"),w=document.getElementById("matilda-chat-input"),v=document.getElementById("matilda-chat-send");if(window.appendMessage=window.appendMessage||function(y){try{var s=y&&y.role?String(y.role):"system",r=y&&(y.content??y.text??y.message)?String(y.content??y.text??y.message):"",h=s==="user"?"You":s==="matilda"?"Matilda":"System";u(c,h,r)}catch{}},window.__appendMessage=window.__appendMessage||window.appendMessage,!c||!w||!v){p("Missing one or more Matilda chat elements; aborting wiring.");return}function k(y){return(y||"").toString().trim()}async function l(){var y=k(w.value);if(y){u(c,"You",y),w.value="",b(v,w,!0);try{var s=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:y,agent:"matilda"})});if(!s.ok){u(c,"Matilda","(error talking to /api/chat)");return}var r=await s.json(),h=r&&(r.reply||r.message||r.response)||"(no reply)";u(c,"Matilda",h)}catch(E){console.error(E),u(c,"Matilda","(network error)")}finally{b(v,w,!1)}}}v.addEventListener("click",l);var I=document.getElementById("matilda-chat-quick-check");I&&I.addEventListener("click",function(){w.value="Quick systems check from dashboard Phase 11.4.",l()}),w.addEventListener("keydown",function(y){y.key==="Enter"&&!y.shiftKey&&(y.preventDefault(),l())}),p("Matilda chat wiring complete.")}document.addEventListener("DOMContentLoaded",S)})();(function(){console.log("[dashboard-delegation] module loaded");function p(s){return document.getElementById(s)}function u(){var s=window.fetch,r=typeof s;return console.log("[dashboard-delegation] detected fetch type:",r),r!=="function"?(console.error("[dashboard-delegation] fetch is not a function; value:",s),null):s.bind(window)}function b(s){return String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function S(s){return'<pre class="mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300">'+b(JSON.stringify(s,null,2))+"</pre>"}function i(s,r){var h=p("delegation-response"),E=p("delegation-status-panel");h&&(h.innerHTML=r,E&&(E.classList.remove("border-gray-700","border-teal-600","border-green-600","border-red-600","border-amber-500"),s==="sending"?E.classList.add("border-teal-600"):s==="success"?E.classList.add("border-green-600"):s==="error"?E.classList.add("border-red-600"):s==="waiting"?E.classList.add("border-amber-500"):E.classList.add("border-gray-700")))}function c(){i("idle","Awaiting operator input.<br>Results from delegation requests will appear here.")}function w(s){i("sending",'<div class="text-teal-300 font-medium">Sending delegation\u2026</div><div class="mt-2 text-gray-400">Preparing request for the orchestration layer.</div>'+(s?'<div class="mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words">'+b(s)+"</div>":""))}function v(){i("waiting",'<div class="text-amber-300 font-medium">Still waiting on delegation response\u2026</div><div class="mt-2 text-gray-400">The request may still be processing.</div>')}function k(s){var r="Delegation accepted.";s&&typeof s=="object"&&(r=s.message||s.status||s.result||s.reply||s.ok&&"Delegation accepted."||r),i("success",'<div class="text-green-300 font-medium">'+b(r)+'</div><div class="mt-2 text-gray-400">Request completed successfully.</div>'+(s&&typeof s=="object"?S(s):""))}function l(s,r){i("error",'<div class="text-red-300 font-medium">Delegation failed.</div><div class="mt-2 text-gray-300">'+b(s||"Unknown error")+"</div>"+(r?'<div class="mt-3 text-xs text-gray-400 break-words">'+b(r)+"</div>":""))}async function I(){var s=p("delegation-input"),r=p("delegation-submit");if(!s){console.warn("[dashboard-delegation] delegation input not found at click time"),l("Delegation input field was not found.");return}var h=String(s.value||"").trim();if(!h){console.warn("[dashboard-delegation] empty delegation input; skipping"),l("Please enter a delegation request before submitting.");return}console.log("[dashboard-delegation] sending delegation:",h);var E=u();if(!E){console.error("[dashboard-delegation] aborting delegation because fetch is unavailable or invalid"),l("Browser fetch is unavailable.");return}var T=r&&r.textContent||"Submit Delegation",A=null;try{r&&(r.disabled=!0,r.textContent="Sending...",r.classList.add("opacity-70","cursor-not-allowed")),w(h),A=window.setTimeout(v,4e3);var a;try{a=await E("/api/delegate-task",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({prompt:h,message:h,text:h,task:h})})}catch(f){throw console.error("[dashboard-delegation] fetch threw before response:",f),f}console.log("[dashboard-delegation] fetch returned:",{ok:a&&a.ok,status:a&&a.status,statusText:a&&a.statusText});var d=null,x="";try{x=await a.text(),d=x?JSON.parse(x):{}}catch(f){console.error("[dashboard-delegation] error parsing JSON response:",f),d={error:"Non-JSON response from /api/delegate-task",raw:x||""}}if(console.log("[dashboard-delegation] delegation response:",d),!a.ok){l(d&&(d.error||d.message||d.statusText)||"HTTP "+a.status+" "+(a.statusText||""),x);return}k(d)}catch(f){l(f&&f.message?f.message:String(f))}finally{A&&window.clearTimeout(A),r&&(r.disabled=!1,r.textContent=T,r.classList.remove("opacity-70","cursor-not-allowed"))}}function y(){var s=p("delegation-submit"),r=p("delegation-input");if(!s||!r){console.warn("[dashboard-delegation] delegation button or input not found in init");return}if(c(),s.dataset.delegationWired==="true"){console.log("[dashboard-delegation] Task Delegation wiring already active");return}s.dataset.delegationWired="true",s.addEventListener("click",I),r.addEventListener("keydown",function(h){(h.metaKey||h.ctrlKey)&&h.key==="Enter"&&(h.preventDefault(),I())}),console.log("[dashboard-delegation] Task Delegation wiring active")}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();(()=>{let p="/events/task-events",u="mb-task-events-panel",b="mb-task-events-feed",S="mb-task-events-counts",i="mb-task-events-panel-anchor";function c(){let n=document.getElementById(i);return n||document.body}function w(){if(document.getElementById(u))return;let n=c(),o=document.createElement("div");o.id=u;let g=n&&n.id===i;o.style.width=g?"100%":"360px",o.style.maxWidth=g?"100%":"calc(100vw - 24px)",o.style.maxHeight=g?"260px":"40vh",o.style.overflow="hidden",o.style.zIndex="9999",o.style.border="1px solid rgba(255,255,255,0.12)",o.style.borderRadius="14px",o.style.background="rgba(10,10,14,0.92)",o.style.backdropFilter="blur(10px)",o.style.boxShadow="0 10px 30px rgba(0,0,0,0.35)",o.style.color="rgba(255,255,255,0.92)",o.style.fontFamily="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial",g?o.style.marginTop="12px":(o.style.position="fixed",o.style.right="12px",o.style.bottom="12px");let e=document.createElement("div");e.style.display="flex",e.style.alignItems="center",e.style.justifyContent="space-between",e.style.gap="8px",e.style.padding="10px 12px",e.style.borderBottom="1px solid rgba(255,255,255,0.10)";let m=document.createElement("div");m.textContent="Task Events",m.style.fontSize="12px",m.style.letterSpacing="0.06em",m.style.opacity="0.9";let _=document.createElement("div");_.style.display="flex",_.style.alignItems="center",_.style.gap="10px";let C=document.createElement("div");C.id=S,C.textContent="created:0  completed:0  failed:0",C.style.fontSize="11px",C.style.opacity="0.85",C.style.fontVariantNumeric="tabular-nums";let L=document.createElement("span");L.setAttribute("aria-label","task-events connection"),L.title="task-events connection",L.style.display="inline-block",L.style.width="10px",L.style.height="10px",L.style.borderRadius="999px",L.style.background="rgba(255,255,255,0.25)",L.style.boxShadow="0 0 0 2px rgba(255,255,255,0.08) inset";let O=document.createElement("button");O.type="button",O.textContent="\xD7",O.title="hide",O.style.cursor="pointer",O.style.border="1px solid rgba(255,255,255,0.14)",O.style.background="transparent",O.style.color="rgba(255,255,255,0.85)",O.style.borderRadius="10px",O.style.width="28px",O.style.height="24px",O.style.lineHeight="22px",O.style.fontSize="14px",O.onclick=()=>o.remove(),_.appendChild(L),_.appendChild(C),_.appendChild(O),e.appendChild(m),e.appendChild(_);let N=document.createElement("div");N.id=b,N.style.padding="10px 12px",N.style.overflow="auto",N.style.maxHeight=g?"200px":"calc(40vh - 46px)",o.appendChild(e),o.appendChild(N),n.appendChild(o),window.__MB_TASK_EVENTS_PANEL={dot:L,feed:N,counts:C},console.log("[phase22] task-events panel mounted (anchored=%s)",g)}function v(n){w();let o=window.__MB_TASK_EVENTS_PANEL?.dot;o&&(n==="open"?o.style.background="rgba(80,200,120,0.85)":n==="error"?o.style.background="rgba(240,90,90,0.85)":o.style.background="rgba(255,255,255,0.25)")}let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\.created|task\.started|task\.running|task\.queued|task\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\.completed|task\.failed|task\.cancelled|task\.canceled|task\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}function y(n){if(typeof n=="number")return new Date(n).toISOString();if(typeof n=="string"&&n.trim()){let o=new Date(n);return Number.isNaN(o.getTime())?n.trim():o.toISOString()}return new Date().toISOString()}function s(n,o,g){let e=`${n||""} ${o||""} ${g||""}`.toLowerCase();return/fail|error|cancel|timeout/.test(e)?"terminal-error":/complete|done|success/.test(e)?"terminal-success":/queue|pending|retry|wait|hold|sleep/.test(e)?"waiting":/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(e)?"active":"neutral"}function r(n){return n==="terminal-error"?"rgba(240,90,90,0.95)":n==="terminal-success"?"rgba(80,200,120,0.95)":n==="waiting"?"rgba(250,204,21,0.95)":n==="active"?"rgba(96,165,250,0.95)":"rgba(255,255,255,0.18)"}function h(n,o){let g=y(n.ts),e=String(n.kind??o??"event"),m=n.task_id??n.taskId??"unknown",_=n.run_id??n.runId??"",C=n.actor??n.meta?.actor??n.meta?.owner??"",L=n.status??n.meta?.status??"",O=typeof n.cursor=="number"||typeof n.cursor=="string"?String(n.cursor):"",N=String(n.msg??n.message??"").trim(),B=[];B.push(`task=${m}`),_&&B.push(`run=${_}`),C&&B.push(`actor=${C}`),L&&B.push(`status=${L}`),O&&B.push(`cursor=${O}`),N&&B.push(N);let M=B.join(" \u2022 "),P=s(e,N,L);return{ts:g,kind:e,detail:M,tone:P}}function E(n,o){w();let g=document.getElementById(b);if(!g)return;let e=h(n,o),m=document.createElement("div");m.className=`phase61-task-event phase61-task-event-${e.tone}`,m.dataset.eventKind=e.kind,m.dataset.eventTone=e.tone,m.style.position="relative",m.style.display="grid",m.style.gridTemplateColumns="minmax(112px, 132px) minmax(150px, 170px) 1fr",m.style.gap="10px",m.style.alignItems="start",m.style.padding="10px 12px 10px 14px",m.style.marginBottom="8px",m.style.border="1px solid rgba(255,255,255,0.10)",m.style.borderRadius="12px",m.style.background="rgba(255,255,255,0.03)",m.style.lineHeight="1.35",m.style.fontSize="12px";let _=document.createElement("span");_.setAttribute("aria-hidden","true"),_.style.position="absolute",_.style.left="0",_.style.top="10px",_.style.bottom="10px",_.style.width="3px",_.style.borderRadius="999px",_.style.background=r(e.tone);let C=document.createElement("div");C.textContent=e.ts,C.style.color="rgba(255,255,255,0.68)",C.style.fontVariantNumeric="tabular-nums",C.style.whiteSpace="nowrap";let L=document.createElement("div");L.textContent=e.kind,L.style.color="rgba(255,255,255,0.92)",L.style.fontWeight="600",L.style.letterSpacing="0.01em",L.style.wordBreak="break-word";let O=document.createElement("div");O.textContent=e.detail||"\u2014",O.style.color="rgba(255,255,255,0.78)",O.style.wordBreak="break-word",m.appendChild(_),m.appendChild(C),m.appendChild(L),m.appendChild(O),g.prepend(m);let N=Array.from(g.children);if(N.length>60)for(let B=60;B<N.length;B++)N[B].remove()}function T(n){try{window.dispatchEvent(new CustomEvent("mb.task.event",{detail:n}))}catch{}}function A(n){try{return JSON.parse(n)}catch{return null}}function a(n,o){let g=typeof o=="string"?A(o):o,e=g&&typeof g=="object"?g:{kind:n,raw:o};n==="task.event"&&(e.actor==null&&e.meta&&typeof e.meta=="object"&&(e.actor=e.meta.actor??e.meta.owner??null),!e.kind&&e.type&&(e.kind=e.type),e.kind==="task.event"&&e.type&&(e.kind=e.type),e.task_id==null&&e.taskId!=null&&(e.task_id=e.taskId),e.run_id==null&&e.runId!=null&&(e.run_id=e.runId),e&&e.meta&&typeof e.meta=="object"&&(e.task_id==null&&e.meta.task_id!=null&&(e.task_id=e.meta.task_id),e.run_id==null&&e.meta.run_id!=null&&(e.run_id=e.meta.run_id),e.actor==null&&e.meta.actor!=null&&(e.actor=e.meta.actor),e.actor==null&&e.meta.owner!=null&&(e.actor=e.meta.owner),e.status==null&&e.meta.status!=null&&(e.status=e.meta.status))),e.kind||(e.kind=n);let m=`${n}|${e.kind}|${e.ts??""}|${e.task_id??""}|${e.run_id??""}|${e.cursor??""}`;if(!k.has(m)){if(k.add(m),(e.kind==="task.created"||e.kind==="task.completed"||e.kind==="task.failed")&&I(String(e.kind)),G(e,n),E(e,n),window.__UI_DEBUG)try{console.log("[task-events] mb.task.event",e)}catch{}T(e)}}let d=null,x=0;function f(){if(w(),d){try{d.close()}catch{}d=null}let n=p;d=new EventSource(n),d.onopen=()=>{x=0,v("open"),E({ts:Date.now(),kind:"sse.open",message:`url=${n}`},"sse.open"),console.log("[phase22] task-events SSE open",n)},d.onerror=()=>{v("error");try{d.close()}catch{}d=null,x+=1;let g=Math.min(15e3,500*Math.pow(2,Math.min(6,x)));E({ts:Date.now(),kind:"sse.error",message:`reconnect_in=${g}ms`},"sse.error"),console.log("[phase22] task-events SSE error; reconnect in",g),setTimeout(f,g)},d.onmessage=g=>a("message",g.data);let o=["hello","heartbeat","task.event","task.created","task.completed","task.failed","task.updated","task.status"];for(let g of o)d.addEventListener(g,e=>a(g,e.data))}function t(){f(),setInterval(()=>{if(!document.getElementById(u))try{f()}catch{}},2e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",t,{once:!0}):t(),window.__MB_TASK_EVENTS={url:p,reconnect:()=>f()}})();(()=>{let p="mb.task.event",u=new Map,b={queued:"task-status-queued",done:"task-status-done",failed:"task-status-failed"};function S(s){let r=String(s??"").toLowerCase();return r==="queued"||r==="pending"?"queued":r==="done"||r==="complete"||r==="completed"?"done":r==="failed"||r==="error"?"failed":r||"unknown"}function i(s){return s?.task_id??s?.taskId??s?.task?.id??null}function c(s){let r=s?.task&&typeof s.task=="object"?s.task:null,h=i(s),E=s?.status??s?.payload?.status??r?.status??(s?.kind==="task.created"?"queued":null);return{id:h!=null?String(h):null,status:E!=null?S(E):null,title:r?.title??s?.title??null,agent:r?.agent??s?.agent??null,error:s?.error??s?.payload?.error??r?.error??null,updated_at:r?.updated_at??s?.ts??Date.now()}}function w(s,r){if(!s)return;let h=S(r);s.setAttribute("data-task-status",h),s.classList?.remove(...Object.values(b)),b[h]&&s.classList?.add(b[h]);let E=s.querySelector?.("[data-task-field='status']")||s.querySelector?.(".task-status")||s.querySelector?.(".status")||null;E&&(E.textContent=h)}function v(s){if(!s?.id)return;let r=String(s.id),h=[document.getElementById(`task-${r}`),document.getElementById(`task_${r}`),document.querySelector?.(`[data-task-id="${CSS.escape(r)}"]`),document.querySelector?.(`[data-taskid="${CSS.escape(r)}"]`)].filter(Boolean);for(let E of h)w(E,s.status)}function k(){let s=0,r=0,h=0;for(let T of u.values()){let A=S(T.status);A==="queued"?s++:A==="done"?r++:A==="failed"&&h++}let E=[["queued",s],["done",r],["failed",h]];for(let[T,A]of E){let a=document.getElementById(`task-count-${T}`)||document.getElementById(`tasks-${T}-count`)||document.querySelector?.(`[data-task-count="${T}"]`)||null;a&&(a.textContent=String(A))}}function l(s){if(!s?.id)return;let r=String(s.id),h=u.get(r)||{},E={...h,...s,id:r,status:s.status??h.status};u.set(r,E),v(E),k()}function I(s){let r=c(s);!r.id&&s?.kind&&(s.kind==="task.completed"&&(r.status="done"),s.kind==="task.failed"&&(r.status="failed")),r.id&&l(r)}function y(){window.__PHASE22_TASK_UI_BOUND||(window.__PHASE22_TASK_UI_BOUND=!0,window.addEventListener(p,s=>{try{(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&console.log("[phase22] mb.task.event",s.detail),I(s.detail)}catch{}}),window.__PHASE22_TASK_UI={tasks:u},console.log("[phase22] bindings attached"))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();typeof window<"u"&&typeof window.__DISABLE_OPTIONAL_SSE>"u"&&(window.__DISABLE_OPTIONAL_SSE=!1);})();
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3477:./public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance with role emojis.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Atlas\", \"Cade\", \"Effie\"];\n\n  const AGENT_EMOJI = {\n    matilda: \"\uD83D\uDDE3\uFE0F\",\n    atlas: \"\uD83E\uDDED\",\n    cade: \"\uD83D\uDCBB\",\n    effie: \"\uD83D\uDCCA\",\n  };\n\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const key = name.toLowerCase();\n\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const emoji = document.createElement(\"span\");\n    emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    emoji.textContent = AGENT_EMOJI[key] || \"\u2022\";\n    emoji.style.width = \"18px\";\n    emoji.style.minWidth = \"18px\";\n    emoji.style.height = \"18px\";\n    emoji.style.minHeight = \"18px\";\n    emoji.style.fontSize = \"14px\";\n    emoji.style.lineHeight = \"1\";\n    emoji.style.background = \"transparent\";\n    emoji.style.borderRadius = \"0\";\n    emoji.style.boxShadow = \"none\";\n    emoji.style.marginRight = \"0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(emoji, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[key] = { row, emoji, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    indicator.emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    indicator.emoji.textContent = AGENT_EMOJI[agentKey] || \"\u2022\";\n    indicator.emoji.style.display = \"inline-flex\";\n    indicator.emoji.style.width = \"18px\";\n    indicator.emoji.style.minWidth = \"18px\";\n    indicator.emoji.style.height = \"18px\";\n    indicator.emoji.style.minHeight = \"18px\";\n    indicator.emoji.style.fontSize = \"14px\";\n    indicator.emoji.style.lineHeight = \"1\";\n    indicator.emoji.style.background = \"transparent\";\n    indicator.emoji.style.borderRadius = \"0\";\n    indicator.emoji.style.boxShadow = \"none\";\n    indicator.emoji.style.marginRight = \"0\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  function parseJson(raw) {\n    try {\n      return JSON.parse(raw);\n    } catch {\n      return null;\n    }\n  }\n\n  function extractPayload(data) {\n    if (!data || typeof data !== \"object\") return null;\n    if (data.payload && typeof data.payload === \"object\") return data.payload;\n    if (data.data && typeof data.data === \"object\") return data.data;\n    if (data.state && typeof data.state === \"object\") return data.state;\n    return data;\n  }\n\n  function applyAgentMap(payload) {\n    if (!payload || typeof payload !== \"object\") return false;\n    const agents = payload.agents;\n    if (!agents || typeof agents !== \"object\") return false;\n\n    let applied = false;\n    for (const [name, value] of Object.entries(agents)) {\n      const key = String(name || \"\").toLowerCase();\n      if (!indicators[key]) continue;\n\n      let status = \"unknown\";\n      if (typeof value === \"string\") {\n        status = value;\n      } else if (value && typeof value === \"object\") {\n        status =\n          value.status ??\n          value.state ??\n          value.level ??\n          value.health ??\n          value.mode ??\n          \"unknown\";\n      }\n\n      applyVisual(key, String(status || \"unknown\"));\n      applied = true;\n    }\n    return applied;\n  }\n\n  function applySingleAgent(data) {\n    if (!data || typeof data !== \"object\") return false;\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || data.name || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return false;\n\n    const status =\n      data.status ??\n      data.state ??\n      data.level ??\n      data.health ??\n      data.mode ??\n      \"unknown\";\n\n    applyVisual(agentName, String(status || \"unknown\"));\n    return true;\n  }\n\n  function handleOpsEvent(eventName, event) {\n    const data = parseJson(event.data);\n    if (!data) return;\n\n    const payload = extractPayload(data);\n\n    if (eventName === \"ops.state\") {\n      if (applyAgentMap(payload)) return;\n    }\n\n    if (applyAgentMap(data)) return;\n    if (applyAgentMap(payload)) return;\n    applySingleAgent(payload);\n    applySingleAgent(data);\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => handleOpsEvent(\"message\", event);\n  source.addEventListener(\"hello\", (event) => handleOpsEvent(\"hello\", event));\n  source.addEventListener(\"ops.state\", (event) => handleOpsEvent(\"ops.state\", event));\n  source.addEventListener(\"state\", (event) => handleOpsEvent(\"state\", event));\n  source.addEventListener(\"update\", (event) => handleOpsEvent(\"update\", event));\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3931:./public/js/ops-globals-bridge.js:26:    console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):", opsUrl);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3974:./public/js/phase22_task_delegation_live_bindings.js:32:      (ev?.kind === "task.created" ? "queued" : null);
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4046:./public/js/telemetry_queue_depth_reducer.js:11:    return state === "task.created" || state === "task.queued";
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4096:./public/scripts/agent-status-row.js:19:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:4987:./scripts/_local/phase62b_hydrate_tasks_running.sh:15:new = 'let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\\.created|task\\.started|task\\.running|task\\.queued|task\\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\\.completed|task\\.failed|task\\.cancelled|task\\.canceled|task\\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}'
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5361:./scripts/_local/phase65_1_tasks_running_inspect.sh:17:rg -n 'task\.|task-events|EventSource|SSE|recent history|recent tasks|running|queued|completed|failed|cancelled|started' \
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5402:./scripts/_local/phase65_5_post_patch_verify.sh:32:  grep -nE 'base_url=|dashboard_up=|container_http_up=|metric-tasks|metric-success|metric-success-rate|metric-latency|policy.probe.task|task_status|status":"running"|status":"queued"|Recv failure|Connection reset|error|failed' \
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:5975:./scripts/phase14_5_patch_dashboard_no_cache.mjs:104:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6100:./scripts/phase22_wire_task_events_ui.sh:293:      echo "Appending import for task-events-sse-client into: $f"
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6143:./scripts/phase35_fix_appendTaskEvent_pool_signature.sh:113:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6439:./server/task-events.mjs:153:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./docs/checkpoints/PHASE66C_REDUCER_EVENT_ASSUMPTIONS_20260316.md:57:task.queued
./docs/checkpoints/PHASE66C_REDUCER_EVENT_ASSUMPTIONS_20260316.md:107:task.queued
./docs/checkpoints/PHASE61_5_5N_LOCATE_OPS_STATE_EMITTER_20260310_172424.txt:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE61_5_5N_LOCATE_OPS_STATE_EMITTER_20260310_172424.txt:955:    if (s.includes("queued") || s.includes("pending") || s.includes("init") || s.includes("running")) return "pending";
./docs/checkpoints/PHASE61_5_5N_LOCATE_OPS_STATE_EMITTER_20260310_172424.txt:1003:      case "pending":
./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:136:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:219:213:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:331:public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Cade\", \"Effie\", \"Atlas\"];\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const bar = document.createElement(\"span\");\n    bar.className = \"inline-block w-2 h-2 rounded-full bg-amber-300 shrink-0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(bar, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[name.toLowerCase()] = { row, bar, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n    \nindicator.bar.className = \"inline-block shrink-0\";\n    indicator.bar.style.display = \"inline-block\";\n    indicator.bar.style.width = \"8px\";\n    indicator.bar.style.height = \"8px\";\n    indicator.bar.style.minWidth = \"8px\";\n    indicator.bar.style.minHeight = \"8px\";\n    indicator.bar.style.borderRadius = \"999px\";\n    indicator.bar.style.marginRight = \"8px\";\n    indicator.bar.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n    indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\nindicator.bar.style.width = \"8px\";\nindicator.bar.style.height = \"8px\";\nindicator.bar.style.minWidth = \"8px\";\nindicator.bar.style.minHeight = \"8px\";\nindicator.bar.style.borderRadius = \"999px\";\nindicator.bar.style.marginRight = \"8px\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(52,211,153,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(251,113,133,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(252,211,77,0.95)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.bar.style.background = \"rgba(148,163,184,0.8)\";\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => {\n    let data;\n    try {\n      data = JSON.parse(event.data);\n    } catch {\n      return;\n    }\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return;\n\n    const status = (data.status || data.state || data.level || \"\").toString() || \"unknown\";\n    applyVisual(agentName, status);\n  };\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:418:    if (s.includes("queued") || s.includes("pending") || s.includes("init") || s.includes("running")) return "pending";
./docs/checkpoints/PHASE61_5_5O_EXTRACT_OPS_FINDINGS_20260310_172536.txt:466:      case "pending":
./docs/checkpoints/STATE_HANDOFF_PHASE63_1_COMPLETE_TO_63_2_20260312.md:123:- queued
./docs/checkpoints/PHASE62B_TASKS_COMPLETED_EXECUTION_PROTOCOL_20260316.md:56:• queued
./docs/checkpoints/PHASE62B_TASKS_FAILED_EXECUTION_PROTOCOL_20260316.md:58:• queued
./docs/checkpoints/PHASE63_1_SHARED_TASK_EVENTS_METRIC_CORRIDOR_NORMALIZED_20260312.md:31:- `queued`
./docs/checkpoints/PHASE63_3_SEMANTIC_VERIFIER_CANDIDATES_AUDIT_STARTED_20260312.md:34:- depending on live network timing
./docs/checkpoints/PHASE63_3_SEMANTIC_VERIFIER_CANDIDATES_AUDIT_STARTED_20260312.md:35:- depending on full browser automation
./docs/checkpoints/PHASE62B_NEXT_METRIC_SELECTION_20260316.md:80:queued
./docs/checkpoints/PHASE66A_TELEMETRY_INTEGRITY_VERIFICATION_PLAN_20260316.md:92:task.queued
./docs/phase17/PHASE17_NEXT_WIRING_PLAN.md:19:   - read queued tasks
./PHASE32_SUPERVISOR_SUMMARY.txt:21:- phase32_claim_one.sql: claims only queued + eligible by next_run_at; FOR UPDATE SKIP LOCKED prevents double-claim
./PHASE32_SUPERVISOR_SUMMARY.txt:22:- phase32_mark_failure.sql: guarded from running; attempts++ ; queued+next_run_at w/ capped exponential backoff; terminal failed at max_attempts
./PHASE39_STATE_SEALED.md:34:Phase 39 is sealed pending merge + golden tag.
./PHASE62B_TASKS_QUEUED_PROTOCOL.md:28:Count queued status
./PHASE62B_TASKS_QUEUED_PROTOCOL.md:33:Step 1 — Identify queued state definition
./PHASE27_FAILURE_RETRY_CONTRACT.md:15:- queued: eligible to claim (subject to available_at + lease rules)
./PHASE27_FAILURE_RETRY_CONTRACT.md:22:- status = 'queued'
./PHASE27_FAILURE_RETRY_CONTRACT.md:56:- status = 'queued'
./PHASE27_FAILURE_RETRY_CONTRACT.md:63:- task.requeued { ts, task_id, run_id, actor, attempt, max_attempts, available_at }
./cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./PROJECT_VISUAL_OUTPUT_CONTAINER_DEPLOYMENT.md:16:2. Push any pending local commits:
./public/bundle.js.map:4:  "sourcesContent": ["/**\n * Phase 16 \u2013 Shared Heartbeat Bus (OPS + Tasks)\n *\n * Wrap EventSource so connection + any SSE message updates a normalized heartbeat store.\n *\n * Exposes:\n *   window.__HB = { record(kind, ts), get(kind), snapshot() }\n *\n * kind: \"ops\" | \"tasks\" | \"reflections\" | \"unknown\"\n */\n(function () {\n  const w = window;\n\n  const STORE_KEY = \"__HB\";\n  if (!w[STORE_KEY]) {\n    const state = { ops: null, tasks: null, reflections: null, unknown: null };\n    w[STORE_KEY] = {\n      record(kind, ts) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        state[k] = typeof ts === \"number\" ? ts : Date.now();\n        return state[k];\n      },\n      get(kind) {\n        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : \"unknown\";\n        return state[k];\n      },\n      snapshot() {\n        return { ...state };\n      },\n    };\n  }\n\n  const NativeEventSource = w.EventSource;\n  if (!NativeEventSource || NativeEventSource.__hbWrapped) return;\n\n  function classify(url) {\n    const u = String(url || \"\");\n    if (u.includes(\"/events/ops\")) return \"ops\";\n    if (u.includes(\"/events/task-events\") || u.includes(\"/events/tasks\")) return \"tasks\";\n    if (u.includes(\"/events/reflections\")) return \"reflections\";\n    return \"unknown\";\n  }\n\n  function HeartbeatEventSource(url, eventSourceInitDict) {\n    const kind = classify(url);\n\n    // Record immediately on connection attempt so \"connected but quiet\" still looks alive.\n    try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n\n    const es = new NativeEventSource(url, eventSourceInitDict);\n\n    const update = () => {\n      try { w[STORE_KEY].record(kind, Date.now()); } catch (_) {}\n    };\n\n    // Update heartbeat when the connection opens (most reliable for \"alive\").\n    try { es.addEventListener(\"open\", update); } catch (_) {}\n\n    // Update heartbeat on any message for this stream.\n    try { es.addEventListener(\"message\", update); } catch (_) {}\n\n    // Also wrap onmessage if assigned later.\n    let _onmessage = null;\n    Object.defineProperty(es, \"onmessage\", {\n      get() { return _onmessage; },\n      set(fn) {\n        _onmessage = function (ev) {\n          update();\n          if (typeof fn === \"function\") return fn.call(es, ev);\n        };\n      },\n      configurable: true,\n    });\n\n    // Update heartbeat when errors occur too (helps show \"something happened\" vs silence).\n    try { es.addEventListener(\"error\", update); } catch (_) {}\n\n    return es;\n  }\n\n  HeartbeatEventSource.prototype = NativeEventSource.prototype;\n  HeartbeatEventSource.__hbWrapped = true;\n\n  w.EventSource = HeartbeatEventSource;\n})();\n", "/**\n * Phase 16 \u2013 Heartbeat stale-state indicator (non-intrusive)\n *\n * Reads window.__HB (from sse-heartbeat-shim) and updates a tiny badge.\n * - Adds a badge to the top-right corner of the dashboard.\n * - Shows OK when both OPS+Tasks have recent heartbeats.\n * - Shows STALE when either stream is missing or old.\n *\n * No coupling to widget internals. Safe to remove.\n */\n(function () {\n  const w = window;\n  const HB = w.__HB;\n\n  function now() {\n    return Date.now();\n  }\n\n  function ms(n) {\n    return Math.max(0, Number(n) || 0);\n  }\n\n  // Consider heartbeats stale after this many ms.\n  const STALE_MS = 15000;\n\n  function fmtAge(ts) {\n    if (!ts) return \"\u2014\";\n    const s = Math.floor((now() - ts) / 1000);\n    return s <= 0 ? \"0s\" : `${s}s`;\n  }\n\n  function ensureBadge() {\n    let el = document.getElementById(\"hb-badge\");\n    if (el) return el;\n\n    el = document.createElement(\"div\");\n    el.id = \"hb-badge\";\n    el.setAttribute(\"role\", \"status\");\n    el.style.position = \"fixed\";\n    el.style.top = \"12px\";\n    el.style.right = \"12px\";\n    el.style.zIndex = \"9999\";\n    el.style.fontFamily = \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n    el.style.fontSize = \"12px\";\n    el.style.padding = \"6px 10px\";\n    el.style.borderRadius = \"999px\";\n    el.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    el.style.background = \"rgba(0,0,0,0.55)\";\n    el.style.backdropFilter = \"blur(6px)\";\n    el.style.webkitBackdropFilter = \"blur(6px)\";\n    el.style.color = \"rgba(255,255,255,0.92)\";\n    el.style.boxShadow = \"0 8px 18px rgba(0,0,0,0.35)\";\n    el.style.userSelect = \"none\";\n    el.style.cursor = \"default\";\n    document.body.appendChild(el);\n    return el;\n  }\n\n  function setState(el, ok) {\n    // No colors specified by instruction in other contexts; keep neutral text-only signal.\n    // We\u2019ll use symbols instead of colored fills.\n    el.textContent = ok\n      ? `HB \u2713 (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`\n      : `HB ! (ops ${fmtAge(HB && HB.get(\"ops\"))}, tasks ${fmtAge(HB && HB.get(\"tasks\"))})`;\n  }\n\n  function tick() {\n    const el = ensureBadge();\n\n    if (!HB || typeof HB.get !== \"function\") {\n      el.textContent = \"HB ? (shim not loaded)\";\n      return;\n    }\n\n    const ops = HB.get(\"ops\");\n    const tasks = HB.get(\"tasks\");\n\n    const opsOk = !!ops && ms(now() - ops) <= STALE_MS;\n    const tasksOk = !!tasks && ms(now() - tasks) <= STALE_MS;\n\n    setState(el, opsOk && tasksOk);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", () => {\n      tick();\n      setInterval(tick, 1000);\n    });\n  } else {\n    tick();\n    setInterval(tick, 1000);\n  }\n})();\n", "/**\n * Phase 16: Dashboard wiring for OPS + Reflections SSE streams.\n *\n * - Consume /events/ops + /events/reflections via EventSource\n * - Treat *.state as \"initial paint\" (replace baseline state)\n * - Treat subsequent events as incremental updates (merge / patch)\n * - Add tiny \u201Cconnected / last event\u201D UI indicator for each stream\n *\n * NOTE: Your SSE server emits NAMED events (e.g. \"ops.state\", \"reflections.state\").\n * EventSource.onmessage only receives UNNAMED events, so we must addEventListener()\n * for the named event types we care about.\n */\n\n(() => {\n  \"use strict\";\n\n  const OPS_SSE_URL = \"/events/ops\";\n  const REFLECTIONS_SSE_URL = \"/events/reflections\";\n\n  const NOW = () => Date.now();\n\n  function safeJsonParse(s) {\n    try { return JSON.parse(s); } catch { return null; }\n  }\n\n  function formatAge(ms) {\n    if (!Number.isFinite(ms)) return \"\u2014\";\n    const s = Math.floor(ms / 1000);\n    if (s < 60) return `${s}s`;\n    const m = Math.floor(s / 60);\n    if (m < 60) return `${m}m`;\n    return `${Math.floor(m / 60)}h`;\n  }\n\n  function el(tag, attrs = {}, text = \"\") {\n    const n = document.createElement(tag);\n    for (const [k, v] of Object.entries(attrs)) {\n      if (k === \"class\") n.className = v;\n      else if (k === \"style\") n.setAttribute(\"style\", v);\n      else n.setAttribute(k, v);\n    }\n    if (text) n.textContent = text;\n    return n;\n  }\n\n  function ensureStyles() {\n    if (document.getElementById(\"phase16-sse-style\")) return;\n    const s = el(\"style\", { id: \"phase16-sse-style\" });\n    s.textContent = `\n      .sse-indicator {\n        display:inline-flex;\n        align-items:center;\n        gap:6px;\n        font-size:11px;\n        line-height:1;\n        opacity:.85;\n        user-select:none;\n        white-space:nowrap;\n      }\n      .sse-indicator .dot {\n        width:7px; height:7px; border-radius:999px;\n        background:#555;\n        box-shadow:0 0 0 1px rgba(255,255,255,.08) inset;\n      }\n      .sse-indicator[data-connected=\"true\"] .dot { background:#2dd4bf; }\n      .sse-indicator[data-connected=\"false\"] .dot { background:#f97316; }\n      .sse-indicator .meta { font-variant-numeric: tabular-nums; }\n    `;\n    document.head.appendChild(s);\n  }\n\n  function mount(anchor, id, label) {\n    ensureStyles();\n\n    if (!anchor) {\n      let tray = document.getElementById(\"phase16-sse-tray\");\n      if (!tray) {\n        tray = el(\"div\", {\n          id: \"phase16-sse-tray\",\n          style: [\n            \"position:fixed\",\n            \"left:10px\",\n            \"bottom:10px\",\n            \"display:flex\",\n            \"flex-direction:column\",\n            \"gap:6px\",\n            \"z-index:9999\",\n            \"pointer-events:none\",\n          ].join(\";\")\n        });\n        document.body.appendChild(tray);\n      }\n      anchor = tray;\n    }\n\n    let node = document.getElementById(id);\n    if (node) return node;\n\n    node = el(\"span\", { id, class: \"sse-indicator\", \"data-connected\": \"false\" });\n    node.append(\n      el(\"span\", { class: \"dot\", \"aria-hidden\": \"true\" }),\n      el(\"span\", { class: \"meta\" }, `${label}: disconnected \u00B7 last: \u2014`)\n    );\n\n    // try to place nicely; otherwise append\n    try {\n      if (anchor.matches && anchor.matches(\"header,h1,h2,h3,h4,strong\")) {\n        const wrap = el(\"span\", { style: \"margin-left:8px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      } else {\n        const wrap = el(\"div\", { style: \"margin-top:4px\" });\n        wrap.appendChild(node);\n        anchor.appendChild(wrap);\n      }\n    } catch {\n      anchor.appendChild(node);\n    }\n\n    return node;\n  }\n\n  function set(ind, label, connected, lastAt) {\n    if (!ind) return;\n    ind.dataset.connected = connected ? \"true\" : \"false\";\n    const meta = ind.querySelector(\".meta\");\n    if (!meta) return;\n    meta.textContent = `${label}: ${connected ? \"connected\" : \"disconnected\"} \u00B7 last: ${lastAt ? formatAge(NOW() - lastAt) : \"\u2014\"}`;\n  }\n\n  function ensureGlobal() {\n    window.__MB_STREAMS ||= {\n      ops: { connected: false, lastAt: 0, state: {}, es: null },\n      reflections: { connected: false, lastAt: 0, state: {}, es: null },\n    };\n    return window.__MB_STREAMS;\n  }\n\n  function shallowMerge(target, patch) {\n    if (!target || typeof target !== \"object\") target = {};\n    if (!patch || typeof patch !== \"object\") return target;\n    return Object.assign(target, patch);\n  }\n\n  // Minimal dot-path patch support: { path:\"a.b.c\", value:any }\n  function applyDotPathPatch(state, patch) {\n    if (!state || typeof state !== \"object\") state = {};\n    const path = patch && typeof patch.path === \"string\" ? patch.path : \"\";\n    if (!path) return state;\n    const parts = path.split(\".\").filter(Boolean);\n    if (!parts.length) return state;\n    let cur = state;\n    for (let i = 0; i < parts.length - 1; i++) {\n      const k = parts[i];\n      if (!cur[k] || typeof cur[k] !== \"object\") cur[k] = {};\n      cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = patch.value;\n    return state;\n  }\n\n  function isStateEvent(evtType, parsed) {\n    if (typeof evtType === \"string\" && evtType.endsWith(\".state\")) return true;\n    if (parsed && typeof parsed === \"object\") {\n      if (parsed.state && typeof parsed.state === \"object\") return true;\n      if (typeof parsed.type === \"string\" && parsed.type.includes(\"state\")) return true;\n      if (typeof parsed.event === \"string\" && parsed.event.includes(\"state\")) return true;\n    }\n    return false;\n  }\n\n  function extractPayload(parsed) {\n    if (!parsed || typeof parsed !== \"object\") return parsed;\n    if (parsed.payload !== undefined) return parsed.payload;\n    if (parsed.data !== undefined) return parsed.data;\n    if (parsed.delta !== undefined) return parsed.delta;\n    if (parsed.patch !== undefined) return parsed.patch;\n    if (parsed.state !== undefined) return parsed.state;\n    return parsed;\n  }\n\n  function connect(key, label, url, ind) {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) {\n    return null;\n  }\n\n    const g = ensureGlobal();\n\n    // close any existing connection\n    try { g[key].es && g[key].es.close(); } catch {}\n    g[key].es = null;\n\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(url));\n    g[key].es = es;\n\n\n    // Phase16: guard null EventSource before handlers\n    if (!es) return null;\n\n    const tick = () => set(ind, label, g[key].connected, g[key].lastAt);\n\n    es.onopen = () => { g[key].connected = true; tick(); };\n    es.onerror = () => { g[key].connected = false; tick(); };\n\n    const handle = (evtType, e) => {\n      g[key].lastAt = NOW();\n\n      const parsed = safeJsonParse(e && e.data ? e.data : \"\");\n      const payload = extractPayload(parsed);\n\n      if (isStateEvent(evtType, parsed)) {\n        // initial paint: replace\n        g[key].state = (payload && typeof payload === \"object\") ? payload : { value: payload };\n      } else {\n        // incremental: patch or merge\n        if (payload && typeof payload === \"object\") {\n          if (typeof payload.path === \"string\" && \"value\" in payload) {\n            g[key].state = applyDotPathPatch(g[key].state, payload);\n          } else {\n            g[key].state = shallowMerge(g[key].state, payload);\n          }\n        } else if (payload !== null && payload !== undefined) {\n          g[key].state = shallowMerge(g[key].state, { lastValue: payload });\n        }\n      }\n\n      tick();\n      try {\n        window.dispatchEvent(new CustomEvent(`mb:${key}:update`, {\n          detail: { event: evtType, state: g[key].state, raw: parsed }\n        }));\n      } catch {}\n    };\n\n    // Unnamed events (rare in your streams, but safe)\n    es.onmessage = (e) => handle(\"message\", e);\n\n    // Named events (your streams DO emit these)\n    const eventNames = [\n      \"hello\",\n      `${key}.state`,\n      `${key}.update`,\n      `${key}.patch`,\n      `${key}.delta`,\n      \"state\",\n      \"update\",\n      \"patch\",\n      \"delta\",\n    ];\n\n    for (const name of eventNames) {\n      try {\n        es.addEventListener(name, (e) => handle(name, e));\n      } catch {}\n    }\n\n    tick();\n  }\n\n  function findOpsAnchor() {\n    return (\n      document.getElementById(\"ops-pill\") ||\n      document.querySelector(\"[data-widget='ops-pill']\") ||\n      document.querySelector(\".ops-pill\") ||\n      document.querySelector(\"#ops\") ||\n      null\n    );\n  }\n\n  function findReflectionsAnchor() {\n    return (\n      document.getElementById(\"reflections-header\") ||\n      document.getElementById(\"reflections\") ||\n      document.querySelector(\"[data-panel='reflections']\") ||\n      document.querySelector(\".reflections\") ||\n      (() => {\n        const heads = Array.from(document.querySelectorAll(\"h1,h2,h3,h4,header,strong\"));\n        return heads.find(h => (h.textContent || \"\").toLowerCase().includes(\"reflections\")) || null;\n      })()\n    );\n  }\n\n  function boot() {\n    const opsInd = mount(findOpsAnchor(), \"ops-sse-indicator\", \"OPS SSE\");\n    const refInd = mount(findReflectionsAnchor(), \"reflections-sse-indicator\", \"Reflections SSE\");\n\n    connect(\"ops\", \"OPS SSE\", OPS_SSE_URL, opsInd);\n    connect(\"reflections\", \"Reflections SSE\", REFLECTIONS_SSE_URL, refInd);\n\n    // keep age ticking\n    setInterval(() => {\n      const g = ensureGlobal();\n      set(opsInd, \"OPS SSE\", g.ops.connected, g.ops.lastAt);\n      set(refInd, \"Reflections SSE\", g.reflections.connected, g.reflections.lastAt);\n    }, 1000);\n  }\n\n  document.readyState === \"loading\"\n    ? document.addEventListener(\"DOMContentLoaded\", boot, { once: true })\n    : boot();\n})();\n\n// PHASE16_CONNECT_GUARD_FIXED_1767139733\n", "// Phase 60 \u2014 Agent Pool compact console rows\n// Cosmetic renderer only.\n// Keeps the existing OPS SSE behavior, but restores the intended\n// dense operator-console row appearance with role emojis.\n\n(() => {\n  const container = document.getElementById(\"agent-status-container\");\n  if (!container) {\n    console.warn(\"agent-status-row.js: #agent-status-container not found.\");\n    return;\n  }\n\n  const title = container.querySelector(\"h2\");\n  container.innerHTML = \"\";\n  if (title) container.appendChild(title);\n\n  const AGENTS = [\"Matilda\", \"Atlas\", \"Cade\", \"Effie\"];\n\n  const AGENT_EMOJI = {\n    matilda: \"\uD83D\uDDE3\uFE0F\",\n    atlas: \"\uD83E\uDDED\",\n    cade: \"\uD83D\uDCBB\",\n    effie: \"\uD83D\uDCCA\",\n  };\n\n  const indicators = {};\n\n  const stack = document.createElement(\"div\");\n  stack.className = \"w-full flex flex-col gap-0.5\";\n  container.appendChild(stack);\n\n  AGENTS.forEach((name) => {\n    const key = name.toLowerCase();\n\n    const row = document.createElement(\"div\");\n    row.className =\n      \"w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    const left = document.createElement(\"div\");\n    left.className = \"flex items-center gap-3 min-w-0 h-[18px]\";\n\n    const emoji = document.createElement(\"span\");\n    emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    emoji.textContent = AGENT_EMOJI[key] || \"\u2022\";\n    emoji.style.width = \"18px\";\n    emoji.style.minWidth = \"18px\";\n    emoji.style.height = \"18px\";\n    emoji.style.minHeight = \"18px\";\n    emoji.style.fontSize = \"14px\";\n    emoji.style.lineHeight = \"1\";\n    emoji.style.background = \"transparent\";\n    emoji.style.borderRadius = \"0\";\n    emoji.style.boxShadow = \"none\";\n    emoji.style.marginRight = \"0\";\n\n    const label = document.createElement(\"span\");\n    label.className = \"text-[13px] font-semibold tracking-tight text-slate-100/95 truncate\";\n    label.textContent = name;\n\n    const status = document.createElement(\"span\");\n    status.className = \"text-[12px] font-medium text-slate-200/90 truncate\";\n    status.textContent = \"unknown\";\n\n    left.append(emoji, label);\n    row.append(left, status);\n    stack.appendChild(row);\n\n    indicators[key] = { row, emoji, label, status };\n  });\n\n  const OPS_SSE_URL = `/events/ops`;\n  const __DISABLE_OPTIONAL_SSE =\n    (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n\n  function classifyStatus(statusString) {\n    if (\n      typeof window !== \"undefined\" &&\n      window.__PHASE16_SSE_OWNER_STARTED\n    ) {\n      return \"unknown\";\n    }\n\n    const s = (statusString || \"\").toLowerCase();\n    if (!s) return \"unknown\";\n    if (s.includes(\"error\") || s.includes(\"failed\") || s.includes(\"offline\")) return \"error\";\n    if (s.includes(\"online\") || s.includes(\"ready\") || s.includes(\"ok\")) return \"online\";\n    if (s.includes(\"queued\") || s.includes(\"pending\") || s.includes(\"init\") || s.includes(\"running\")) return \"pending\";\n    return \"unknown\";\n  }\n\n  function applyVisual(agentKey, statusString) {\n    const indicator = indicators[agentKey];\n    if (!indicator) return;\n\n    const kind = classifyStatus(statusString);\n    const finalStatus = statusString || \"unknown\";\n    indicator.status.textContent = finalStatus;\n\n    indicator.row.className =\n      \"w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm\";\n\n    indicator.emoji.className = \"inline-flex items-center justify-center shrink-0\";\n    indicator.emoji.textContent = AGENT_EMOJI[agentKey] || \"\u2022\";\n    indicator.emoji.style.display = \"inline-flex\";\n    indicator.emoji.style.width = \"18px\";\n    indicator.emoji.style.minWidth = \"18px\";\n    indicator.emoji.style.height = \"18px\";\n    indicator.emoji.style.minHeight = \"18px\";\n    indicator.emoji.style.fontSize = \"14px\";\n    indicator.emoji.style.lineHeight = \"1\";\n    indicator.emoji.style.background = \"transparent\";\n    indicator.emoji.style.borderRadius = \"0\";\n    indicator.emoji.style.boxShadow = \"none\";\n    indicator.emoji.style.marginRight = \"0\";\n\n    indicator.label.className = \"text-[13px] font-semibold tracking-tight truncate\";\n    indicator.status.className = \"text-[11px] font-medium truncate\";\n\n    switch (kind) {\n      case \"online\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-emerald-300/90\");\n        break;\n      case \"error\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-rose-300/90\");\n        break;\n      case \"pending\":\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-amber-200/90\");\n        break;\n      case \"unknown\":\n      default:\n        indicator.row.classList.add(\"bg-gray-900\", \"border-gray-700\");\n        indicator.label.classList.add(\"text-slate-100\");\n        indicator.status.classList.add(\"text-slate-300/75\");\n        break;\n    }\n  }\n\n  Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[agent-status-row] Optional SSE disabled:\", OPS_SSE_URL);\n    return;\n  }\n\n  function parseJson(raw) {\n    try {\n      return JSON.parse(raw);\n    } catch {\n      return null;\n    }\n  }\n\n  function extractPayload(data) {\n    if (!data || typeof data !== \"object\") return null;\n    if (data.payload && typeof data.payload === \"object\") return data.payload;\n    if (data.data && typeof data.data === \"object\") return data.data;\n    if (data.state && typeof data.state === \"object\") return data.state;\n    return data;\n  }\n\n  function applyAgentMap(payload) {\n    if (!payload || typeof payload !== \"object\") return false;\n    const agents = payload.agents;\n    if (!agents || typeof agents !== \"object\") return false;\n\n    let applied = false;\n    for (const [name, value] of Object.entries(agents)) {\n      const key = String(name || \"\").toLowerCase();\n      if (!indicators[key]) continue;\n\n      let status = \"unknown\";\n      if (typeof value === \"string\") {\n        status = value;\n      } else if (value && typeof value === \"object\") {\n        status =\n          value.status ??\n          value.state ??\n          value.level ??\n          value.health ??\n          value.mode ??\n          \"unknown\";\n      }\n\n      applyVisual(key, String(status || \"unknown\"));\n      applied = true;\n    }\n    return applied;\n  }\n\n  function applySingleAgent(data) {\n    if (!data || typeof data !== \"object\") return false;\n\n    const agentName =\n      (data.agent || data.actor || data.source || data.worker || data.name || \"\").toString().toLowerCase();\n    if (!agentName || !indicators[agentName]) return false;\n\n    const status =\n      data.status ??\n      data.state ??\n      data.level ??\n      data.health ??\n      data.mode ??\n      \"unknown\";\n\n    applyVisual(agentName, String(status || \"unknown\"));\n    return true;\n  }\n\n  function handleOpsEvent(eventName, event) {\n    const data = parseJson(event.data);\n    if (!data) return;\n\n    const payload = extractPayload(data);\n\n    if (eventName === \"ops.state\") {\n      if (applyAgentMap(payload)) return;\n    }\n\n    if (applyAgentMap(data)) return;\n    if (applyAgentMap(payload)) return;\n    applySingleAgent(payload);\n    applySingleAgent(data);\n  }\n\n  let source;\n  try {\n    source = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(OPS_SSE_URL));\n  } catch (err) {\n    console.error(\"agent-status-row.js: Failed to open OPS SSE connection:\", err);\n    return;\n  }\n\n  if (!source) return;\n\n  source.onmessage = (event) => handleOpsEvent(\"message\", event);\n  source.addEventListener(\"hello\", (event) => handleOpsEvent(\"hello\", event));\n  source.addEventListener(\"ops.state\", (event) => handleOpsEvent(\"ops.state\", event));\n  source.addEventListener(\"state\", (event) => handleOpsEvent(\"state\", event));\n  source.addEventListener(\"update\", (event) => handleOpsEvent(\"update\", event));\n\n  source.onerror = (err) => {\n    console.warn(\"agent-status-row.js: OPS SSE error:\", err);\n    Object.keys(indicators).forEach((key) => applyVisual(key, \"unknown\"));\n  };\n})();\n", "// <0001fad7> Phase 5.1 \u2014 Matilda \u2192 Cade \u2192 Effie Broadcast Visualization\n// Wrapped in a guarded init() so we don't create duplicate intervals on reload.\n\nconst BROADCAST_GUARD_KEY = \"__broadcastVisualizationInited\";\n\nconst nodes = [\"Matilda\", \"Cade\", \"Effie\"];\n\nfunction renderBroadcastNodes() {\nconst container = document.getElementById(\"broadcast-visual\");\nif (!container) return;\n\nconst parts = [];\nfor (let i = 0; i < nodes.length; i++) {\nconst n = nodes[i];\nparts.push('<div class=\"node\" id=\"node-' + n + '\">' + n + \"</div>\");\nif (i < nodes.length - 1) {\nparts.push('<div class=\"arrow\">\u279C</div>');\n}\n}\ncontainer.innerHTML = parts.join(\"\");\n}\n\nfunction startBroadcastCycle() {\nlet idx = 0;\n\nsetInterval(() => {\nconst allNodes = document.querySelectorAll(\".node\");\nallNodes.forEach((n) => n.classList.remove(\"active\"));\n\nconst activeId = \"node-\" + nodes[idx];\nconst active = document.getElementById(activeId);\nif (active) active.classList.add(\"active\");\n\nidx = (idx + 1) % nodes.length;\n\n}, 1500);\n}\n\n/**\n\n* Initialize the broadcast visualization in a guarded way so it\n* does not re-register intervals or duplicate DOM nodes.\n  */\n  export function initBroadcastVisualization() {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") {\n  return;\n  }\n\nif (window[BROADCAST_GUARD_KEY]) {\nreturn;\n}\nwindow[BROADCAST_GUARD_KEY] = true;\n\nconst run = () => {\nrenderBroadcastNodes();\nstartBroadcastCycle();\n};\n\nif (document.readyState === \"loading\") {\nwindow.addEventListener(\"DOMContentLoaded\", run);\n} else {\nrun();\n}\n}\n\n// Optional: expose for manual debugging in the browser console\nif (typeof window !== \"undefined\") {\nwindow.initBroadcastVisualization = initBroadcastVisualization;\n}\n", "// public/js/ops-status-widget.js\n// Phase 11: minimal pill ID normalizer; all state comes from ops-pill-state.js\n(function () {\n  if (typeof document === \"undefined\") return;\n\n  var existing = document.getElementById(\"ops-dashboard-pill\");\n  if (existing) return;\n\n  var pill = document.querySelector(\"[data-ops-pill]\");\n  if (!pill) return;\n\n  pill.id = \"ops-dashboard-pill\";\n})();\n", "// Lightweight OPS SSE \u2192 global state bridge for Phase 11\n(() => {\n  // Phase16: bail if SSE owner already started\n  if (typeof window !== \"undefined\" && window.__PHASE16_SSE_OWNER_STARTED) return;\n\n  if (typeof window === \"undefined\" || typeof EventSource === \"undefined\") return;\n\n  // Avoid multiple initializations if bundle is loaded twice\n  if (window.__opsGlobalsBridgeInitialized) return;\n  window.__opsGlobalsBridgeInitialized = true;\n\n  // Initialize globals if they don't exist\n  if (typeof window.lastOpsHeartbeat === \"undefined\") {\n    window.lastOpsHeartbeat = null;\n  }\n  if (typeof window.lastOpsStatusSnapshot === \"undefined\") {\n    window.lastOpsStatusSnapshot = null;\n  }\n\n  const opsUrl = `/events/ops`;\n\n  \n\n  const __DISABLE_OPTIONAL_SSE = (typeof window !== \"undefined\" && window.__DISABLE_OPTIONAL_SSE) === true;\n  if (__DISABLE_OPTIONAL_SSE) {\n    console.warn(\"[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):\", opsUrl);\n    return;\n  }\nconst handleEvent = (event) => {\n    try {\n      const data = JSON.parse(event.data || \"null\");\n      if (!data) return;\n\n      window.lastOpsHeartbeat = Math.floor(Date.now() / 1000);\n      window.lastOpsStatusSnapshot = data;\n    } catch (err) {\n      console.warn(\"[ops-globals-bridge] Failed to parse OPS event:\", err);\n    }\n  \n  // Phase16: emit a unified CustomEvent for OPS pill + listeners\n  try {\n    window.dispatchEvent(new CustomEvent(\"mb:ops:update\", {\n      detail: { event: \"message\", state: window.lastOpsStatusSnapshot }\n    }));\n  } catch {}\n};\n\n  try {\n    const es = (window.__PHASE16_SSE_OWNER_STARTED ? null : new EventSource(opsUrl));\n\n    // Default unnamed \"message\" events (if any in future)\n    // Phase16: guard null EventSource before handlers\n\n    if (!es) return null;\n\n    es.onmessage = handleEvent;\n\n    // Named \"hello\" events from OPS SSE\n    es.addEventListener(\"hello\", handleEvent);\n\n    // Phase16: guard null EventSource before handlers (onerror)\n\n    if (!es) return;\n\n    es.onerror = (err) => {\n      console.warn(\"[ops-globals-bridge] EventSource error:\", err);\n    };\n  } catch (err) {\n    console.warn(\"[ops-globals-bridge] Failed to init EventSource:\", err);\n  }\n})();\n", "// public/js/ops-pill-state.js\n// Phase 11: simple dashboard OPS pill driven by lastOpsHeartbeat.\n// - Creates #ops-dashboard-pill on /dashboard if missing.\n// - Hides any external #ops-status-pill overlay.\n// - Shows \"OPS: Unknown\" / \"OPS: Online\" based on heartbeat.\n\n(function () {\n  if (typeof window === \"undefined\" || typeof document === \"undefined\") return;\n  if (window.location.pathname !== \"/dashboard\") return;\n\n  var POLL_INTERVAL_MS = 5000;\n  var PILL_ID = \"ops-dashboard-pill\";\n\n  function ensurePill() {\n    var pill = document.getElementById(PILL_ID);\n    if (pill) return pill;\n\n    // Create the dashboard pill near the top of the body\n    pill = document.createElement(\"span\");\n    pill.id = PILL_ID;\n    pill.className = \"ops-pill ops-pill-unknown\";\n    pill.textContent = \"OPS: Unknown\";\n\n    // Keep styling minimal; main styles come from CSS class\n    pill.style.display = \"inline-block\";\n\n    if (document.body.firstChild) {\n      document.body.insertBefore(pill, document.body.firstChild);\n    } else {\n      document.body.appendChild(pill);\n    }\n\n    return pill;\n  }\n\n  function applyState() {\n    // Always hide any external overlay pill if present\n    var overlay = document.getElementById(\"ops-status-pill\");\n    if (overlay) {\n      overlay.style.display = \"none\";\n    }\n\n    var pill = ensurePill();\n    if (!pill) return;\n\n    var hasHeartbeat = (typeof window.lastOpsHeartbeat === \"number\");\n\n    var label = hasHeartbeat ? \"OPS: Online\"  : \"OPS: Unknown\";\n    var cls   = hasHeartbeat ? \"ops-pill-online\" : \"ops-pill-unknown\";\n\n    pill.classList.remove(\n      \"ops-pill-unknown\",\n      \"ops-pill-online\",\n      \"ops-pill-stale\",\n      \"ops-pill-error\"\n    );\n    pill.classList.add(cls);\n    pill.textContent = label;\n  }\n\n  applyState();\n  setInterval(applyState, POLL_INTERVAL_MS);\n})();\n", "(function () {\n  function log(msg) {\n    console.log(\"[matilda-chat]\", msg);\n  }\n\n  function appendMessage(transcriptEl, sender, text) {\n    if (!transcriptEl) return;\n    var line = document.createElement(\"p\");\n    line.className = \"mb-1 text-sm\";\n    var label = sender ? sender + \": \" : \"\";\n    line.textContent = label + text;\n    transcriptEl.appendChild(line);\n    transcriptEl.scrollTop = transcriptEl.scrollHeight;\n  }\n\n  function setSendingState(sendBtn, input, isSending) {\n    if (sendBtn) {\n      sendBtn.disabled = isSending;\n      sendBtn.classList.toggle(\"opacity-60\", isSending);\n      sendBtn.textContent = isSending ? \"Sending...\" : \"Send\";\n    }\n    if (input) {\n      input.disabled = isSending;\n    }\n  }\n\n  async function wireChat() {\n    var root = document.getElementById(\"matilda-chat-root\");\n    if (!root) {\n      log(\"No #matilda-chat-root found; skipping wiring.\");\n      return;\n    }\n\n    var transcript = document.getElementById(\"matilda-chat-transcript\");\n    var input = document.getElementById(\"matilda-chat-input\");\n    var sendBtn = document.getElementById(\"matilda-chat-send\");\n\n      // Phase23: expose append API for other modules (task-events bridge, ops, etc.)\n      // Usage:\n      //   window.appendMessage({role:\"system\"|\"user\"|\"matilda\", content:\"...\"})\n      //   window.__appendMessage({role:\"system\", content:\"...\"})   (alias)\n      window.appendMessage = window.appendMessage || function (msg) {\n        try {\n          var role = (msg && msg.role) ? String(msg.role) : \"system\";\n          var content = (msg && (msg.content ?? msg.text ?? msg.message)) ? String(msg.content ?? msg.text ?? msg.message) : \"\";\n          var sender = role === \"user\" ? \"You\" : (role === \"matilda\" ? \"Matilda\" : \"System\");\n          appendMessage(transcript, sender, content);\n        } catch (_) {}\n      };\n      window.__appendMessage = window.__appendMessage || window.appendMessage;\n\n    if (!transcript || !input || !sendBtn) {\n      log(\"Missing one or more Matilda chat elements; aborting wiring.\");\n      return;\n    }\n\n    function safeTrim(value) {\n      return (value || \"\").toString().trim();\n    }\n\n    async function handleSend() {\n      var message = safeTrim(input.value);\n      if (!message) return;\n\n      appendMessage(transcript, \"You\", message);\n      input.value = \"\";\n      setSendingState(sendBtn, input, true);\n\n      try {\n        var res = await fetch(\"/api/chat\", {\n          method: \"POST\",\n          headers: { \"Content-Type\": \"application/json\" },\n          body: JSON.stringify({ message: message, agent: \"matilda\" }),\n        });\n\n        if (!res.ok) {\n          appendMessage(\n            transcript,\n            \"Matilda\",\n            \"(error talking to /api/chat)\"\n          );\n          return;\n        }\n\n        var data = await res.json();\n        var reply =\n          (data && (data.reply || data.message || data.response)) ||\n          \"(no reply)\";\n        appendMessage(transcript, \"Matilda\", reply);\n      } catch (err) {\n        console.error(err);\n        appendMessage(transcript, \"Matilda\", \"(network error)\");\n      } finally {\n        setSendingState(sendBtn, input, false);\n      }\n    }\n\n    sendBtn.addEventListener(\"click\", handleSend);\n\n    var quickBtn = document.getElementById(\"matilda-chat-quick-check\");\n    if (quickBtn) {\n      quickBtn.addEventListener(\"click\", function () {\n        input.value = \"Quick systems check from dashboard Phase 11.4.\";\n        handleSend();\n      });\n    }\n\n    input.addEventListener(\"keydown\", function (e) {\n      if (e.key === \"Enter\" && !e.shiftKey) {\n        e.preventDefault();\n        handleSend();\n      }\n    });\n\n    log(\"Matilda chat wiring complete.\");\n  }\n\n  document.addEventListener(\"DOMContentLoaded\", wireChat);\n})();\n", "(function () {\n  console.log(\"[dashboard-delegation] module loaded\");\n\n  function $(id) {\n    return document.getElementById(id);\n  }\n\n  function getSafeFetch() {\n    var f = window.fetch;\n    var t = typeof f;\n    console.log(\"[dashboard-delegation] detected fetch type:\", t);\n    if (t !== \"function\") {\n      console.error(\"[dashboard-delegation] fetch is not a function; value:\", f);\n      return null;\n    }\n    return f.bind(window);\n  }\n\n  function escapeHtml(value) {\n    return String(value == null ? \"\" : value)\n      .replace(/&/g, \"&amp;\")\n      .replace(/</g, \"&lt;\")\n      .replace(/>/g, \"&gt;\")\n      .replace(/\"/g, \"&quot;\")\n      .replace(/'/g, \"&#39;\");\n  }\n\n  function formatJsonBlock(obj) {\n    return \"<pre class=\\\"mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300\\\">\" +\n      escapeHtml(JSON.stringify(obj, null, 2)) +\n      \"</pre>\";\n  }\n\n  function setResponseState(kind, html) {\n    var response = $(\"delegation-response\");\n    var panel = $(\"delegation-status-panel\");\n    if (!response) return;\n\n    response.innerHTML = html;\n\n    if (!panel) return;\n    panel.classList.remove(\"border-gray-700\", \"border-teal-600\", \"border-green-600\", \"border-red-600\", \"border-amber-500\");\n\n    if (kind === \"sending\") panel.classList.add(\"border-teal-600\");\n    else if (kind === \"success\") panel.classList.add(\"border-green-600\");\n    else if (kind === \"error\") panel.classList.add(\"border-red-600\");\n    else if (kind === \"waiting\") panel.classList.add(\"border-amber-500\");\n    else panel.classList.add(\"border-gray-700\");\n  }\n\n  function setIdle() {\n    setResponseState(\n      \"idle\",\n      \"Awaiting operator input.<br>Results from delegation requests will appear here.\"\n    );\n  }\n\n  function setSending(text) {\n    setResponseState(\n      \"sending\",\n      \"<div class=\\\"text-teal-300 font-medium\\\">Sending delegation\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Preparing request for the orchestration layer.</div>\" +\n      (text ? \"<div class=\\\"mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words\\\">\" + escapeHtml(text) + \"</div>\" : \"\")\n    );\n  }\n\n  function setWaiting() {\n    setResponseState(\n      \"waiting\",\n      \"<div class=\\\"text-amber-300 font-medium\\\">Still waiting on delegation response\u2026</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">The request may still be processing.</div>\"\n    );\n  }\n\n  function setSuccess(data) {\n    var summary = \"Delegation accepted.\";\n    if (data && typeof data === \"object\") {\n      summary =\n        data.message ||\n        data.status ||\n        data.result ||\n        data.reply ||\n        data.ok && \"Delegation accepted.\" ||\n        summary;\n    }\n\n    setResponseState(\n      \"success\",\n      \"<div class=\\\"text-green-300 font-medium\\\">\" + escapeHtml(summary) + \"</div>\" +\n      \"<div class=\\\"mt-2 text-gray-400\\\">Request completed successfully.</div>\" +\n      (data && typeof data === \"object\" ? formatJsonBlock(data) : \"\")\n    );\n  }\n\n  function setError(message, extra) {\n    setResponseState(\n      \"error\",\n      \"<div class=\\\"text-red-300 font-medium\\\">Delegation failed.</div>\" +\n      \"<div class=\\\"mt-2 text-gray-300\\\">\" + escapeHtml(message || \"Unknown error\") + \"</div>\" +\n      (extra ? \"<div class=\\\"mt-3 text-xs text-gray-400 break-words\\\">\" + escapeHtml(extra) + \"</div>\" : \"\")\n    );\n  }\n\n  async function onDelegationClick() {\n    var input = $(\"delegation-input\");\n    var btn = $(\"delegation-submit\");\n    if (!input) {\n      console.warn(\"[dashboard-delegation] delegation input not found at click time\");\n      setError(\"Delegation input field was not found.\");\n      return;\n    }\n\n    var value = String(input.value || \"\").trim();\n    if (!value) {\n      console.warn(\"[dashboard-delegation] empty delegation input; skipping\");\n      setError(\"Please enter a delegation request before submitting.\");\n      return;\n    }\n\n    console.log(\"[dashboard-delegation] sending delegation:\", value);\n    var safeFetch = getSafeFetch();\n    if (!safeFetch) {\n      console.error(\"[dashboard-delegation] aborting delegation because fetch is unavailable or invalid\");\n      setError(\"Browser fetch is unavailable.\");\n      return;\n    }\n\n    var oldText = btn ? (btn.textContent || \"Submit Delegation\") : \"Submit Delegation\";\n    var waitingTimer = null;\n\n    try {\n      if (btn) {\n        btn.disabled = true;\n        btn.textContent = \"Sending...\";\n        btn.classList.add(\"opacity-70\", \"cursor-not-allowed\");\n      }\n\n      setSending(value);\n      waitingTimer = window.setTimeout(setWaiting, 4000);\n\n      var res;\n      try {\n        res = await safeFetch(\"/api/delegate-task\", {\n          method: \"POST\",\n          headers: {\n            \"Content-Type\": \"application/json\",\n            \"Accept\": \"application/json\"\n          },\n          body: JSON.stringify({ prompt: value, message: value, text: value, task: value })\n        });\n      } catch (err) {\n        console.error(\"[dashboard-delegation] fetch threw before response:\", err);\n        throw err;\n      }\n\n      console.log(\"[dashboard-delegation] fetch returned:\", {\n        ok: res && res.ok,\n        status: res && res.status,\n        statusText: res && res.statusText\n      });\n\n      var data = null;\n      var rawText = \"\";\n      try {\n        rawText = await res.text();\n        data = rawText ? JSON.parse(rawText) : {};\n      } catch (err) {\n        console.error(\"[dashboard-delegation] error parsing JSON response:\", err);\n        data = { error: \"Non-JSON response from /api/delegate-task\", raw: rawText || \"\" };\n      }\n\n      console.log(\"[dashboard-delegation] delegation response:\", data);\n\n      if (!res.ok) {\n        setError(\n          (data && (data.error || data.message || data.statusText)) || (\"HTTP \" + res.status + \" \" + (res.statusText || \"\")),\n          rawText\n        );\n        return;\n      }\n\n      setSuccess(data);\n    } catch (err) {\n      setError(err && err.message ? err.message : String(err));\n    } finally {\n      if (waitingTimer) window.clearTimeout(waitingTimer);\n      if (btn) {\n        btn.disabled = false;\n        btn.textContent = oldText;\n        btn.classList.remove(\"opacity-70\", \"cursor-not-allowed\");\n      }\n    }\n  }\n\n  function init() {\n    var btn = $(\"delegation-submit\");\n    var input = $(\"delegation-input\");\n    if (!btn || !input) {\n      console.warn(\"[dashboard-delegation] delegation button or input not found in init\");\n      return;\n    }\n\n    setIdle();\n\n    if (btn.dataset.delegationWired === \"true\") {\n      console.log(\"[dashboard-delegation] Task Delegation wiring already active\");\n      return;\n    }\n\n    btn.dataset.delegationWired = \"true\";\n    btn.addEventListener(\"click\", onDelegationClick);\n\n    input.addEventListener(\"keydown\", function (e) {\n      if ((e.metaKey || e.ctrlKey) && e.key === \"Enter\") {\n        e.preventDefault();\n        onDelegationClick();\n      }\n    });\n\n    console.log(\"[dashboard-delegation] Task Delegation wiring active\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", init, { once: true });\n  } else {\n    init();\n  }\n})();\n", "(() => {\n  const SSE_URL = \"/events/task-events\";\n\n  const PANEL_ID = \"mb-task-events-panel\";\n  const FEED_ID = \"mb-task-events-feed\";\n  const COUNTS_ID = \"mb-task-events-counts\";\n  const ANCHOR_ID = \"mb-task-events-panel-anchor\";\n\n  function mountRoot() {\n    const anchor = document.getElementById(ANCHOR_ID);\n    if (anchor) return anchor;\n    return document.body;\n  }\n\n  function ensurePanel() {\n    if (document.getElementById(PANEL_ID)) return;\n\n    const root = mountRoot();\n\n    const panel = document.createElement(\"div\");\n    panel.id = PANEL_ID;\n\n    const anchored = root && root.id === ANCHOR_ID;\n\n    panel.style.width = anchored ? \"100%\" : \"360px\";\n    panel.style.maxWidth = anchored ? \"100%\" : \"calc(100vw - 24px)\";\n    panel.style.maxHeight = anchored ? \"260px\" : \"40vh\";\n    panel.style.overflow = \"hidden\";\n    panel.style.zIndex = \"9999\";\n    panel.style.border = \"1px solid rgba(255,255,255,0.12)\";\n    panel.style.borderRadius = \"14px\";\n    panel.style.background = \"rgba(10,10,14,0.92)\";\n    panel.style.backdropFilter = \"blur(10px)\";\n    panel.style.boxShadow = \"0 10px 30px rgba(0,0,0,0.35)\";\n    panel.style.color = \"rgba(255,255,255,0.92)\";\n    panel.style.fontFamily =\n      \"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial\";\n\n    if (!anchored) {\n      panel.style.position = \"fixed\";\n      panel.style.right = \"12px\";\n      panel.style.bottom = \"12px\";\n    } else {\n      panel.style.marginTop = \"12px\";\n    }\n\n    const header = document.createElement(\"div\");\n    header.style.display = \"flex\";\n    header.style.alignItems = \"center\";\n    header.style.justifyContent = \"space-between\";\n    header.style.gap = \"8px\";\n    header.style.padding = \"10px 12px\";\n    header.style.borderBottom = \"1px solid rgba(255,255,255,0.10)\";\n\n    const title = document.createElement(\"div\");\n    title.textContent = \"Task Events\";\n    title.style.fontSize = \"12px\";\n    title.style.letterSpacing = \"0.06em\";\n    title.style.opacity = \"0.9\";\n\n    const right = document.createElement(\"div\");\n    right.style.display = \"flex\";\n    right.style.alignItems = \"center\";\n    right.style.gap = \"10px\";\n\n    const counts = document.createElement(\"div\");\n    counts.id = COUNTS_ID;\n    counts.textContent = \"created:0  completed:0  failed:0\";\n    counts.style.fontSize = \"11px\";\n    counts.style.opacity = \"0.85\";\n    counts.style.fontVariantNumeric = \"tabular-nums\";\n\n    const dot = document.createElement(\"span\");\n    dot.setAttribute(\"aria-label\", \"task-events connection\");\n    dot.title = \"task-events connection\";\n    dot.style.display = \"inline-block\";\n    dot.style.width = \"10px\";\n    dot.style.height = \"10px\";\n    dot.style.borderRadius = \"999px\";\n    dot.style.background = \"rgba(255,255,255,0.25)\";\n    dot.style.boxShadow = \"0 0 0 2px rgba(255,255,255,0.08) inset\";\n\n    const btn = document.createElement(\"button\");\n    btn.type = \"button\";\n    btn.textContent = \"\u00D7\";\n    btn.title = \"hide\";\n    btn.style.cursor = \"pointer\";\n    btn.style.border = \"1px solid rgba(255,255,255,0.14)\";\n    btn.style.background = \"transparent\";\n    btn.style.color = \"rgba(255,255,255,0.85)\";\n    btn.style.borderRadius = \"10px\";\n    btn.style.width = \"28px\";\n    btn.style.height = \"24px\";\n    btn.style.lineHeight = \"22px\";\n    btn.style.fontSize = \"14px\";\n    btn.onclick = () => panel.remove();\n\n    right.appendChild(dot);\n    right.appendChild(counts);\n    right.appendChild(btn);\n\n    header.appendChild(title);\n    header.appendChild(right);\n\n    const feed = document.createElement(\"div\");\n    feed.id = FEED_ID;\n    feed.style.padding = \"10px 12px\";\n    feed.style.overflow = \"auto\";\n    feed.style.maxHeight = anchored ? \"200px\" : \"calc(40vh - 46px)\";\n\n    panel.appendChild(header);\n    panel.appendChild(feed);\n\n    root.appendChild(panel);\n\n    window.__MB_TASK_EVENTS_PANEL = { dot, feed, counts };\n    console.log(\"[phase22] task-events panel mounted (anchored=%s)\", anchored);\n  }\n\n  function setDot(state) {\n    ensurePanel();\n    const dot = window.__MB_TASK_EVENTS_PANEL?.dot;\n    if (!dot) return;\n    if (state === \"open\") dot.style.background = \"rgba(80,200,120,0.85)\";\n    else if (state === \"error\") dot.style.background = \"rgba(240,90,90,0.85)\";\n    else dot.style.background = \"rgba(255,255,255,0.25)\";\n  }\n\n  const seen = new Set();\n  const tally = { created: 0, completed: 0, failed: 0 };\n\n  function bumpCounts(kind) {\n    if (kind === \"task.created\") tally.created += 1;\n    if (kind === \"task.completed\") tally.completed += 1;\n    if (kind === \"task.failed\") tally.failed += 1;\n\n    const el = document.getElementById(COUNTS_ID);\n    if (el) el.textContent = `created:${tally.created}  completed:${tally.completed}  failed:${tally.failed}`;\n  }\n\n  function isoText(ts) {\n    if (typeof ts === \"number\") return new Date(ts).toISOString();\n    if (typeof ts === \"string\" && ts.trim()) {\n      const d = new Date(ts);\n      if (!Number.isNaN(d.getTime())) return d.toISOString();\n      return ts.trim();\n    }\n    return new Date().toISOString();\n  }\n\n  function classifyKind(kind, message, status) {\n    const text = `${kind || \"\"} ${message || \"\"} ${status || \"\"}`.toLowerCase();\n    if (/fail|error|cancel|timeout/.test(text)) return \"terminal-error\";\n    if (/complete|done|success/.test(text)) return \"terminal-success\";\n    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return \"waiting\";\n    if (/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(text)) return \"active\";\n    return \"neutral\";\n  }\n\n  function accentColor(tone) {\n    if (tone === \"terminal-error\") return \"rgba(240,90,90,0.95)\";\n    if (tone === \"terminal-success\") return \"rgba(80,200,120,0.95)\";\n    if (tone === \"waiting\") return \"rgba(250,204,21,0.95)\";\n    if (tone === \"active\") return \"rgba(96,165,250,0.95)\";\n    return \"rgba(255,255,255,0.18)\";\n  }\n\n  function buildEventRecord(ev, fallbackKind) {\n    const ts = isoText(ev.ts);\n    const kind = String(ev.kind ?? fallbackKind ?? \"event\");\n    const taskId = ev.task_id ?? ev.taskId ?? \"unknown\";\n    const runId = ev.run_id ?? ev.runId ?? \"\";\n    const actor = ev.actor ?? ev.meta?.actor ?? ev.meta?.owner ?? \"\";\n    const status = ev.status ?? ev.meta?.status ?? \"\";\n    const cursor =\n      typeof ev.cursor === \"number\" || typeof ev.cursor === \"string\" ? String(ev.cursor) : \"\";\n    const message = String(ev.msg ?? ev.message ?? \"\").trim();\n\n    const detailParts = [];\n    detailParts.push(`task=${taskId}`);\n    if (runId) detailParts.push(`run=${runId}`);\n    if (actor) detailParts.push(`actor=${actor}`);\n    if (status) detailParts.push(`status=${status}`);\n    if (cursor) detailParts.push(`cursor=${cursor}`);\n    if (message) detailParts.push(message);\n\n    const detail = detailParts.join(\" \u2022 \");\n    const tone = classifyKind(kind, message, status);\n\n    return { ts, kind, detail, tone };\n  }\n\n  function appendEvent(ev, fallbackKind) {\n    ensurePanel();\n    const feed = document.getElementById(FEED_ID);\n    if (!feed) return;\n\n    const record = buildEventRecord(ev, fallbackKind);\n\n    const row = document.createElement(\"div\");\n    row.className = `phase61-task-event phase61-task-event-${record.tone}`;\n    row.dataset.eventKind = record.kind;\n    row.dataset.eventTone = record.tone;\n    row.style.position = \"relative\";\n    row.style.display = \"grid\";\n    row.style.gridTemplateColumns = \"minmax(112px, 132px) minmax(150px, 170px) 1fr\";\n    row.style.gap = \"10px\";\n    row.style.alignItems = \"start\";\n    row.style.padding = \"10px 12px 10px 14px\";\n    row.style.marginBottom = \"8px\";\n    row.style.border = \"1px solid rgba(255,255,255,0.10)\";\n    row.style.borderRadius = \"12px\";\n    row.style.background = \"rgba(255,255,255,0.03)\";\n    row.style.lineHeight = \"1.35\";\n    row.style.fontSize = \"12px\";\n\n    const accent = document.createElement(\"span\");\n    accent.setAttribute(\"aria-hidden\", \"true\");\n    accent.style.position = \"absolute\";\n    accent.style.left = \"0\";\n    accent.style.top = \"10px\";\n    accent.style.bottom = \"10px\";\n    accent.style.width = \"3px\";\n    accent.style.borderRadius = \"999px\";\n    accent.style.background = accentColor(record.tone);\n\n    const time = document.createElement(\"div\");\n    time.textContent = record.ts;\n    time.style.color = \"rgba(255,255,255,0.68)\";\n    time.style.fontVariantNumeric = \"tabular-nums\";\n    time.style.whiteSpace = \"nowrap\";\n\n    const kind = document.createElement(\"div\");\n    kind.textContent = record.kind;\n    kind.style.color = \"rgba(255,255,255,0.92)\";\n    kind.style.fontWeight = \"600\";\n    kind.style.letterSpacing = \"0.01em\";\n    kind.style.wordBreak = \"break-word\";\n\n    const detail = document.createElement(\"div\");\n    detail.textContent = record.detail || \"\u2014\";\n    detail.style.color = \"rgba(255,255,255,0.78)\";\n    detail.style.wordBreak = \"break-word\";\n\n    row.appendChild(accent);\n    row.appendChild(time);\n    row.appendChild(kind);\n    row.appendChild(detail);\n\n    feed.prepend(row);\n\n    const children = Array.from(feed.children);\n    if (children.length > 60) {\n      for (let i = 60; i < children.length; i++) children[i].remove();\n    }\n  }\n\n  function dispatchWindowEvent(ev) {\n    try { window.dispatchEvent(new CustomEvent(\"mb.task.event\", { detail: ev })); } catch {}\n  }\n\n  function parseMaybeJSON(raw) {\n    try { return JSON.parse(raw); } catch { return null; }\n  }\n\n  function handleFrame(eventName, rawData) {\n    const data = typeof rawData === \"string\" ? parseMaybeJSON(rawData) : rawData;\n    const ev = (data && typeof data === \"object\") ? data : { kind: eventName, raw: rawData };\n\n    if (eventName === \"task.event\") {\n      if (ev.actor == null && ev.meta && typeof ev.meta === \"object\") ev.actor = (ev.meta.actor ?? ev.meta.owner ?? null);\n\n      if (!ev.kind && ev.type) ev.kind = ev.type;\n      if (ev.kind === \"task.event\" && ev.type) ev.kind = ev.type;\n      if (ev.task_id == null && ev.taskId != null) ev.task_id = ev.taskId;\n      if (ev.run_id == null && ev.runId != null) ev.run_id = ev.runId;\n      if (ev && ev.meta && typeof ev.meta === \"object\") {\n        if (ev.task_id == null && ev.meta.task_id != null) ev.task_id = ev.meta.task_id;\n        if (ev.run_id  == null && ev.meta.run_id  != null) ev.run_id  = ev.meta.run_id;\n        if (ev.actor   == null && ev.meta.actor   != null) ev.actor   = ev.meta.actor;\n        if (ev.actor   == null && ev.meta.owner   != null) ev.actor   = ev.meta.owner;\n        if (ev.status  == null && ev.meta.status  != null) ev.status  = ev.meta.status;\n      }\n    }\n\n    if (!ev.kind) ev.kind = eventName;\n\n    const key = `${eventName}|${ev.kind}|${ev.ts ?? \"\"}|${ev.task_id ?? \"\"}|${ev.run_id ?? \"\"}|${ev.cursor ?? \"\"}`;\n    if (seen.has(key)) return;\n    seen.add(key);\n\n    if (ev.kind === \"task.created\" || ev.kind === \"task.completed\" || ev.kind === \"task.failed\") {\n      bumpCounts(String(ev.kind));\n    }\n\n    appendEvent(ev, eventName);\n    if (window.__UI_DEBUG) try { console.log(\"[task-events] mb.task.event\", ev); } catch {}\n    dispatchWindowEvent(ev);\n  }\n\n  let es = null;\n  let attempt = 0;\n\n  function connect() {\n    ensurePanel();\n\n    if (es) {\n      try { es.close(); } catch {}\n      es = null;\n    }\n\n    const url = SSE_URL;\n    es = new EventSource(url);\n\n    es.onopen = () => {\n      attempt = 0;\n      setDot(\"open\");\n      appendEvent({ ts: Date.now(), kind: \"sse.open\", message: `url=${url}` }, \"sse.open\");\n      console.log(\"[phase22] task-events SSE open\", url);\n    };\n\n    es.onerror = () => {\n      setDot(\"error\");\n      try { es.close(); } catch {}\n      es = null;\n\n      attempt += 1;\n      const delay = Math.min(15000, 500 * Math.pow(2, Math.min(6, attempt)));\n      appendEvent({ ts: Date.now(), kind: \"sse.error\", message: `reconnect_in=${delay}ms` }, \"sse.error\");\n      console.log(\"[phase22] task-events SSE error; reconnect in\", delay);\n      setTimeout(connect, delay);\n    };\n\n    es.onmessage = (msg) => handleFrame(\"message\", msg.data);\n\n    const names = [\n      \"hello\",\n      \"heartbeat\",\n      \"task.event\",\n      \"task.created\",\n      \"task.completed\",\n      \"task.failed\",\n      \"task.updated\",\n      \"task.status\",\n    ];\n    for (const name of names) {\n      es.addEventListener(name, (e) => handleFrame(name, e.data));\n    }\n  }\n\n  function boot() {\n    connect();\n    setInterval(() => {\n      if (!document.getElementById(PANEL_ID)) {\n        try { connect(); } catch {}\n      }\n    }, 2000);\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", boot, { once: true });\n  } else {\n    boot();\n  }\n\n  window.__MB_TASK_EVENTS = { url: SSE_URL, reconnect: () => connect() };\n})();\n", "(() => {\n  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)\n\n  const TASK_EVENT_NAME = \"mb.task.event\";\n  const tasks = new Map();\n\n  const STATUS_CLASS = {\n    queued: \"task-status-queued\",\n    done: \"task-status-done\",\n    failed: \"task-status-failed\",\n  };\n\n  function normStatus(s) {\n    const v = String(s ?? \"\").toLowerCase();\n    if (v === \"queued\" || v === \"pending\") return \"queued\";\n    if (v === \"done\" || v === \"complete\" || v === \"completed\") return \"done\";\n    if (v === \"failed\" || v === \"error\") return \"failed\";\n    return v || \"unknown\";\n  }\n\n  function pluckId(ev) {\n    return ev?.task_id ?? ev?.taskId ?? ev?.task?.id ?? null;\n  }\n\n  function pluckTask(ev) {\n    const t = ev?.task && typeof ev.task === \"object\" ? ev.task : null;\n    const id = pluckId(ev);\n    const status =\n      ev?.status ??\n      ev?.payload?.status ??\n      t?.status ??\n      (ev?.kind === \"task.created\" ? \"queued\" : null);\n\n    return {\n      id: id != null ? String(id) : null,\n      status: status != null ? normStatus(status) : null,\n      title: t?.title ?? ev?.title ?? null,\n      agent: t?.agent ?? ev?.agent ?? null,\n      error: ev?.error ?? ev?.payload?.error ?? t?.error ?? null,\n      updated_at: t?.updated_at ?? ev?.ts ?? Date.now(),\n    };\n  }\n\n  function setStatusOnNode(node, status) {\n    if (!node) return;\n    const s = normStatus(status);\n    node.setAttribute(\"data-task-status\", s);\n    node.classList?.remove(...Object.values(STATUS_CLASS));\n    if (STATUS_CLASS[s]) node.classList?.add(STATUS_CLASS[s]);\n\n    const sub =\n      node.querySelector?.(\"[data-task-field='status']\") ||\n      node.querySelector?.(\".task-status\") ||\n      node.querySelector?.(\".status\") ||\n      null;\n    if (sub) sub.textContent = s;\n  }\n\n  function updateTaskRowUI(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const nodes = [\n      document.getElementById(`task-${id}`),\n      document.getElementById(`task_${id}`),\n      document.querySelector?.(`[data-task-id=\"${CSS.escape(id)}\"]`),\n      document.querySelector?.(`[data-taskid=\"${CSS.escape(id)}\"]`),\n    ].filter(Boolean);\n\n    for (const n of nodes) setStatusOnNode(n, task.status);\n  }\n\n  function updateCountersUI() {\n    let queued = 0, done = 0, failed = 0;\n    for (const t of tasks.values()) {\n      const s = normStatus(t.status);\n      if (s === \"queued\") queued++;\n      else if (s === \"done\") done++;\n      else if (s === \"failed\") failed++;\n    }\n\n    const map = [\n      [\"queued\", queued],\n      [\"done\", done],\n      [\"failed\", failed],\n    ];\n\n    for (const [k, v] of map) {\n      const el =\n        document.getElementById(`task-count-${k}`) ||\n        document.getElementById(`tasks-${k}-count`) ||\n        document.querySelector?.(`[data-task-count=\"${k}\"]`) ||\n        null;\n      if (el) el.textContent = String(v);\n    }\n  }\n\n  function ingestTask(task) {\n    if (!task?.id) return;\n    const id = String(task.id);\n    const prev = tasks.get(id) || {};\n    const next = { ...prev, ...task, id, status: task.status ?? prev.status };\n    tasks.set(id, next);\n    updateTaskRowUI(next);\n    updateCountersUI();\n  }\n\n  function onTaskEvent(ev) {\n    const t = pluckTask(ev);\n    if (!t.id && ev?.kind) {\n      if (ev.kind === \"task.completed\") t.status = \"done\";\n      if (ev.kind === \"task.failed\") t.status = \"failed\";\n    }\n    if (t.id) ingestTask(t);\n  }\n\n  function attach() {\n    if (window.__PHASE22_TASK_UI_BOUND) return;\n    window.__PHASE22_TASK_UI_BOUND = true;\n\n    window.addEventListener(TASK_EVENT_NAME, (e) => {\n      try { if (window.__UI_DEBUG || window.__PHASE22_DEBUG) if (window.__UI_DEBUG || window.__PHASE22_DEBUG) console.log(\"[phase22] mb.task.event\", e.detail); onTaskEvent(e.detail); } catch {}\n    });\n\n    window.__PHASE22_TASK_UI = { tasks }; console.log(\"[phase22] bindings attached\");\n  }\n\n  if (document.readyState === \"loading\") {\n    document.addEventListener(\"DOMContentLoaded\", attach, { once: true });\n  } else {\n    attach();\n  }\n})();\n", "import \"./sse-heartbeat-shim.js\";\nimport \"./heartbeat-stale-indicator.js\";\n// Phase 11 \u2013 Unified dashboard bundle entrypoint\n\n// Core dashboard status + tiles\nimport \"./dashboard-status.js\";\n\n// Phase 15/16 boundary: disable optional SSE (OPS + Reflections) until backends exist\nif (typeof window !== \"undefined\" && typeof window.__DISABLE_OPTIONAL_SSE === \"undefined\") {\n  window.__DISABLE_OPTIONAL_SSE = false;\n}\n\nimport \"./agent-status-row.js\";\n\n// OPS / PM2 status + SSE wiring\nimport \"./dashboard-broadcast.js\";\nimport \"./ops-status-widget.js\";\nimport \"./ops-globals-bridge.js\";\nimport \"./ops-pill-state.js\";\n// TEMP phase57c isolate /api/tasks loop\n// import \"./dashboard-tasks-widget.js\";\n\n// Matilda chat console wiring\nimport \"./matilda-chat-console.js\";\nimport \"./dashboard-delegation.js\";\n\n// TEMP: dashboard graph disabled until canvas is present on all pages\n// import \"./dashboard-graph.js\";\n\n\n// Phase 22: task-events live UI\nimport \"./task-events-sse-client.js\";\nimport \"./phase22_task_delegation_live_bindings.js\";\n"],
./public/js/telemetry_queue_depth_reducer.js:11:    return state === "task.created" || state === "task.queued";
./public/js/ops-globals-bridge.js:26:    console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):", opsUrl);
./public/js/agent-status-row.js:384:    'queued',
./public/js/task-events-sse-client.js:155:    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return "waiting";
./public/js/telemetry/latency_metric.js:10:    "task.queued",
./public/js/telemetry/latency_metric.js:18:    "queued",
./public/js/phase61_recent_history_wire.js:36:    if (/queue|pending|wait|retry|hold/.test(s)) return "rgba(250,204,21,0.95)";
./public/js/phase22_task_delegation_live_bindings.js:12:    queued: "task-status-queued",
./public/js/phase22_task_delegation_live_bindings.js:19:    if (v === "queued" || v === "pending") return "queued";
./public/js/phase22_task_delegation_live_bindings.js:84:      v === "queued" ||
./public/js/phase22_task_delegation_live_bindings.js:85:      v === "pending" ||
./public/js/phase22_task_delegation_live_bindings.js:175:      (ev?.kind === "task.created" ? "queued" : null);
./public/js/phase22_task_delegation_live_bindings.js:226:    let queued = 0;
./public/js/phase22_task_delegation_live_bindings.js:232:      if (s === "queued") queued++;
./public/js/phase22_task_delegation_live_bindings.js:237:    updateCounterNode("queued", queued);
./public/bundle.js:20:    `,document.head.appendChild(a)}function v(a,d,x){if(w(),!a){let t=document.getElementById("phase16-sse-tray");t||(t=c("div",{id:"phase16-sse-tray",style:["position:fixed","left:10px","bottom:10px","display:flex","flex-direction:column","gap:6px","z-index:9999","pointer-events:none"].join(";")}),document.body.appendChild(t)),a=t}let f=document.getElementById(d);if(f)return f;f=c("span",{id:d,class:"sse-indicator","data-connected":"false"}),f.append(c("span",{class:"dot","aria-hidden":"true"}),c("span",{class:"meta"},`${x}: disconnected \xB7 last: \u2014`));try{if(a.matches&&a.matches("header,h1,h2,h3,h4,strong")){let t=c("span",{style:"margin-left:8px"});t.appendChild(f),a.appendChild(t)}else{let t=c("div",{style:"margin-top:4px"});t.appendChild(f),a.appendChild(t)}}catch{a.appendChild(f)}return f}function k(a,d,x,f){if(!a)return;a.dataset.connected=x?"true":"false";let t=a.querySelector(".meta");t&&(t.textContent=`${d}: ${x?"connected":"disconnected"} \xB7 last: ${f?i(b()-f):"\u2014"}`)}function l(){return window.__MB_STREAMS||(window.__MB_STREAMS={ops:{connected:!1,lastAt:0,state:{},es:null},reflections:{connected:!1,lastAt:0,state:{},es:null}}),window.__MB_STREAMS}function I(a,d){return(!a||typeof a!="object")&&(a={}),!d||typeof d!="object"?a:Object.assign(a,d)}function y(a,d){(!a||typeof a!="object")&&(a={});let x=d&&typeof d.path=="string"?d.path:"";if(!x)return a;let f=x.split(".").filter(Boolean);if(!f.length)return a;let t=a;for(let n=0;n<f.length-1;n++){let o=f[n];(!t[o]||typeof t[o]!="object")&&(t[o]={}),t=t[o]}return t[f[f.length-1]]=d.value,a}function s(a,d){return!!(typeof a=="string"&&a.endsWith(".state")||d&&typeof d=="object"&&(d.state&&typeof d.state=="object"||typeof d.type=="string"&&d.type.includes("state")||typeof d.event=="string"&&d.event.includes("state")))}function r(a){return!a||typeof a!="object"?a:a.payload!==void 0?a.payload:a.data!==void 0?a.data:a.delta!==void 0?a.delta:a.patch!==void 0?a.patch:a.state!==void 0?a.state:a}function h(a,d,x,f){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return null;let t=l();try{t[a].es&&t[a].es.close()}catch{}t[a].es=null;let n=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(x);if(t[a].es=n,!n)return null;let o=()=>k(f,d,t[a].connected,t[a].lastAt);n.onopen=()=>{t[a].connected=!0,o()},n.onerror=()=>{t[a].connected=!1,o()};let g=(m,_)=>{t[a].lastAt=b();let C=S(_&&_.data?_.data:""),L=r(C);s(m,C)?t[a].state=L&&typeof L=="object"?L:{value:L}:L&&typeof L=="object"?typeof L.path=="string"&&"value"in L?t[a].state=y(t[a].state,L):t[a].state=I(t[a].state,L):L!=null&&(t[a].state=I(t[a].state,{lastValue:L})),o();try{window.dispatchEvent(new CustomEvent(`mb:${a}:update`,{detail:{event:m,state:t[a].state,raw:C}}))}catch{}};n.onmessage=m=>g("message",m);let e=["hello",`${a}.state`,`${a}.update`,`${a}.patch`,`${a}.delta`,"state","update","patch","delta"];for(let m of e)try{n.addEventListener(m,_=>g(m,_))}catch{}o()}function E(){return document.getElementById("ops-pill")||document.querySelector("[data-widget='ops-pill']")||document.querySelector(".ops-pill")||document.querySelector("#ops")||null}function T(){return document.getElementById("reflections-header")||document.getElementById("reflections")||document.querySelector("[data-panel='reflections']")||document.querySelector(".reflections")||Array.from(document.querySelectorAll("h1,h2,h3,h4,header,strong")).find(d=>(d.textContent||"").toLowerCase().includes("reflections"))||null}function A(){let a=v(E(),"ops-sse-indicator","OPS SSE"),d=v(T(),"reflections-sse-indicator","Reflections SSE");h("ops","OPS SSE",p,a),h("reflections","Reflections SSE",u,d),setInterval(()=>{let x=l();k(a,"OPS SSE",x.ops.connected,x.ops.lastAt),k(d,"Reflections SSE",x.reflections.connected,x.reflections.lastAt)},1e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",A,{once:!0}):A()})();(()=>{let p=document.getElementById("agent-status-container");if(!p){console.warn("agent-status-row.js: #agent-status-container not found.");return}let u=p.querySelector("h2");p.innerHTML="",u&&p.appendChild(u);let b=["Matilda","Atlas","Cade","Effie"],S={matilda:"\u{1F5E3}\uFE0F",atlas:"\u{1F9ED}",cade:"\u{1F4BB}",effie:"\u{1F4CA}"},i={},c=Object.create(null),w=60*1e3,v=document.getElementById("metric-agents");function k(t,n){t&&(t.textContent=n)}function l(t){if(typeof t=="number"&&Number.isFinite(t))return t;if(typeof t=="string"&&t.trim()){let n=Number(t);if(Number.isFinite(n))return n;let o=Date.parse(t);if(Number.isFinite(o))return o}return null}function I(){if(!v)return;let t=Date.now(),n=0;for(let o of b){let g=o.toLowerCase(),e=l(c[g]);e!=null&&t-e<=w&&(n+=1)}k(v,String(n))}k(v,"\u2014");let y=document.createElement("div");y.className="w-full flex flex-col gap-0.5",p.appendChild(y),b.forEach(t=>{let n=t.toLowerCase(),o=document.createElement("div");o.className="w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm";let g=document.createElement("div");g.className="flex items-center gap-3 min-w-0 h-[18px]";let e=document.createElement("span");e.className="inline-flex items-center justify-center shrink-0",e.textContent=S[n]||"\u2022",e.style.width="18px",e.style.minWidth="18px",e.style.height="18px",e.style.minHeight="18px",e.style.fontSize="14px",e.style.lineHeight="1",e.style.background="transparent",e.style.borderRadius="0",e.style.boxShadow="none",e.style.marginRight="0";let m=document.createElement("span");m.className="text-[13px] font-semibold tracking-tight text-slate-100/95 truncate",m.textContent=t;let _=document.createElement("span");_.className="text-[12px] font-medium text-slate-200/90 truncate",_.textContent="unknown",g.append(e,m),o.append(g,_),y.appendChild(o),i[n]={row:o,emoji:e,label:m,status:_}});let s="/events/ops",r=(typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0;function h(t){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return"unknown";let n=(t||"").toLowerCase();return n?n.includes("error")||n.includes("failed")||n.includes("offline")?"error":n.includes("online")||n.includes("ready")||n.includes("ok")?"online":n.includes("queued")||n.includes("pending")||n.includes("init")||n.includes("running")?"pending":"unknown":"unknown"}function E(t,n){let o=i[t];if(!o)return;let g=h(n),e=n||"unknown";switch(o.status.textContent=e,o.row.className="w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm",o.emoji.className="inline-flex items-center justify-center shrink-0",o.emoji.textContent=S[t]||"\u2022",o.emoji.style.display="inline-flex",o.emoji.style.width="18px",o.emoji.style.minWidth="18px",o.emoji.style.height="18px",o.emoji.style.minHeight="18px",o.emoji.style.fontSize="14px",o.emoji.style.lineHeight="1",o.emoji.style.background="transparent",o.emoji.style.borderRadius="0",o.emoji.style.boxShadow="none",o.emoji.style.marginRight="0",o.label.className="text-[13px] font-semibold tracking-tight truncate",o.status.className="text-[11px] font-medium truncate",g){case"online":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-emerald-300/90");break;case"error":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-rose-300/90");break;case"pending":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-amber-200/90");break;default:o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-slate-300/75");break}}if(Object.keys(i).forEach(t=>E(t,"unknown")),r){console.warn("[agent-status-row] Optional SSE disabled:",s);return}function T(t){try{return JSON.parse(t)}catch{return null}}function A(t){return!t||typeof t!="object"?null:t.payload&&typeof t.payload=="object"?t.payload:t.data&&typeof t.data=="object"?t.data:t.state&&typeof t.state=="object"?t.state:t}function a(t){if(!t||typeof t!="object")return!1;let n=t.agents;if(!n||typeof n!="object")return!1;let o=!1;for(let[g,e]of Object.entries(n)){let m=String(g||"").toLowerCase();if(!i[m])continue;let _="unknown";if(typeof e=="string")_=e;else if(e&&typeof e=="object"){_=e.status??e.state??e.level??e.health??e.mode??"unknown";let C=e.at??e.ts??e.last_activity??e.lastActivity??e.last_seen??e.lastSeen??null;C!=null&&(c[m]=C)}E(m,String(_||"unknown")),o=!0}return I(),o}function d(t){if(!t||typeof t!="object")return!1;let n=(t.agent||t.actor||t.source||t.worker||t.name||"").toString().toLowerCase();if(!n||!i[n])return!1;let o=t.status??t.state??t.level??t.health??t.mode??"unknown",g=t.at??t.ts??t.last_activity??t.lastActivity??t.last_seen??t.lastSeen??null;return g!=null&&(c[n]=g),E(n,String(o||"unknown")),I(),!0}function x(t,n){let o=T(n.data);if(!o)return;let g=A(o);t==="ops.state"&&a(g)||a(o)||a(g)||(d(g),d(o))}let f;try{f=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(s)}catch(t){console.error("agent-status-row.js: Failed to open OPS SSE connection:",t);return}f&&(f.onmessage=t=>x("message",t),f.addEventListener("hello",t=>x("hello",t)),f.addEventListener("ops.state",t=>x("ops.state",t)),f.addEventListener("state",t=>x("state",t)),f.addEventListener("update",t=>x("update",t)),f.onerror=t=>{console.warn("agent-status-row.js: OPS SSE error:",t),Object.keys(i).forEach(n=>E(n,"unknown")),k(v,"\u2014")})})();var D="__broadcastVisualizationInited",j=["Matilda","Cade","Effie"];function R(){let p=document.getElementById("broadcast-visual");if(!p)return;let u=[];for(let b=0;b<j.length;b++){let S=j[b];u.push('<div class="node" id="node-'+S+'">'+S+"</div>"),b<j.length-1&&u.push('<div class="arrow">\u279C</div>')}p.innerHTML=u.join("")}function $(){let p=0;setInterval(()=>{document.querySelectorAll(".node").forEach(i=>i.classList.remove("active"));let b="node-"+j[p],S=document.getElementById(b);S&&S.classList.add("active"),p=(p+1)%j.length},1500)}function H(){if(typeof window>"u"||typeof document>"u"||window[D])return;window[D]=!0;let p=()=>{R(),$()};document.readyState==="loading"?window.addEventListener("DOMContentLoaded",p):p()}typeof window<"u"&&(window.initBroadcastVisualization=H);(function(){if(!(typeof document>"u")){var p=document.getElementById("ops-dashboard-pill");if(!p){var u=document.querySelector("[data-ops-pill]");u&&(u.id="ops-dashboard-pill")}}})();(()=>{if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED||typeof window>"u"||typeof EventSource>"u"||window.__opsGlobalsBridgeInitialized)return;window.__opsGlobalsBridgeInitialized=!0,typeof window.lastOpsHeartbeat>"u"&&(window.lastOpsHeartbeat=null),typeof window.lastOpsStatusSnapshot>"u"&&(window.lastOpsStatusSnapshot=null);let p="/events/ops";if((typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0){console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):",p);return}let b=S=>{try{let i=JSON.parse(S.data||"null");if(!i)return;window.lastOpsHeartbeat=Math.floor(Date.now()/1e3),window.lastOpsStatusSnapshot=i}catch(i){console.warn("[ops-globals-bridge] Failed to parse OPS event:",i)}try{window.dispatchEvent(new CustomEvent("mb:ops:update",{detail:{event:"message",state:window.lastOpsStatusSnapshot}}))}catch{}};try{let S=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(p);if(!S)return null;if(S.onmessage=b,S.addEventListener("hello",b),!S)return;S.onerror=i=>{console.warn("[ops-globals-bridge] EventSource error:",i)}}catch(S){console.warn("[ops-globals-bridge] Failed to init EventSource:",S)}})();(function(){if(typeof window>"u"||typeof document>"u"||window.location.pathname!=="/dashboard")return;var p=5e3,u="ops-dashboard-pill";function b(){var i=document.getElementById(u);return i||(i=document.createElement("span"),i.id=u,i.className="ops-pill ops-pill-unknown",i.textContent="OPS: Unknown",i.style.display="inline-block",document.body.firstChild?document.body.insertBefore(i,document.body.firstChild):document.body.appendChild(i),i)}function S(){var i=document.getElementById("ops-status-pill");i&&(i.style.display="none");var c=b();if(c){var w=typeof window.lastOpsHeartbeat=="number",v=w?"OPS: Online":"OPS: Unknown",k=w?"ops-pill-online":"ops-pill-unknown";c.classList.remove("ops-pill-unknown","ops-pill-online","ops-pill-stale","ops-pill-error"),c.classList.add(k),c.textContent=v}}S(),setInterval(S,p)})();(function(){function p(i){console.log("[matilda-chat]",i)}function u(i,c,w){if(i){var v=document.createElement("p");v.className="mb-1 text-sm";var k=c?c+": ":"";v.textContent=k+w,i.appendChild(v),i.scrollTop=i.scrollHeight}}function b(i,c,w){i&&(i.disabled=w,i.classList.toggle("opacity-60",w),i.textContent=w?"Sending...":"Send"),c&&(c.disabled=w)}async function S(){var i=document.getElementById("matilda-chat-root");if(!i){p("No #matilda-chat-root found; skipping wiring.");return}var c=document.getElementById("matilda-chat-transcript"),w=document.getElementById("matilda-chat-input"),v=document.getElementById("matilda-chat-send");if(window.appendMessage=window.appendMessage||function(y){try{var s=y&&y.role?String(y.role):"system",r=y&&(y.content??y.text??y.message)?String(y.content??y.text??y.message):"",h=s==="user"?"You":s==="matilda"?"Matilda":"System";u(c,h,r)}catch{}},window.__appendMessage=window.__appendMessage||window.appendMessage,!c||!w||!v){p("Missing one or more Matilda chat elements; aborting wiring.");return}function k(y){return(y||"").toString().trim()}async function l(){var y=k(w.value);if(y){u(c,"You",y),w.value="",b(v,w,!0);try{var s=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:y,agent:"matilda"})});if(!s.ok){u(c,"Matilda","(error talking to /api/chat)");return}var r=await s.json(),h=r&&(r.reply||r.message||r.response)||"(no reply)";u(c,"Matilda",h)}catch(E){console.error(E),u(c,"Matilda","(network error)")}finally{b(v,w,!1)}}}v.addEventListener("click",l);var I=document.getElementById("matilda-chat-quick-check");I&&I.addEventListener("click",function(){w.value="Quick systems check from dashboard Phase 11.4.",l()}),w.addEventListener("keydown",function(y){y.key==="Enter"&&!y.shiftKey&&(y.preventDefault(),l())}),p("Matilda chat wiring complete.")}document.addEventListener("DOMContentLoaded",S)})();(function(){console.log("[dashboard-delegation] module loaded");function p(s){return document.getElementById(s)}function u(){var s=window.fetch,r=typeof s;return console.log("[dashboard-delegation] detected fetch type:",r),r!=="function"?(console.error("[dashboard-delegation] fetch is not a function; value:",s),null):s.bind(window)}function b(s){return String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function S(s){return'<pre class="mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300">'+b(JSON.stringify(s,null,2))+"</pre>"}function i(s,r){var h=p("delegation-response"),E=p("delegation-status-panel");h&&(h.innerHTML=r,E&&(E.classList.remove("border-gray-700","border-teal-600","border-green-600","border-red-600","border-amber-500"),s==="sending"?E.classList.add("border-teal-600"):s==="success"?E.classList.add("border-green-600"):s==="error"?E.classList.add("border-red-600"):s==="waiting"?E.classList.add("border-amber-500"):E.classList.add("border-gray-700")))}function c(){i("idle","Awaiting operator input.<br>Results from delegation requests will appear here.")}function w(s){i("sending",'<div class="text-teal-300 font-medium">Sending delegation\u2026</div><div class="mt-2 text-gray-400">Preparing request for the orchestration layer.</div>'+(s?'<div class="mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words">'+b(s)+"</div>":""))}function v(){i("waiting",'<div class="text-amber-300 font-medium">Still waiting on delegation response\u2026</div><div class="mt-2 text-gray-400">The request may still be processing.</div>')}function k(s){var r="Delegation accepted.";s&&typeof s=="object"&&(r=s.message||s.status||s.result||s.reply||s.ok&&"Delegation accepted."||r),i("success",'<div class="text-green-300 font-medium">'+b(r)+'</div><div class="mt-2 text-gray-400">Request completed successfully.</div>'+(s&&typeof s=="object"?S(s):""))}function l(s,r){i("error",'<div class="text-red-300 font-medium">Delegation failed.</div><div class="mt-2 text-gray-300">'+b(s||"Unknown error")+"</div>"+(r?'<div class="mt-3 text-xs text-gray-400 break-words">'+b(r)+"</div>":""))}async function I(){var s=p("delegation-input"),r=p("delegation-submit");if(!s){console.warn("[dashboard-delegation] delegation input not found at click time"),l("Delegation input field was not found.");return}var h=String(s.value||"").trim();if(!h){console.warn("[dashboard-delegation] empty delegation input; skipping"),l("Please enter a delegation request before submitting.");return}console.log("[dashboard-delegation] sending delegation:",h);var E=u();if(!E){console.error("[dashboard-delegation] aborting delegation because fetch is unavailable or invalid"),l("Browser fetch is unavailable.");return}var T=r&&r.textContent||"Submit Delegation",A=null;try{r&&(r.disabled=!0,r.textContent="Sending...",r.classList.add("opacity-70","cursor-not-allowed")),w(h),A=window.setTimeout(v,4e3);var a;try{a=await E("/api/delegate-task",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({prompt:h,message:h,text:h,task:h})})}catch(f){throw console.error("[dashboard-delegation] fetch threw before response:",f),f}console.log("[dashboard-delegation] fetch returned:",{ok:a&&a.ok,status:a&&a.status,statusText:a&&a.statusText});var d=null,x="";try{x=await a.text(),d=x?JSON.parse(x):{}}catch(f){console.error("[dashboard-delegation] error parsing JSON response:",f),d={error:"Non-JSON response from /api/delegate-task",raw:x||""}}if(console.log("[dashboard-delegation] delegation response:",d),!a.ok){l(d&&(d.error||d.message||d.statusText)||"HTTP "+a.status+" "+(a.statusText||""),x);return}k(d)}catch(f){l(f&&f.message?f.message:String(f))}finally{A&&window.clearTimeout(A),r&&(r.disabled=!1,r.textContent=T,r.classList.remove("opacity-70","cursor-not-allowed"))}}function y(){var s=p("delegation-submit"),r=p("delegation-input");if(!s||!r){console.warn("[dashboard-delegation] delegation button or input not found in init");return}if(c(),s.dataset.delegationWired==="true"){console.log("[dashboard-delegation] Task Delegation wiring already active");return}s.dataset.delegationWired="true",s.addEventListener("click",I),r.addEventListener("keydown",function(h){(h.metaKey||h.ctrlKey)&&h.key==="Enter"&&(h.preventDefault(),I())}),console.log("[dashboard-delegation] Task Delegation wiring active")}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();(()=>{let p="/events/task-events",u="mb-task-events-panel",b="mb-task-events-feed",S="mb-task-events-counts",i="mb-task-events-panel-anchor";function c(){let n=document.getElementById(i);return n||document.body}function w(){if(document.getElementById(u))return;let n=c(),o=document.createElement("div");o.id=u;let g=n&&n.id===i;o.style.width=g?"100%":"360px",o.style.maxWidth=g?"100%":"calc(100vw - 24px)",o.style.maxHeight=g?"260px":"40vh",o.style.overflow="hidden",o.style.zIndex="9999",o.style.border="1px solid rgba(255,255,255,0.12)",o.style.borderRadius="14px",o.style.background="rgba(10,10,14,0.92)",o.style.backdropFilter="blur(10px)",o.style.boxShadow="0 10px 30px rgba(0,0,0,0.35)",o.style.color="rgba(255,255,255,0.92)",o.style.fontFamily="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial",g?o.style.marginTop="12px":(o.style.position="fixed",o.style.right="12px",o.style.bottom="12px");let e=document.createElement("div");e.style.display="flex",e.style.alignItems="center",e.style.justifyContent="space-between",e.style.gap="8px",e.style.padding="10px 12px",e.style.borderBottom="1px solid rgba(255,255,255,0.10)";let m=document.createElement("div");m.textContent="Task Events",m.style.fontSize="12px",m.style.letterSpacing="0.06em",m.style.opacity="0.9";let _=document.createElement("div");_.style.display="flex",_.style.alignItems="center",_.style.gap="10px";let C=document.createElement("div");C.id=S,C.textContent="created:0  completed:0  failed:0",C.style.fontSize="11px",C.style.opacity="0.85",C.style.fontVariantNumeric="tabular-nums";let L=document.createElement("span");L.setAttribute("aria-label","task-events connection"),L.title="task-events connection",L.style.display="inline-block",L.style.width="10px",L.style.height="10px",L.style.borderRadius="999px",L.style.background="rgba(255,255,255,0.25)",L.style.boxShadow="0 0 0 2px rgba(255,255,255,0.08) inset";let O=document.createElement("button");O.type="button",O.textContent="\xD7",O.title="hide",O.style.cursor="pointer",O.style.border="1px solid rgba(255,255,255,0.14)",O.style.background="transparent",O.style.color="rgba(255,255,255,0.85)",O.style.borderRadius="10px",O.style.width="28px",O.style.height="24px",O.style.lineHeight="22px",O.style.fontSize="14px",O.onclick=()=>o.remove(),_.appendChild(L),_.appendChild(C),_.appendChild(O),e.appendChild(m),e.appendChild(_);let N=document.createElement("div");N.id=b,N.style.padding="10px 12px",N.style.overflow="auto",N.style.maxHeight=g?"200px":"calc(40vh - 46px)",o.appendChild(e),o.appendChild(N),n.appendChild(o),window.__MB_TASK_EVENTS_PANEL={dot:L,feed:N,counts:C},console.log("[phase22] task-events panel mounted (anchored=%s)",g)}function v(n){w();let o=window.__MB_TASK_EVENTS_PANEL?.dot;o&&(n==="open"?o.style.background="rgba(80,200,120,0.85)":n==="error"?o.style.background="rgba(240,90,90,0.85)":o.style.background="rgba(255,255,255,0.25)")}let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\.created|task\.started|task\.running|task\.queued|task\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\.completed|task\.failed|task\.cancelled|task\.canceled|task\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}function y(n){if(typeof n=="number")return new Date(n).toISOString();if(typeof n=="string"&&n.trim()){let o=new Date(n);return Number.isNaN(o.getTime())?n.trim():o.toISOString()}return new Date().toISOString()}function s(n,o,g){let e=`${n||""} ${o||""} ${g||""}`.toLowerCase();return/fail|error|cancel|timeout/.test(e)?"terminal-error":/complete|done|success/.test(e)?"terminal-success":/queue|pending|retry|wait|hold|sleep/.test(e)?"waiting":/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(e)?"active":"neutral"}function r(n){return n==="terminal-error"?"rgba(240,90,90,0.95)":n==="terminal-success"?"rgba(80,200,120,0.95)":n==="waiting"?"rgba(250,204,21,0.95)":n==="active"?"rgba(96,165,250,0.95)":"rgba(255,255,255,0.18)"}function h(n,o){let g=y(n.ts),e=String(n.kind??o??"event"),m=n.task_id??n.taskId??"unknown",_=n.run_id??n.runId??"",C=n.actor??n.meta?.actor??n.meta?.owner??"",L=n.status??n.meta?.status??"",O=typeof n.cursor=="number"||typeof n.cursor=="string"?String(n.cursor):"",N=String(n.msg??n.message??"").trim(),B=[];B.push(`task=${m}`),_&&B.push(`run=${_}`),C&&B.push(`actor=${C}`),L&&B.push(`status=${L}`),O&&B.push(`cursor=${O}`),N&&B.push(N);let M=B.join(" \u2022 "),P=s(e,N,L);return{ts:g,kind:e,detail:M,tone:P}}function E(n,o){w();let g=document.getElementById(b);if(!g)return;let e=h(n,o),m=document.createElement("div");m.className=`phase61-task-event phase61-task-event-${e.tone}`,m.dataset.eventKind=e.kind,m.dataset.eventTone=e.tone,m.style.position="relative",m.style.display="grid",m.style.gridTemplateColumns="minmax(112px, 132px) minmax(150px, 170px) 1fr",m.style.gap="10px",m.style.alignItems="start",m.style.padding="10px 12px 10px 14px",m.style.marginBottom="8px",m.style.border="1px solid rgba(255,255,255,0.10)",m.style.borderRadius="12px",m.style.background="rgba(255,255,255,0.03)",m.style.lineHeight="1.35",m.style.fontSize="12px";let _=document.createElement("span");_.setAttribute("aria-hidden","true"),_.style.position="absolute",_.style.left="0",_.style.top="10px",_.style.bottom="10px",_.style.width="3px",_.style.borderRadius="999px",_.style.background=r(e.tone);let C=document.createElement("div");C.textContent=e.ts,C.style.color="rgba(255,255,255,0.68)",C.style.fontVariantNumeric="tabular-nums",C.style.whiteSpace="nowrap";let L=document.createElement("div");L.textContent=e.kind,L.style.color="rgba(255,255,255,0.92)",L.style.fontWeight="600",L.style.letterSpacing="0.01em",L.style.wordBreak="break-word";let O=document.createElement("div");O.textContent=e.detail||"\u2014",O.style.color="rgba(255,255,255,0.78)",O.style.wordBreak="break-word",m.appendChild(_),m.appendChild(C),m.appendChild(L),m.appendChild(O),g.prepend(m);let N=Array.from(g.children);if(N.length>60)for(let B=60;B<N.length;B++)N[B].remove()}function T(n){try{window.dispatchEvent(new CustomEvent("mb.task.event",{detail:n}))}catch{}}function A(n){try{return JSON.parse(n)}catch{return null}}function a(n,o){let g=typeof o=="string"?A(o):o,e=g&&typeof g=="object"?g:{kind:n,raw:o};n==="task.event"&&(e.actor==null&&e.meta&&typeof e.meta=="object"&&(e.actor=e.meta.actor??e.meta.owner??null),!e.kind&&e.type&&(e.kind=e.type),e.kind==="task.event"&&e.type&&(e.kind=e.type),e.task_id==null&&e.taskId!=null&&(e.task_id=e.taskId),e.run_id==null&&e.runId!=null&&(e.run_id=e.runId),e&&e.meta&&typeof e.meta=="object"&&(e.task_id==null&&e.meta.task_id!=null&&(e.task_id=e.meta.task_id),e.run_id==null&&e.meta.run_id!=null&&(e.run_id=e.meta.run_id),e.actor==null&&e.meta.actor!=null&&(e.actor=e.meta.actor),e.actor==null&&e.meta.owner!=null&&(e.actor=e.meta.owner),e.status==null&&e.meta.status!=null&&(e.status=e.meta.status))),e.kind||(e.kind=n);let m=`${n}|${e.kind}|${e.ts??""}|${e.task_id??""}|${e.run_id??""}|${e.cursor??""}`;if(!k.has(m)){if(k.add(m),(e.kind==="task.created"||e.kind==="task.completed"||e.kind==="task.failed")&&I(String(e.kind)),G(e,n),E(e,n),window.__UI_DEBUG)try{console.log("[task-events] mb.task.event",e)}catch{}T(e)}}let d=null,x=0;function f(){if(w(),d){try{d.close()}catch{}d=null}let n=p;d=new EventSource(n),d.onopen=()=>{x=0,v("open"),E({ts:Date.now(),kind:"sse.open",message:`url=${n}`},"sse.open"),console.log("[phase22] task-events SSE open",n)},d.onerror=()=>{v("error");try{d.close()}catch{}d=null,x+=1;let g=Math.min(15e3,500*Math.pow(2,Math.min(6,x)));E({ts:Date.now(),kind:"sse.error",message:`reconnect_in=${g}ms`},"sse.error"),console.log("[phase22] task-events SSE error; reconnect in",g),setTimeout(f,g)},d.onmessage=g=>a("message",g.data);let o=["hello","heartbeat","task.event","task.created","task.completed","task.failed","task.updated","task.status"];for(let g of o)d.addEventListener(g,e=>a(g,e.data))}function t(){f(),setInterval(()=>{if(!document.getElementById(u))try{f()}catch{}},2e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",t,{once:!0}):t(),window.__MB_TASK_EVENTS={url:p,reconnect:()=>f()}})();(()=>{let p="mb.task.event",u=new Map,b={queued:"task-status-queued",done:"task-status-done",failed:"task-status-failed"};function S(s){let r=String(s??"").toLowerCase();return r==="queued"||r==="pending"?"queued":r==="done"||r==="complete"||r==="completed"?"done":r==="failed"||r==="error"?"failed":r||"unknown"}function i(s){return s?.task_id??s?.taskId??s?.task?.id??null}function c(s){let r=s?.task&&typeof s.task=="object"?s.task:null,h=i(s),E=s?.status??s?.payload?.status??r?.status??(s?.kind==="task.created"?"queued":null);return{id:h!=null?String(h):null,status:E!=null?S(E):null,title:r?.title??s?.title??null,agent:r?.agent??s?.agent??null,error:s?.error??s?.payload?.error??r?.error??null,updated_at:r?.updated_at??s?.ts??Date.now()}}function w(s,r){if(!s)return;let h=S(r);s.setAttribute("data-task-status",h),s.classList?.remove(...Object.values(b)),b[h]&&s.classList?.add(b[h]);let E=s.querySelector?.("[data-task-field='status']")||s.querySelector?.(".task-status")||s.querySelector?.(".status")||null;E&&(E.textContent=h)}function v(s){if(!s?.id)return;let r=String(s.id),h=[document.getElementById(`task-${r}`),document.getElementById(`task_${r}`),document.querySelector?.(`[data-task-id="${CSS.escape(r)}"]`),document.querySelector?.(`[data-taskid="${CSS.escape(r)}"]`)].filter(Boolean);for(let E of h)w(E,s.status)}function k(){let s=0,r=0,h=0;for(let T of u.values()){let A=S(T.status);A==="queued"?s++:A==="done"?r++:A==="failed"&&h++}let E=[["queued",s],["done",r],["failed",h]];for(let[T,A]of E){let a=document.getElementById(`task-count-${T}`)||document.getElementById(`tasks-${T}-count`)||document.querySelector?.(`[data-task-count="${T}"]`)||null;a&&(a.textContent=String(A))}}function l(s){if(!s?.id)return;let r=String(s.id),h=u.get(r)||{},E={...h,...s,id:r,status:s.status??h.status};u.set(r,E),v(E),k()}function I(s){let r=c(s);!r.id&&s?.kind&&(s.kind==="task.completed"&&(r.status="done"),s.kind==="task.failed"&&(r.status="failed")),r.id&&l(r)}function y(){window.__PHASE22_TASK_UI_BOUND||(window.__PHASE22_TASK_UI_BOUND=!0,window.addEventListener(p,s=>{try{(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&console.log("[phase22] mb.task.event",s.detail),I(s.detail)}catch{}}),window.__PHASE22_TASK_UI={tasks:u},console.log("[phase22] bindings attached"))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();typeof window<"u"&&typeof window.__DISABLE_OPTIONAL_SSE>"u"&&(window.__DISABLE_OPTIONAL_SSE=!1);})();
./public/bundle.js:42:    'queued',
./public/dashboard.html:640:      const EVENT_WAITING = /queue|queued|pending|wait|retry|hold|sleep/i;
./public/dashboard.html.phase61_agent_emoji_backup:564:      const EVENT_WAITING = /queue|queued|pending|wait|retry|hold|sleep/i;
./public/scripts/agent-status-row.js:19:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./PHASE27_SUPERVISOR_SUMMARY.txt:4:- Worker loop (phase26_task_worker.mjs) claims queued tasks via phase27_claim_one.sql and marks success/failure via phase27 markers.
./PHASE27_SUPERVISOR_SUMMARY.txt:9:- End-to-end success path: queued -> running -> completed, with task.running + task.completed events written to task_events.
./PHASE41_ACCEPTANCE.md:26:- Tier A task is claimable (transitions away from `queued`).
./PHASE41_ACCEPTANCE.md:27:- Tier B task remains `queued`.
./PR_PHASE39_1.md:8:  - Phase39 action_tier smoke proves Tier A claims, Tier B remains queued
./PR_PHASE39_1.md:30:- Phase39: Tier A becomes `running`; Tier B remains `queued`
./PR_PHASE39_1.md:33:If a queued task is not Tier A, it must not be claimable via the canonical claim SQL.
./scripts/phase35_fix_appendTaskEvent_pool_signature.sh:113:    "task.created","task.queued","task.started","task.progress","task.completed","task.failed","task.canceled",
./scripts/demo/run-create-atlas.ts:12:  const status = "queued";
./scripts/phase40_tierA_claim_gate_smoke.sh:6:# 1) Insert queued Tier A + Tier B tasks
./scripts/phase40_tierA_claim_gate_smoke.sh:8:# 3) Tier B remains queued
./scripts/phase40_tierA_claim_gate_smoke.sh:33:echo "=== insert Tier A + Tier B queued tasks ==="
./scripts/phase40_tierA_claim_gate_smoke.sh:38:  ('$A_ID', 'Phase40 TierGate A', 'queued', 0, 3, 'A'),
./scripts/phase40_tierA_claim_gate_smoke.sh:39:  ('$B_ID', 'Phase40 TierGate B', 'queued', 0, 3, 'B');
./scripts/phase31_7_verify_container_reclaim.sh:20:    and status in ('queued','running')
./scripts/phase57_ci_bootstrap.sh:70:  status TEXT NOT NULL DEFAULT 'queued',
./scripts/phase39_action_tier_claim_smoke.sh:21:echo "=== Phase39 smoke: deterministic preflight (no claimable Tier-A queued tasks) ==="
./scripts/phase39_action_tier_claim_smoke.sh:32:  WHERE status='queued'
./scripts/phase39_action_tier_claim_smoke.sh:38:      'preflight failed: % claimable Tier-A queued tasks present. Refusing to run Phase39 smoke.',
./scripts/phase39_action_tier_claim_smoke.sh:43:SELECT 'OK: no claimable Tier-A queued tasks' AS verdict;
./scripts/phase39_action_tier_claim_smoke.sh:47:echo "=== insert Tier A + Tier B queued tasks (available_at NOW, max_attempts NULL) ==="
./scripts/phase39_action_tier_claim_smoke.sh:59:  ('${TASK_A}', 'queued', 0, NULL, 'A', 'Phase39 smoke Tier A', now()),
./scripts/phase39_action_tier_claim_smoke.sh:60:  ('${TASK_B}', 'queued', 0, NULL, 'B', 'Phase39 smoke Tier B', now());
./scripts/phase39_action_tier_claim_smoke.sh:102:-- Assert Tier B stayed queued (fail hard)
./scripts/phase39_action_tier_claim_smoke.sh:105:    SELECT 1 FROM tasks WHERE task_id = :'task_b' AND status <> 'queued'
./scripts/phase39_action_tier_claim_smoke.sh:111:-- Assert Tier A was claimed (not still queued) (fail hard)
./scripts/phase39_action_tier_claim_smoke.sh:114:    SELECT 1 FROM tasks WHERE task_id = :'task_a' AND status = 'queued'
./scripts/phase14_5_patch_dashboard_no_cache.mjs:104:  console.warn("⚠️ Could not find dashboard-build-banner anchor — appending to body instead");
./scripts/phase28_smoke_one.sh:22:      'queued',
./scripts/phase28_smoke_one.sh:36:      'queued',
./scripts/phase28_smoke_one.sh:60:    and status = 'queued'
./scripts/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./scripts/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./scripts/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./scripts/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./scripts/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./scripts/phase32_null_max_attempts_claim_smoke.sh:15:echo "=== Phase32 smoke: deterministic preflight (no competing queued tasks) ==="
./scripts/phase32_null_max_attempts_claim_smoke.sh:27:  SELECT count(*) INTO q FROM tasks WHERE status='queued';
./scripts/phase32_null_max_attempts_claim_smoke.sh:29:    RAISE EXCEPTION 'preflight failed: queued tasks present (%). Refusing to run smoke.', q;
./scripts/phase32_null_max_attempts_claim_smoke.sh:37:echo "=== insert queued task with max_attempts NULL (Tier A, available now) ==="
./scripts/phase32_null_max_attempts_claim_smoke.sh:41:VALUES ('$TASK_ID', 'queued', 0, NULL, 'A', 'smoke phase32 NULL max_attempts', now());
./scripts/phase22_wire_task_events_ui.sh:293:      echo "Appending import for task-events-sse-client into: $f"
./scripts/phase57_snapshot_smoke.sh:103:# last_event_id may be UUID/text depending on lane; use last_event_ts (epoch ms) for monotonic advancement.
./scripts/agent-status-row.js:45:    console.warn("⚠️ Could not find Dashboard v2.0.3 anchor — appending to body instead");
./scripts/phase39_action_tier_smoke.sh:15:# Drain window for any non-smoke queued work (deterministic precondition).
./scripts/phase39_action_tier_smoke.sh:125:# -------- drain non-smoke queued (determinism precondition) --------
./scripts/phase39_action_tier_smoke.sh:133:WHERE status='queued'
./scripts/phase39_action_tier_smoke.sh:140:  echo "queued(non-smoke)=$Q"
./scripts/phase39_action_tier_smoke.sh:151:WHERE status IN ('queued','running')
./scripts/phase39_action_tier_smoke.sh:170:note "Insert queued Tier A + Tier B tasks (Tier A inserted first)"
./scripts/phase39_action_tier_smoke.sh:177:    VALUES ('${TASK_A_ID}', 'Phase39 Smoke Tier A', 'queued', 'A');
./scripts/phase39_action_tier_smoke.sh:182:    VALUES ('${TASK_B_ID}', 'Phase39 Smoke Tier B', 'queued', 'B');
./scripts/phase39_action_tier_smoke.sh:187:note "Pre-check: confirm both tasks exist + queued"
./scripts/phase39_action_tier_smoke.sh:205:  WHERE status='queued'
./scripts/phase39_action_tier_smoke.sh:236:note "Verify Tier A completed; Tier B still queued"
./scripts/phase39_action_tier_smoke.sh:254:  WHERE status='queued'
./scripts/phase39_action_tier_smoke.sh:277:# -------- final: prove TierB stayed queued, then cancel it to leave DB clean --------
./scripts/phase39_action_tier_smoke.sh:278:note "Final check: Tier B remained queued, then cancel it for clean re-runs"
./scripts/phase39_action_tier_smoke.sh:287:  AND status='queued';
./scripts/phase39_value_gate_smoke.sh:71:echo "=== insert Tier ${TIER} queued task (baseline mutation: INSERT only) ==="
./scripts/phase39_value_gate_smoke.sh:75:VALUES ('${TASK_ID}', '${TITLE}', 'queued', 0, '${TIER}');
./scripts/phase27_fix_claim_one_sql.sh:26:-- phase27: claim one queued task (conditional ORDER BY priority if present)
./scripts/phase27_fix_claim_one_sql.sh:30:  where status = 'queued'
./scripts/_local/phase61_4_task_events_render_path_patch.sh:184:    if (/queue|pending|retry|wait|hold|sleep/.test(text)) return "waiting";
./scripts/_local/phase62b_tasks_failed_local_verification.sh:91:  if (v === "queued" || v === "pending") return "queued";
./scripts/_local/phase61_5_1_recent_history_takeover_patch.sh:44:    if (/queue|pending|wait|retry|hold/.test(s)) return "rgba(250,204,21,0.95)";
./scripts/_local/hydrate_phase62b_tasks_running_metric.sh:31:    'queued',
./scripts/_local/hydrate_phase62b_tasks_running_metric.sh:217:      'queued',
./scripts/_local/phase66a_reducer_replay_check.sh:57:  { task_id: "t1", state: "task.queued", ts: 2 },
./scripts/_local/phase66a_reducer_replay_check.sh:60:  { task_id: "t3", state: "task.queued", ts: 5 },
./scripts/_local/phase62b_hydrate_tasks_running.sh:15:new = 'let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\\.created|task\\.started|task\\.running|task\\.queued|task\\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\\.completed|task\\.failed|task\\.cancelled|task\\.canceled|task\\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}'
./scripts/_local/phase62b_tasks_completed_local_verification.sh:91:  if (v === "queued" || v === "pending") return "queued";
./scripts/_local/phase62b_running_tasks_local_verification.sh:91:  if (v === "queued" || v === "pending") return "queued";
./scripts/_local/phase62b_running_tasks_local_verification.sh:114:  return v === "queued" || v === "pending" || v === "running" || v === "started" || v === "active" || v === "in_progress" || v === "in-progress";
./scripts/_local/phase66a_determinism_check.sh:62:  { task_id: "q2", state: "task.queued", ts: 2 },
./scripts/_local/phase66a_determinism_check.sh:65:  { task_id: "q3", state: "task.queued", ts: 5 },
./scripts/_local/phase66a_determinism_check.sh:66:  { task_id: "q3", state: "task.queued", ts: 5 },
./scripts/_local/route-loader/ops-sse-bootstrap.ts:13:    status TEXT DEFAULT 'pending',
./scripts/_local/phase61_5_5e_restore_pending_dot_color.sh:22:print("default agent dot color restored to pending/yellow")
./scripts/_local/phase62b_tasks_queued_discovery.sh:42:echo "-- queued/pending status references --"
./scripts/_local/phase62b_tasks_queued_discovery.sh:44:  "queued|pending|enqueued|status.*queued|run_state.*pending" \
./scripts/_local/phase62b_tasks_queued_discovery.sh:72:    grep -nE "create table.*tasks|table.*tasks|status|run_state|state|queued|pending|pgTable\\(['\"]tasks|sqliteTable\\(['\"]tasks" "$file" || true
./scripts/_local/phase62b_tasks_queued_discovery.sh:79:echo "Next: use output to confirm exact queued-state definition before wiring metric."
./scripts/_local/phase61_5_5v_trace_agent_status_producers.sh:36:    -d '{"agent":"Cade","status":"queued","meta":{"source":"phase61_5_5v_probe"}}'
./scripts/_local/normalize_phase63_shared_task_events_metric_corridor.sh:43:    'queued',
./scripts/_local/hydrate_phase62b_latency_metric.sh:30:    'queued',
./scripts/_local/hydrate_phase62b_latency_metric.sh:288:      'queued',
./scripts/_local/phase61_3_task_events_polish_patch.sh:220:      const EVENT_WAITING = /queue|queued|pending|wait|retry|hold|sleep/i;
./scripts/_local/phase61_5_5z_prove_agent_status_route_updates_ops_snapshot.sh:27:    -d '{"agent":"Cade","status":"queued","meta":{"source":"phase61_5_5z_probe"}}'
./scripts/_local/phase61_5_5z_prove_agent_status_route_updates_ops_snapshot.sh:46:    | grep -nE 'ops.state|Matilda|Cade|Effie|Atlas|agents|status|running|queued|ready' || true
./scripts/_local/phase65_1_tasks_running_inspect.sh:17:rg -n 'task\.|task-events|EventSource|SSE|recent history|recent tasks|running|queued|completed|failed|cancelled|started' \
./scripts/_local/phase65_1_tasks_running_inspect.sh:24:rg -n '/api/tasks|task_id|run_id|state|status|queued|running|completed|failed|cancelled|started' \
./scripts/_local/phase64_4_probe_ops_stream_task_events.sh:53:grep -Ein 'task|run|lease|dispatch|heartbeat|queue|queued|start|started|complete|completed|cancel|cancelled|failed|error|progress' "$TMP_FILE" || true
./scripts/_local/phase64_4_probe_ops_stream_task_events.sh:90:    if re.search(r'task|run|lease|dispatch|heartbeat|queue|queued|start|started|complete|completed|cancel|cancelled|failed|error|progress', blob):
./scripts/_local/phase61_5_wire_recent_tasks_and_history.sh:43:    if (/queue|pending|wait|retry|hold/.test(s)) return "rgba(250,204,21,0.95)";
./scripts/_local/phase65_5_post_patch_verify.sh:32:  grep -nE 'base_url=|dashboard_up=|container_http_up=|metric-tasks|metric-success|metric-success-rate|metric-latency|policy.probe.task|task_status|status":"running"|status":"queued"|Recv failure|Connection reset|error|failed' \
./scripts/phase41_tierA_claim_gate_smoke.sh:25:echo "=== insert Tier A + Tier B tasks (queued) ==="
./scripts/phase41_tierA_claim_gate_smoke.sh:34:  ('${TASK_A}', 'Phase 41 smoke: Tier A claimable', 'queued', 0, NULL, 'A'),
./scripts/phase41_tierA_claim_gate_smoke.sh:35:  ('${TASK_B}', 'Phase 41 smoke: Tier B NOT claimable', 'queued', 0, NULL, 'B');
./scripts/phase41_tierA_claim_gate_smoke.sh:67:    echo "ERROR: claim returned no row (no Tier-A queued candidates?)"
./scripts/phase41_tierA_claim_gate_smoke.sh:105:echo "=== verify DB state: TASK_A running, TASK_B still queued ==="
./scripts/phase41_tierA_claim_gate_smoke.sh:114:echo "OK: Phase 41 smoke passed (canonical claim only yields Tier A; Tier B remains queued; TASK_A claimed)."
./scripts/phase27_wire_worker_backoff.sh:139:      (coalesce(status,'') in ('created','queued','requeued') or status is null)
./scripts/phase27_wire_worker_backoff.sh:167:        (coalesce(status,'') in ('created','queued','requeued') or status is null)
./scripts/phase27_wire_worker_backoff.sh:243:    kind: "task.requeued",
./PHASE25_AUTHORITY_ORCHESTRATION_CONTRACT.md:86:- `task.queued`
./PHASE25_AUTHORITY_ORCHESTRATION_CONTRACT.md:106:- created → queued → started → progress* → terminal
./PHASE25_AUTHORITY_ORCHESTRATION_CONTRACT.md:107:- queued → started | canceled
./PHASE39_TERMINAL_STOP.md:21:Phase 39 is terminally complete pending merge + tag.
./PHASE31_HANDOFF.md:14:  - Pins claim SQL to the seeded TASK_ID so other queued tasks cannot steal the claim.
./ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./.git/logs/HEAD:102:0c22cb3af0c0267a5a99fe916230a8291e4d93ae 884c1951ecbbcaac5e636105bd374d713de48984 Marcela Gama Le <marcelagamale@gmail.com> 1773332925 -0700	commit: Add Phase 62B label binding fix verification pending checkpoint
./.git/logs/HEAD:540:034d7db7e601a8c7cb277d7324276bdaca38e125 27dd8319abd86a6c5625fb44d1e7f41d91b1e4fd Marcela Gama Le <marcelagamale@gmail.com> 1773720607 -0700	commit: phase62b: define next telemetry metric protocol (tasks queued)
./.git/logs/HEAD:541:27dd8319abd86a6c5625fb44d1e7f41d91b1e4fd 146aa8671ee3e83d874e1f650221f2cb0f95e489 Marcela Gama Le <marcelagamale@gmail.com> 1773720626 -0700	commit: phase62b: tasks queued execution plan defined
./.git/logs/HEAD:542:146aa8671ee3e83d874e1f650221f2cb0f95e489 b1ca66d937781d652e0acc62b549cc65c8f03882 Marcela Gama Le <marcelagamale@gmail.com> 1773720787 -0700	commit: phase62b: add tasks queued discovery script
./.git/logs/HEAD:543:b1ca66d937781d652e0acc62b549cc65c8f03882 2c1fa5e97511489851c7ecb9d93bf840286f3f07 Marcela Gama Le <marcelagamale@gmail.com> 1773720843 -0700	commit: phase62b: add tasks queued discovery runbook
./.git/logs/refs/heads/phase65b-running-tasks-hydration:245:034d7db7e601a8c7cb277d7324276bdaca38e125 27dd8319abd86a6c5625fb44d1e7f41d91b1e4fd Marcela Gama Le <marcelagamale@gmail.com> 1773720607 -0700	commit: phase62b: define next telemetry metric protocol (tasks queued)
./.git/logs/refs/heads/phase65b-running-tasks-hydration:246:27dd8319abd86a6c5625fb44d1e7f41d91b1e4fd 146aa8671ee3e83d874e1f650221f2cb0f95e489 Marcela Gama Le <marcelagamale@gmail.com> 1773720626 -0700	commit: phase62b: tasks queued execution plan defined
./.git/logs/refs/heads/phase65b-running-tasks-hydration:247:146aa8671ee3e83d874e1f650221f2cb0f95e489 b1ca66d937781d652e0acc62b549cc65c8f03882 Marcela Gama Le <marcelagamale@gmail.com> 1773720787 -0700	commit: phase62b: add tasks queued discovery script
./.git/logs/refs/heads/phase65b-running-tasks-hydration:248:b1ca66d937781d652e0acc62b549cc65c8f03882 2c1fa5e97511489851c7ecb9d93bf840286f3f07 Marcela Gama Le <marcelagamale@gmail.com> 1773720843 -0700	commit: phase62b: add tasks queued discovery runbook
./.git/logs/refs/heads/phase62-dashboard-layout:64:0c22cb3af0c0267a5a99fe916230a8291e4d93ae 884c1951ecbbcaac5e636105bd374d713de48984 Marcela Gama Le <marcelagamale@gmail.com> 1773332925 -0700	commit: Add Phase 62B label binding fix verification pending checkpoint
./.git/COMMIT_EDITMSG:1:phase62b: add tasks queued discovery runbook
./ts-backup/ops-sse-bootstrap.ts:13:    status TEXT DEFAULT 'pending',
./ts-backup/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./ts-backup/delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./ts-backup/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./ts-backup/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./ts-backup/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./ts-backup/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./PHASE14_1_TASK_CONTRACT.md:19:queued | delegated | running | blocked | complete | canceled | failed
./PHASE14_1_TASK_CONTRACT.md:22:- queued → delegated
./PHASE14_1_TASK_CONTRACT.md:27:- queued/delegated/running/blocked → canceled
./PHASE14_1_TASK_CONTRACT.md:28:- queued/delegated/running/blocked → failed
./PHASE26_HANDOFF_ONE_LINER.txt:1:Start from tag v25.0-task-events-single-writer-ok; on feature/phase26-task-worker-min we added server/worker/phase26_task_worker.mjs + scripts/phase26_run_worker.sh; worker claims tasks where tasks.status='queued' via FOR UPDATE SKIP LOCKED, sets running/completed(or failed), inserts task_events (kind + JSON payload-as-text) so /events/task-events streams task.created + task.running + task.completed/failed; smoke verified end-to-end with task t86 (events 261–263) and tasks.id=86 completed.
./PHASE26_SUPERVISOR_SUMMARY.txt:4:- Implemented a minimal executor/worker loop that claims queued tasks, marks them running, then completes/fails them.
./PHASE40_ACCEPTANCE_CHECKS.sql:4:-- Prove: Tier B/C remain queued after canonical claim SQL runs.
./routes/delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./routes/routes/delegations.ts:9:    const { description, event_type = "delegation", agent = "Cade", status = "pending" } = req.body;
./PHASE14_PLAN.md:26:  * queued → delegated → running → blocked → complete → canceled → failed
./scripts_backup_2/demo/run-create-atlas.ts:12:  const status = "queued";
./scripts_backup_2/agents/cade-delegation-watcher.ts:32:  const pending = sqlite.prepare(
./scripts_backup_2/agents/cade-delegation-watcher.ts:33:    "SELECT id, description FROM task_events WHERE event_type = 'delegation' AND status = 'pending'"
./scripts_backup_2/agents/cade-delegation-watcher.ts:36:  for (const task of pending) executeDelegation(task);
./scripts_backup_2/utils/ensure-task-events-main.ts:13:    status TEXT DEFAULT 'pending',
./scripts_backup_2/utils/ensure-task-events.ts:9:      status TEXT DEFAULT 'pending',
./scripts_backup_2/_local/route-loader/ops-sse-bootstrap.ts:17:    status TEXT DEFAULT 'pending',
./PHASE28_HANDOFF.md:16:- Smoke script is deterministic: pins claim to the seeded TASK_ID so retries/other queued tasks don't steal the claim.
./PHASE28_HANDOFF.md:27:- Expect OK; task ends queued with attempt=1 and future available_at; task_events includes task.running, task.failed, task.retry_scheduled.
./PHASE11_MATILDA_CHAT_COMPLETION_HANDOFF.md:102:You can pick any of these depending on energy and focus:
./sql/phase40_claim_one_tierA.sql:13:-- NOTE: This claims ONE task by moving queued -> running. It is intentionally minimal:
./sql/phase40_claim_one_tierA.sql:19:  WHERE status = 'queued'

-- count query references --
./PHASE33_SUPERVISOR_SUMMARY.md:31:  - `having count(*) filter (where kind in ('task.completed','task.failed')) > 1` returned 0 rows.
./PHASE37_ACCEPTANCE_CHECKS.sql:26:SELECT 'run_view rowcount' AS check, count(*) AS n
./PHASE37_ACCEPTANCE_CHECKS.sql:68:  count(*) AS n
./PHASE37_ACCEPTANCE_CHECKS.sql:72:HAVING count(*) > 1
./PHASE32_DONE.md:22:- `psql "$POSTGRES_URL" -c "select count(*) from drizzle.__drizzle_migrations;"`
./memory/cade_runtime.log:26:[2025-08-06T21:21:53.099Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:18:[2025-08-06T20:05:16.226Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:25:[2025-08-06T20:05:19.334Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:32:[2025-08-06T20:05:20.807Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:39:[2025-08-06T20:05:23.887Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:46:[2025-08-06T20:05:26.946Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:53:[2025-08-06T20:05:30.024Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:60:[2025-08-06T20:05:33.106Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:67:[2025-08-06T20:05:36.188Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:74:[2025-08-06T20:05:39.272Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:81:[2025-08-06T20:05:42.346Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:88:[2025-08-06T20:05:45.476Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:95:[2025-08-06T20:05:48.548Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:102:[2025-08-06T20:05:51.620Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:109:[2025-08-06T20:05:54.703Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:116:[2025-08-06T20:05:57.782Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:123:[2025-08-06T20:06:00.863Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:130:[2025-08-06T20:06:03.946Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:137:[2025-08-06T20:06:07.028Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:144:[2025-08-06T20:06:10.111Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:151:[2025-08-06T20:06:13.195Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:158:[2025-08-06T20:06:16.265Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:165:[2025-08-06T20:06:19.350Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:172:[2025-08-06T20:06:22.434Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:179:[2025-08-06T20:06:25.521Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:186:[2025-08-06T20:06:28.589Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:193:[2025-08-06T20:06:31.671Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:200:[2025-08-06T20:06:34.751Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:207:[2025-08-06T20:06:37.840Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:214:[2025-08-06T20:06:40.913Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:221:[2025-08-06T20:06:43.992Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:228:[2025-08-06T20:06:47.082Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:235:[2025-08-06T20:06:50.166Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:271:[2025-08-06T20:07:36.383Z] 🔎 Filtered JSON where status=active
./memory/cade_runtime_backup.log:401:[2025-08-06T20:11:04.533Z] 🔎 Filtered JSON where status=active
./reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./snapshots/20251114-114821/scripts/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./snapshots/20251114-114821/scripts/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./snapshots/20251114-114707/scripts/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./snapshots/20251114-114707/scripts/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./PHASE35_HANDOFF.md:61:       COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) AS terminal_count,
./PHASE35_HANDOFF.md:68:HAVING COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) > 1
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:21:tasks where status = 'queued'
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:28:SELECT COUNT(*)
./PHASE62B_TASKS_QUEUED_EXECUTION_PLAN.md:36:1 Identify exact queued definition from tasks table.
./server/worker/phase34_reclaim_stale_pg.sql:28:SELECT count(*)::bigint AS reclaimed FROM r;
./server/worker/phase27_claim_one.sql:6:  from tasks
./server/worker/phase27_claim_one.sql:7:  where status = 'queued'
./server/worker/phase34_reclaim_stale.sql:24:SELECT count(*) FROM r;
./server/worker/phase30_db_doctor_pretty.sh:7:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='payload')::int as has_tasks_payload,
./server/worker/phase30_db_doctor_pretty.sh:8:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='meta')::int as has_tasks_meta,
./server/worker/phase30_db_doctor_pretty.sh:9:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='attempt')::int as has_tasks_attempt,
./server/worker/phase30_db_doctor_pretty.sh:10:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='attempts')::int as has_tasks_attempts,
./server/worker/phase30_db_doctor_pretty.sh:11:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='available_at')::int as has_tasks_available_at,
./server/worker/phase30_db_doctor_pretty.sh:12:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='next_run_at')::int as has_tasks_next_run_at,
./server/worker/phase30_db_doctor_pretty.sh:14:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='payload_jsonb')::int as has_events_payload_jsonb,
./server/worker/phase30_db_doctor_pretty.sh:15:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='run_id')::int as has_events_run_id,
./server/worker/phase30_db_doctor_pretty.sh:16:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='actor')::int as has_events_actor
./server/worker/phase27_mark_failure.sql:8:  from tasks
./server/worker/phase28_claim_one.sql:7:  from tasks
./server/worker/phase28_claim_one.sql:8:  where status = 'queued'
./server/worker/phase29_db_doctor.sh:7:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='payload') as has_tasks_payload,
./server/worker/phase29_db_doctor.sh:8:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='meta') as has_tasks_meta,
./server/worker/phase29_db_doctor.sh:9:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='attempt') as has_tasks_attempt,
./server/worker/phase29_db_doctor.sh:10:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='attempts') as has_tasks_attempts,
./server/worker/phase29_db_doctor.sh:11:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='available_at') as has_tasks_available_at,
./server/worker/phase29_db_doctor.sh:12:  (select count(*) from information_schema.columns where table_name='tasks' and column_name='next_run_at') as has_tasks_next_run_at,
./server/worker/phase29_db_doctor.sh:13:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='payload') as has_events_payload,
./server/worker/phase29_db_doctor.sh:15:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='run_id') as has_events_run_id,
./server/worker/phase29_db_doctor.sh:16:  (select count(*) from information_schema.columns where table_name='task_events' and column_name='actor') as has_events_actor
./server/routes/api-tasks-postgres.mjs:96:    // (workers claim from tasks, not task_events)
./server/sql/phase40_6_shadow_audit_task_events.sql:39:    count(*)::int AS event_count,
./server/sql/phase40_6_shadow_audit_task_events.sql:86:    count(*)::int AS events_after_terminal
./scripts_backup/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./scripts_backup/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./scripts_backup/tests/verify-demo-readiness.sh:7:sqlite3 db/main.db "SELECT COUNT(*) FROM reflection_index;" && echo "✅ SQLite accessible" || echo "⚠️ SQLite check failed."
./scripts_backup/scripts/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./scripts_backup/scripts/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./scripts_backup/scripts/tests/verify-demo-readiness.sh:7:sqlite3 db/main.db "SELECT COUNT(*) FROM reflection_index;" && echo "✅ SQLite accessible" || echo "⚠️ SQLite check failed."
./docs/checkpoints/PHASE65_TASKS_RUNNING_HYDRATION_PLAN_20260314.md:36:Running Tasks = count(tasks where state = running OR queued)
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:451:./PHASE33_SUPERVISOR_SUMMARY.md:31:  - `having count(*) filter (where kind in ('task.completed','task.failed')) > 1` returned 0 rows.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:506:./PHASE35_HANDOFF.md:61:       COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) AS terminal_count,
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:507:./PHASE35_HANDOFF.md:68:HAVING COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) > 1
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4675:./scripts/smoke/phase35_epoch_owner_proof.zsh:207:running   = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.running';") or "0")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4676:./scripts/smoke/phase35_epoch_owner_proof.zsh:208:completed = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.completed';") or "0")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CODE_DISCOVERY_20260316.md:4677:./scripts/smoke/phase35_epoch_owner_proof.zsh:209:failed    = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.failed';") or "0")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3341:./PHASE33_SUPERVISOR_SUMMARY.md:31:  - `having count(*) filter (where kind in ('task.completed','task.failed')) > 1` returned 0 rows.
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3349:./PHASE35_HANDOFF.md:61:       COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) AS terminal_count,
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:3350:./PHASE35_HANDOFF.md:68:HAVING COUNT(*) FILTER (WHERE kind IN ('task.completed','task.failed')) > 1
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6248:./scripts/smoke/phase35_epoch_owner_proof.zsh:207:running   = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.running';") or "0")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6249:./scripts/smoke/phase35_epoch_owner_proof.zsh:208:completed = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.completed';") or "0")
./docs/checkpoints/PHASE62B_RUNNING_TASKS_CANDIDATE_SURFACES_20260316.md:6250:./scripts/smoke/phase35_epoch_owner_proof.zsh:209:failed    = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.failed';") or "0")
./docs/checkpoints/PHASE62B_NEXT_TARGET_SELECTION_20260316.md:47:• Active Agents Count (derived from tasks map)
./scripts/smoke/phase34_reclaim_proof.zsh:38:"select id,status,claimed_by,lease_expires_at,lease_epoch from tasks where id=$TASK_ID;"
./scripts/smoke/phase34_reclaim_proof.zsh:67:SELECT count(*)::bigint AS reclaimed FROM r;
./scripts/smoke/phase34_reclaim_proof.zsh:71:"select id,status,claimed_by,lease_expires_at,lease_epoch from tasks where id=$TASK_ID;"
./scripts/smoke/phase34_reclaim_proof.zsh:77:"select kind, count(*) n
./scripts/smoke/phase34_reclaim_proof.zsh:83:"select count(*) as terminal_n
./scripts/smoke/phase35_epoch_owner_proof.zsh:113:SELECT count(*)::bigint FROM r;
./scripts/smoke/phase35_epoch_owner_proof.zsh:137:SELECT count(*)::bigint FROM u;
./scripts/smoke/phase35_epoch_owner_proof.zsh:197:        psql(f"SELECT count(*) FROM {t} WHERE {task_col}::text = '{TASK_ID}';")
./scripts/smoke/phase35_epoch_owner_proof.zsh:207:running   = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.running';") or "0")
./scripts/smoke/phase35_epoch_owner_proof.zsh:208:completed = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.completed';") or "0")
./scripts/smoke/phase35_epoch_owner_proof.zsh:209:failed    = int(psql(f"SELECT count(*) FROM {table} WHERE {task_col}::text='{TASK_ID}' AND kind='task.failed';") or "0")
./scripts/phase40_tierA_claim_gate_smoke.sh:65:  "SELECT count(*) FROM tasks WHERE task_id LIKE 'smoke.phase40.tiergate.%' AND COALESCE(action_tier,'A')='A' AND status='running'")"
./scripts/phase54_regression_harness.sh:119:  docker exec "${pgc}" sh -lc "psql -U \"${user}\" -d \"${db}\" -Atc \"select count(*) from ${table};\""
./scripts/phase31_7_verify_container_reclaim.sh:9:BASE_TASK_ID="$(docker exec -i "$PG" sh -lc 'psql -U postgres -d postgres -Atc "select coalesce(max(id),0) from tasks;"')"
./scripts/phase31_7_verify_container_reclaim.sh:18:  from tasks
./scripts/phase31_7_verify_container_reclaim.sh:30:\"select count(*) from (
./scripts/phase31_7_verify_container_reclaim.sh:35:  having count(*) > 1
./scripts/phase31_7_verify_container_reclaim.sh:51:select count(*) from b
./scripts/phase31_7_verify_container_reclaim.sh:55:\"select count(*) from tasks
./scripts/phase39_action_tier_claim_smoke.sh:30:  SELECT count(*) INTO q
./scripts/phase28_worker_watch.sh:25:from tasks
./scripts/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./scripts/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./scripts/phase39_action_tier_policy_smoke.sh:44:SELECT COUNT(*)::text FROM picked;
./scripts/phase45_regression_policy_grants.sh:39:  SELECT count(*) FROM policy_grants_active WHERE subject='subject:A' AND scope='scope:X';
./scripts/phase30_smoke_canonical_ready.sh:11:has_jsonb="$(psql "$POSTGRES_URL" -qtAc "select count(*)::int from information_schema.columns where table_name='task_events' and column_name='payload_jsonb'")"
./scripts/phase28_smoke_one.sh:12:HAS_PAYLOAD="$(psql "$POSTGRES_URL" -qtAc "select count(*)::int from information_schema.columns where table_name='tasks' and column_name='payload'")"
./scripts/phase28_smoke_one.sh:58:  from tasks
./scripts/phase28_smoke_one.sh:94:from tasks
./scripts/phase28_smoke_one.sh:108:select count(*)
./scripts/phase50_smoke_db_writepath_enforce.sh:32:t0="$(sql "select count(*) from tasks")"
./scripts/phase50_smoke_db_writepath_enforce.sh:33:e0="$(sql "select count(*) from task_events")"
./scripts/phase50_smoke_db_writepath_enforce.sh:42:t1="$(sql "select count(*) from tasks")"
./scripts/phase50_smoke_db_writepath_enforce.sh:43:e1="$(sql "select count(*) from task_events")"
./scripts/tests/verify-demo-readiness.sh:7:sqlite3 db/main.db "SELECT COUNT(*) FROM reflection_index;" && echo "✅ SQLite accessible" || echo "⚠️ SQLite check failed."
./scripts/phase32_null_max_attempts_claim_smoke.sh:27:  SELECT count(*) INTO q FROM tasks WHERE status='queued';
./scripts/phase51_smoke_http_enforce_403.sh:20:t0="$(sql "select count(*) from tasks")"
./scripts/phase51_smoke_http_enforce_403.sh:21:e0="$(sql "select count(*) from task_events")"
./scripts/phase51_smoke_http_enforce_403.sh:34:t1="$(sql "select count(*) from tasks")"
./scripts/phase51_smoke_http_enforce_403.sh:35:e1="$(sql "select count(*) from task_events")"
./scripts/phase52_smoke_api_tasks_create_201.sh:20:t0="$(sql "select count(*) from tasks")"
./scripts/phase52_smoke_api_tasks_create_201.sh:21:e0="$(sql "select count(*) from task_events")"
./scripts/phase52_smoke_api_tasks_create_201.sh:34:t1="$(sql "select count(*) from tasks")"
./scripts/phase52_smoke_api_tasks_create_201.sh:35:e1="$(sql "select count(*) from task_events")"
./scripts/phase39_action_tier_smoke.sh:114:SELECT COUNT(*)::text FROM u;
./scripts/phase39_action_tier_smoke.sh:131:SELECT COUNT(*)::text
./scripts/phase33_smoke.sh:53:  PSQLRC=/dev/null psql -X -q "$POSTGRES_URL" -v ON_ERROR_STOP=1 -c "select count(*) as tasks_total from tasks;" || true
./scripts/phase33_smoke.sh:54:  PSQLRC=/dev/null psql -X -q "$POSTGRES_URL" -v ON_ERROR_STOP=1 -c "select kind, count(*) from task_events group by kind order by count(*) desc;" || true
./scripts/phase19_task_events_smoke.cjs:31:    const sel = await client.query("select count(*)::bigint as count from public.task_events;");
./scripts/phase34_reclaim_kill_verify.sh:49:CB="$("${PSQL_BASE[@]}" -Atc "select claimed_by from tasks where id=$TASK_ID;")"
./scripts/phase34_reclaim_kill_verify.sh:60:CB2="$("${PSQL_BASE[@]}" -Atc "select coalesce(claimed_by,'') from tasks where id=$TASK_ID;")"
./scripts/phase34_reclaim_kill_verify.sh:61:ST2="$("${PSQL_BASE[@]}" -Atc "select status from tasks where id=$TASK_ID;")"
./scripts/phase34_reclaim_kill_verify.sh:71:CB3="$("${PSQL_BASE[@]}" -Atc "select claimed_by from tasks where id=$TASK_ID;")"
./scripts/phase39_value_gate_smoke.sh:84:SELECT count(*) AS task_events_for_task
./scripts/phase39_value_gate_smoke.sh:98:SELECT count(*) AS task_events_for_task
./scripts/phase36_3_probe_run_view.sh:45:SELECT task_status, COUNT(*) AS n
./scripts/phase36_3_probe_run_view.sh:55:SELECT last_event_kind, COUNT(*) AS n
./scripts/verify-dashboard-layout-contract.sh:20:def expect_count(token: str, expected: int) -> None:
./scripts/verify-dashboard-layout-contract.sh:37:    expect_count(token, 1)
./scripts/phase27_fix_claim_one_sql.sh:29:  from tasks
./scripts/phase27_fix_claim_one_sql.sh:30:  where status = 'queued'
./scripts/phase32_verify.sh:32:from tasks
./scripts/_local/phase62b_tasks_queued_discovery.sh:54:  "COUNT\\(\\*\\)|count\\(|select .*from tasks|from tasks|where status" \
./scripts/_local/phase61_5_5h_restore_truthful_unknown_agent_dot.sh:16:count = text.count(yellow)
./scripts/verify-phase62-layout-contract.sh:20:def count(needle: str) -> int:
./scripts/verify-phase62-layout-contract.sh:21:    return text.count(needle)
./scripts/verify-phase62-layout-contract.sh:25:    c = count(needle)
./scripts/phase27_wire_worker_backoff.sh:137:    from tasks
./scripts/phase27_wire_worker_backoff.sh:165:      from tasks
./scripts/sql/invariants/phase55_terminal_event_precedence.sql:166:    SELECT COUNT(*) FROM mismatches
./scripts/sql/invariants/phase55_terminal_event_precedence.sql:179:    SELECT COUNT(*)
./server.mjs:345:       from tasks
./server.mjs:412:         from tasks
./server.mjs:437:         from tasks
./.git/logs/HEAD:283:af93d074a52baadf39eeb90e359abe4bdbfe1b75 9b0ec7bce4710e521abd554ab7a758dc96b24662 Marcela Gama Le <marcelagamale@gmail.com> 1773507817 -0700	commit: Phase 65: bootstrap shared metrics from tasks API and fix success metric hook
./.git/logs/HEAD:284:9b0ec7bce4710e521abd554ab7a758dc96b24662 6e55564fda8e02530c081172b4b5678907874888 Marcela Gama Le <marcelagamale@gmail.com> 1773507884 -0700	commit: Phase 65: bootstrap shared metrics from tasks API and fix success metric hook
./.git/logs/refs/heads/phase63-telemetry-enrichment:175:af93d074a52baadf39eeb90e359abe4bdbfe1b75 9b0ec7bce4710e521abd554ab7a758dc96b24662 Marcela Gama Le <marcelagamale@gmail.com> 1773507817 -0700	commit: Phase 65: bootstrap shared metrics from tasks API and fix success metric hook
./.git/logs/refs/heads/phase63-telemetry-enrichment:176:9b0ec7bce4710e521abd554ab7a758dc96b24662 6e55564fda8e02530c081172b4b5678907874888 Marcela Gama Le <marcelagamale@gmail.com> 1773507884 -0700	commit: Phase 65: bootstrap shared metrics from tasks API and fix success metric hook
./ts-backup/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./ts-backup/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./PHASE40_ACCEPTANCE_CHECKS.sql:36:  SELECT count(*) AS n
./scripts_backup_2/reset-roundtrip.ts:18:    const checkTasks = sqlite.prepare("SELECT COUNT(*) as c FROM task_events").get();
./scripts_backup_2/reset-roundtrip.ts:19:    const checkReflections = sqlite.prepare("SELECT COUNT(*) as c FROM reflection_index").get();
./scripts_backup_2/tests/verify-demo-readiness.sh:7:sqlite3 db/main.db "SELECT COUNT(*) FROM reflection_index;" && echo "✅ SQLite accessible" || echo "⚠️ SQLite check failed."
./sql/phase41_invariants_run_view.sql:22:    ('dup=' || COUNT(*)::text) AS detail
./sql/phase41_invariants_run_view.sql:25:  HAVING COUNT(*) <> 1
./sql/policy/action_tier_validate.sql:116:    SELECT 'PHASE39_UNMAPPED_COUNT=' || COUNT(*)::text AS phase39_unmapped_count

-- task schema candidates --
FILE: ./cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./drizzle_pg/0009_phase39_db_repair_notes_failedat_taskid.sql
13:    WHERE table_schema='public' AND table_name='tasks' AND column_name='notes'
22:    WHERE table_schema='public' AND table_name='tasks' AND column_name='failed_at'
46:  -- tasks_failed_at_matches_status (phase31_7 guard expects failed_at)
49:    WHERE conname='tasks_failed_at_matches_status'
52:    ADD CONSTRAINT tasks_failed_at_matches_status
54:      (status = 'failed' AND failed_at IS NOT NULL)
56:      (status <> 'failed' AND failed_at IS NULL)

FILE: ./drizzle_pg/0001_phase31_7_failed_at_guard.sql
1:-- phase31.7: enforce failed_at ↔ status invariant at DB layer
5:  if new.status = 'failed' then
19:alter table tasks
20:drop constraint if exists tasks_failed_at_matches_status;
21:alter table tasks
22:add constraint tasks_failed_at_matches_status
24:  (status = 'failed' and failed_at is not null)
26:  (status <> 'failed' and failed_at is null)

FILE: ./drizzle_pg/0007_phase34_lease_heartbeat_reclaim.sql
5:  IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='tasks' AND column_name='claimed_by') THEN
9:  IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='tasks' AND column_name='claimed_at') THEN
13:  IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='tasks' AND column_name='lease_expires_at') THEN
17:  IF NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name='tasks' AND column_name='lease_epoch') THEN
33:    WHERE schemaname='public' AND tablename='tasks' AND indexname='tasks_lease_expires_at_idx'

FILE: ./snapshots/20251114-114821/public/js/dashboard-graph-loader.js
7:    if (!res.ok) throw new Error(`HTTP ${res.status}`);

FILE: ./snapshots/20251114-114821/public/dashboard-tabs.backup.js
23:      div.className = `task ${t.status}`;
24:      if (t.status === "complete" && t.endTime) {
27:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
107:      div.textContent = `${agent.name} — ${agent.status}`;
109:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./snapshots/20251114-114821/public/dashboard.js

FILE: ./snapshots/20251114-114821/public/dashboard-tabs.js
24:      div.className = `task ${t.status}`;
25:      if (t.status === "complete" && t.endTime) {
28:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
109:      div.textContent = `${agent.name} — ${agent.status}`;
111:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./snapshots/20251114-114821/scripts/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./snapshots/20251114-114821/scripts/_local/route-loader/ops-sse-server.ts
18:  res.write(`data: {"status":"connected"}\n\n`);
31:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./snapshots/20251114-114821/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;

FILE: ./snapshots/20251114-114821/routes/api/tasks.ts
10:      "SELECT id, type, agent, status, payload, result, created_at FROM task_events ORDER BY created_at DESC LIMIT 20"
16:    res.status(500).json({ ok: false, error: "task_fetch_error" });

FILE: ./snapshots/20251114-114821/routes/tasks.ts

FILE: ./snapshots/20251114-114707/public/js/dashboard-graph-loader.js
7:    if (!res.ok) throw new Error(`HTTP ${res.status}`);

FILE: ./snapshots/20251114-114707/public/dashboard-tabs.backup.js
23:      div.className = `task ${t.status}`;
24:      if (t.status === "complete" && t.endTime) {
27:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
107:      div.textContent = `${agent.name} — ${agent.status}`;
109:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./snapshots/20251114-114707/public/dashboard.js

FILE: ./snapshots/20251114-114707/public/dashboard-tabs.js
24:      div.className = `task ${t.status}`;
25:      if (t.status === "complete" && t.endTime) {
28:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
109:      div.textContent = `${agent.name} — ${agent.status}`;
111:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./snapshots/20251114-114707/scripts/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./snapshots/20251114-114707/scripts/_local/route-loader/ops-sse-server.ts
18:  res.write(`data: {"status":"connected"}\n\n`);
31:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./snapshots/20251114-114707/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;

FILE: ./snapshots/20251114-114707/routes/api/tasks.ts
10:      "SELECT id, type, agent, status, payload, result, created_at FROM task_events ORDER BY created_at DESC LIMIT 20"
16:    res.status(500).json({ ok: false, error: "task_fetch_error" });

FILE: ./snapshots/20251114-114707/routes/tasks.ts

FILE: ./add_sample_tasks.js
4:const filePath = './memory/agent_chain_state.json';
23:        status: 'Backlog - Set up UI',
32:        status: 'In Progress - Connect API',
41:        status: 'Completed - Initial commit',

FILE: ./effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./server/worker/phase29_db_migrate_to_phase27.sql
4:alter table tasks add column if not exists payload jsonb null;
5:alter table tasks add column if not exists attempts integer not null default 0;
6:alter table tasks add column if not exists next_run_at timestamptz null;
11:  if exists (select 1 from information_schema.columns where table_name='tasks' and column_name='meta')
12:     and exists (select 1 from information_schema.columns where table_name='tasks' and column_name='payload') then
22:  if exists (select 1 from information_schema.columns where table_name='tasks' and column_name='attempt') then
32:  if exists (select 1 from information_schema.columns where table_name='tasks' and column_name='available_at') then
68:create index if not exists idx_tasks_status_next_run on tasks(status, next_run_at);

FILE: ./server/worker/phase30_contract_check.sql
5:  if not exists (select 1 from information_schema.columns where table_name='tasks' and column_name='payload') then
8:  if not exists (select 1 from information_schema.columns where table_name='tasks' and column_name='attempts') then
11:  if not exists (select 1 from information_schema.columns where table_name='tasks' and column_name='next_run_at') then

FILE: ./server/sql/phase54_bootstrap.sql
10:-- Ensure tasks table exists (some repos create tasks outside drizzle_pg; if yours does too, this will be a no-op if already present elsewhere)

FILE: ./server/sql/phase47_decision_correctness_invariants.sql
15:  col_status      text;
24:    RAISE EXCEPTION 'Phase47 invariant failure: missing table public.tasks';
32:  WHERE table_schema='public' AND table_name='tasks' AND column_name='task_id'
64:  SELECT column_name INTO col_status
66:  WHERE table_schema='public' AND table_name='tasks' AND column_name IN ('task_status','status')
67:  ORDER BY CASE WHEN column_name='task_status' THEN 0 ELSE 1 END
72:  WHERE table_schema='public' AND table_name='tasks' AND column_name IN ('is_terminal','terminal')
78:  WHERE table_schema='public' AND table_name='tasks' AND column_name IN ('lease_owner','lease_actor','lease_holder')
88:  WHERE table_schema='public' AND table_name='tasks' AND column_name IN ('lease_expires_at','lease_until','lease_expires_ts')
98:  WHERE table_schema='public' AND table_name='tasks' AND column_name IN ('last_heartbeat_ts','heartbeat_ts','last_heartbeat_ms')
127:  IF col_status IS NOT NULL AND col_lease_owner IS NOT NULL AND col_lease_exp IS NOT NULL THEN
138:    $q$, col_status, col_lease_owner, col_lease_exp)
148:SELECT 'OK: Phase 47 decision correctness invariants clean.' AS phase47_status;

FILE: ./cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./scripts_backup/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./scripts_backup/scripts/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./scripts_backup/scripts/_local/route-loader/ops-sse-server.ts
22:  res.write(`data: {"status":"connected"}\n\n`);
35:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup/scripts/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./scripts_backup/scripts/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup/scripts/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;

FILE: ./scripts_backup/_local/route-loader/ops-sse-server.ts
22:  res.write(`data: {"status":"connected"}\n\n`);
35:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./scripts_backup/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./scripts_backup/_local/agent-runtime/utils/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./scripts_backup/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;

FILE: ./docker-entrypoint-initdb.d/00_phase54_bootstrap.sql
10:  status text,

FILE: ./exports/dashboard_phase6.8/js/dashboard-ops.js

FILE: ./exports/dashboard_phase6.8/js/dashboard-graph-loader.js
7:    if (!res.ok) throw new Error(`HTTP ${res.status}`);

FILE: ./public/js/dashboard-tasks-widget.js
25:  const state = {
62:    if (!state.shellMounted) {
70:      state.shellMounted = true;
99:      ui.errorEl.innerHTML = state.lastError
100:        ? `<div style="color:red">${esc(state.lastError)}</div>`
107:          ${state.loading && !state.tasks.length ? `<div style="opacity:.7">Loading…</div>` : ""}
109:            state.tasks.length
110:              ? state.tasks
116:                  ["complete", "completed", "done"].includes(String(t.status || "").toLowerCase())
124:              : !state.loading
136:    if (ui.runsHost && !state.runsPanelMounted) {
139:        state.runsPanelMounted = true;
147:    if (state.taskFetchPromise) return state.taskFetchPromise;
149:    state.taskFetchPromise = (async () => {
150:      state.loading = true;
154:        state.tasks = (data.tasks || []).map((t) => ({
157:          status: t.status || "",
159:        state.lastError = null;
161:        state.lastError = e?.message || String(e);
163:        state.loading = false;
165:        state.taskFetchPromise = null;
169:    return state.taskFetchPromise;
173:    if (state.taskFetchTimer) clearTimeout(state.taskFetchTimer);
174:    state.taskFetchTimer = setTimeout(() => {
175:      state.taskFetchTimer = null;
181:    if (!taskId || state.inflightComplete.has(taskId)) return;
182:    state.inflightComplete.add(taskId);
190:      state.lastError = null;
192:      state.lastError = e?.message || String(e);
194:      state.inflightComplete.delete(taskId);
199:  function buildRunsQuery({ limit, task_status, is_terminal, since_ts }) {
202:    for (const s of task_status || []) p.append("task_status", String(s));
208:  async function fetchRuns({ limit, task_status, is_terminal, since_ts }) {
209:    const q = buildRunsQuery({ limit, task_status, is_terminal, since_ts });
213:    if (!r.ok) throw new Error(j?.error || j?.detail || "HTTP_" + r.status);
263:        <span>task_status</span>
264:        <select id="runs-status" class="select" multiple size="4">
296:    const statusLine = document.createElement("div");
297:    statusLine.style.fontFamily = "monospace";
298:    statusLine.style.fontSize = "12px";
299:    statusLine.style.opacity = "0.8";
300:    statusLine.style.margin = "6px 0";
301:    body.appendChild(statusLine);
314:          <th>task_status</th>
332:    const $status = wrap.querySelector("#runs-status");
341:      const task_status = Array.from($status.selectedOptions || []).map((o) => o.value);
354:      return { limit, task_status, is_terminal, since_ts };
364:          <td>${esc(String(r.task_status ?? ""))}</td>
382:      statusLine.textContent = "GET /api/runs" + (q ? "?" + q : "");
389:        statusLine.textContent += `  → count=${j.count ?? (j.rows?.length ?? 0)}`;
391:        statusLine.textContent += `  → ERROR: ${String(e?.message || e)}`;
402:    $status.addEventListener("change", refresh);
406:    if (state.runsPanelTimer) clearInterval(state.runsPanelTimer);
407:    state.runsPanelTimer = setInterval(() => {

FILE: ./public/js/heartbeat-stale-indicator.js
2: * Phase 16 – Heartbeat stale-state indicator (non-intrusive)
38:    el.setAttribute("role", "status");

FILE: ./public/js/dashboard-graph-loader.js
11:throw new Error("HTTP " + res.status);

FILE: ./public/js/sse-heartbeat-shim.js
16:    const state = { ops: null, tasks: null, reflections: null, unknown: null };
19:        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : "unknown";
20:        state[k] = typeof ts === "number" ? ts : Date.now();
21:        return state[k];
24:        const k = Object.prototype.hasOwnProperty.call(state, kind) ? kind : "unknown";
25:        return state[k];
28:        return { ...state };

FILE: ./public/js/phase22_task_delegation_live_bindings.js
2:  // Phase 22: bind task-events SSE -> Task Delegation + status UI (best-effort)
12:    queued: "task-status-queued",
13:    done: "task-status-done",
14:    failed: "task-status-failed",
19:    if (v === "queued" || v === "pending") return "queued";
58:  function isTerminalStatus(status) {
59:    const v = normStatus(status);
71:  function isSuccessStatus(status) {
72:    const v = normStatus(status);
76:  function isFailureStatus(status) {
77:    const v = normStatus(status);
81:  function isRunningStatus(status) {
82:    const v = normStatus(status);
84:      v === "queued" ||
85:      v === "pending" ||
99:    const status =
100:      task?.status ??
101:      ev?.status ??
102:      ev?.payload?.status ??
103:      ev?.task?.status ??
111:    if (isTerminalKind(kind) || isTerminalStatus(status)) {
117:    if (isRunningKind(kind) || isRunningStatus(status)) {
127:    const status =
128:      task?.status ??
129:      ev?.status ??
130:      ev?.payload?.status ??
131:      ev?.task?.status ??
136:    if (isFailureKind(kind) || isFailureStatus(status)) return;
138:    if (isSuccessKind(kind) || isSuccessStatus(status)) {
148:    const status =
149:      task?.status ??
150:      ev?.status ??
151:      ev?.payload?.status ??
152:      ev?.task?.status ??
157:    if (isSuccessKind(kind) || isSuccessStatus(status)) return;
159:    if (isFailureKind(kind) || isFailureStatus(status)) {
171:    const status =
172:      ev?.status ??
173:      ev?.payload?.status ??
174:      t?.status ??
175:      (ev?.kind === "task.created" ? "queued" : null);
179:      status: status != null ? normStatus(status) : null,
187:  function setStatusOnNode(node, status) {
189:    const s = normStatus(status);
190:    node.setAttribute("data-task-status", s);
195:      node.querySelector?.("[data-task-field='status']") ||
196:      node.querySelector?.(".task-status") ||
197:      node.querySelector?.(".status") ||
212:    for (const n of nodes) setStatusOnNode(n, task.status);
226:    let queued = 0;
231:      const s = normStatus(t.status);
232:      if (s === "queued") queued++;
237:    updateCounterNode("queued", queued);
249:    const next = { ...prev, ...task, id, status: task.status ?? prev.status };
259:      if (ev.kind === "task.completed") t.status = "done";
260:      if (ev.kind === "task.failed") t.status = "failed";

FILE: ./public/bundle.js
1:(()=>{(function(){let p=window,u="__HB";if(!p[u]){let c={ops:null,tasks:null,reflections:null,unknown:null};p[u]={record(w,v){let k=Object.prototype.hasOwnProperty.call(c,w)?w:"unknown";return c[k]=typeof v=="number"?v:Date.now(),c[k]},get(w){let v=Object.prototype.hasOwnProperty.call(c,w)?w:"unknown";return c[v]},snapshot(){return{...c}}}}let b=p.EventSource;if(!b||b.__hbWrapped)return;function S(c){let w=String(c||"");return w.includes("/events/ops")?"ops":w.includes("/events/task-events")||w.includes("/events/tasks")?"tasks":w.includes("/events/reflections")?"reflections":"unknown"}function i(c,w){let v=S(c);try{p[u].record(v,Date.now())}catch{}let k=new b(c,w),l=()=>{try{p[u].record(v,Date.now())}catch{}};try{k.addEventListener("open",l)}catch{}try{k.addEventListener("message",l)}catch{}let I=null;Object.defineProperty(k,"onmessage",{get(){return I},set(y){I=function(s){if(l(),typeof y=="function")return y.call(k,s)}},configurable:!0});try{k.addEventListener("error",l)}catch{}return k}i.prototype=b.prototype,i.__hbWrapped=!0,p.EventSource=i})();(function(){let u=window.__HB;function b(){return Date.now()}function S(l){return Math.max(0,Number(l)||0)}let i=15e3;function c(l){if(!l)return"\u2014";let I=Math.floor((b()-l)/1e3);return I<=0?"0s":`${I}s`}function w(){let l=document.getElementById("hb-badge");return l||(l=document.createElement("div"),l.id="hb-badge",l.setAttribute("role","status"),l.style.position="fixed",l.style.top="12px",l.style.right="12px",l.style.zIndex="9999",l.style.fontFamily="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial",l.style.fontSize="12px",l.style.padding="6px 10px",l.style.borderRadius="999px",l.style.border="1px solid rgba(255,255,255,0.14)",l.style.background="rgba(0,0,0,0.55)",l.style.backdropFilter="blur(6px)",l.style.webkitBackdropFilter="blur(6px)",l.style.color="rgba(255,255,255,0.92)",l.style.boxShadow="0 8px 18px rgba(0,0,0,0.35)",l.style.userSelect="none",l.style.cursor="default",document.body.appendChild(l),l)}function v(l,I){l.textContent=I?`HB \u2713 (ops ${c(u&&u.get("ops"))}, tasks ${c(u&&u.get("tasks"))})`:`HB ! (ops ${c(u&&u.get("ops"))}, tasks ${c(u&&u.get("tasks"))})`}function k(){let l=w();if(!u||typeof u.get!="function"){l.textContent="HB ? (shim not loaded)";return}let I=u.get("ops"),y=u.get("tasks"),s=!!I&&S(b()-I)<=i,r=!!y&&S(b()-y)<=i;v(l,s&&r)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",()=>{k(),setInterval(k,1e3)}):(k(),setInterval(k,1e3))})();(()=>{"use strict";let p="/events/ops",u="/events/reflections",b=()=>Date.now();function S(a){try{return JSON.parse(a)}catch{return null}}function i(a){if(!Number.isFinite(a))return"\u2014";let d=Math.floor(a/1e3);if(d<60)return`${d}s`;let x=Math.floor(d/60);return x<60?`${x}m`:`${Math.floor(x/60)}h`}function c(a,d={},x=""){let f=document.createElement(a);for(let[t,n]of Object.entries(d))t==="class"?f.className=n:t==="style"?f.setAttribute("style",n):f.setAttribute(t,n);return x&&(f.textContent=x),f}function w(){if(document.getElementById("phase16-sse-style"))return;let a=c("style",{id:"phase16-sse-style"});a.textContent=`
20:    `,document.head.appendChild(a)}function v(a,d,x){if(w(),!a){let t=document.getElementById("phase16-sse-tray");t||(t=c("div",{id:"phase16-sse-tray",style:["position:fixed","left:10px","bottom:10px","display:flex","flex-direction:column","gap:6px","z-index:9999","pointer-events:none"].join(";")}),document.body.appendChild(t)),a=t}let f=document.getElementById(d);if(f)return f;f=c("span",{id:d,class:"sse-indicator","data-connected":"false"}),f.append(c("span",{class:"dot","aria-hidden":"true"}),c("span",{class:"meta"},`${x}: disconnected \xB7 last: \u2014`));try{if(a.matches&&a.matches("header,h1,h2,h3,h4,strong")){let t=c("span",{style:"margin-left:8px"});t.appendChild(f),a.appendChild(t)}else{let t=c("div",{style:"margin-top:4px"});t.appendChild(f),a.appendChild(t)}}catch{a.appendChild(f)}return f}function k(a,d,x,f){if(!a)return;a.dataset.connected=x?"true":"false";let t=a.querySelector(".meta");t&&(t.textContent=`${d}: ${x?"connected":"disconnected"} \xB7 last: ${f?i(b()-f):"\u2014"}`)}function l(){return window.__MB_STREAMS||(window.__MB_STREAMS={ops:{connected:!1,lastAt:0,state:{},es:null},reflections:{connected:!1,lastAt:0,state:{},es:null}}),window.__MB_STREAMS}function I(a,d){return(!a||typeof a!="object")&&(a={}),!d||typeof d!="object"?a:Object.assign(a,d)}function y(a,d){(!a||typeof a!="object")&&(a={});let x=d&&typeof d.path=="string"?d.path:"";if(!x)return a;let f=x.split(".").filter(Boolean);if(!f.length)return a;let t=a;for(let n=0;n<f.length-1;n++){let o=f[n];(!t[o]||typeof t[o]!="object")&&(t[o]={}),t=t[o]}return t[f[f.length-1]]=d.value,a}function s(a,d){return!!(typeof a=="string"&&a.endsWith(".state")||d&&typeof d=="object"&&(d.state&&typeof d.state=="object"||typeof d.type=="string"&&d.type.includes("state")||typeof d.event=="string"&&d.event.includes("state")))}function r(a){return!a||typeof a!="object"?a:a.payload!==void 0?a.payload:a.data!==void 0?a.data:a.delta!==void 0?a.delta:a.patch!==void 0?a.patch:a.state!==void 0?a.state:a}function h(a,d,x,f){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return null;let t=l();try{t[a].es&&t[a].es.close()}catch{}t[a].es=null;let n=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(x);if(t[a].es=n,!n)return null;let o=()=>k(f,d,t[a].connected,t[a].lastAt);n.onopen=()=>{t[a].connected=!0,o()},n.onerror=()=>{t[a].connected=!1,o()};let g=(m,_)=>{t[a].lastAt=b();let C=S(_&&_.data?_.data:""),L=r(C);s(m,C)?t[a].state=L&&typeof L=="object"?L:{value:L}:L&&typeof L=="object"?typeof L.path=="string"&&"value"in L?t[a].state=y(t[a].state,L):t[a].state=I(t[a].state,L):L!=null&&(t[a].state=I(t[a].state,{lastValue:L})),o();try{window.dispatchEvent(new CustomEvent(`mb:${a}:update`,{detail:{event:m,state:t[a].state,raw:C}}))}catch{}};n.onmessage=m=>g("message",m);let e=["hello",`${a}.state`,`${a}.update`,`${a}.patch`,`${a}.delta`,"state","update","patch","delta"];for(let m of e)try{n.addEventListener(m,_=>g(m,_))}catch{}o()}function E(){return document.getElementById("ops-pill")||document.querySelector("[data-widget='ops-pill']")||document.querySelector(".ops-pill")||document.querySelector("#ops")||null}function T(){return document.getElementById("reflections-header")||document.getElementById("reflections")||document.querySelector("[data-panel='reflections']")||document.querySelector(".reflections")||Array.from(document.querySelectorAll("h1,h2,h3,h4,header,strong")).find(d=>(d.textContent||"").toLowerCase().includes("reflections"))||null}function A(){let a=v(E(),"ops-sse-indicator","OPS SSE"),d=v(T(),"reflections-sse-indicator","Reflections SSE");h("ops","OPS SSE",p,a),h("reflections","Reflections SSE",u,d),setInterval(()=>{let x=l();k(a,"OPS SSE",x.ops.connected,x.ops.lastAt),k(d,"Reflections SSE",x.reflections.connected,x.reflections.lastAt)},1e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",A,{once:!0}):A()})();(()=>{let p=document.getElementById("agent-status-container");if(!p){console.warn("agent-status-row.js: #agent-status-container not found.");return}let u=p.querySelector("h2");p.innerHTML="",u&&p.appendChild(u);let b=["Matilda","Atlas","Cade","Effie"],S={matilda:"\u{1F5E3}\uFE0F",atlas:"\u{1F9ED}",cade:"\u{1F4BB}",effie:"\u{1F4CA}"},i={},c=Object.create(null),w=60*1e3,v=document.getElementById("metric-agents");function k(t,n){t&&(t.textContent=n)}function l(t){if(typeof t=="number"&&Number.isFinite(t))return t;if(typeof t=="string"&&t.trim()){let n=Number(t);if(Number.isFinite(n))return n;let o=Date.parse(t);if(Number.isFinite(o))return o}return null}function I(){if(!v)return;let t=Date.now(),n=0;for(let o of b){let g=o.toLowerCase(),e=l(c[g]);e!=null&&t-e<=w&&(n+=1)}k(v,String(n))}k(v,"\u2014");let y=document.createElement("div");y.className="w-full flex flex-col gap-0.5",p.appendChild(y),b.forEach(t=>{let n=t.toLowerCase(),o=document.createElement("div");o.className="w-full min-h-0 rounded-md bg-slate-600/55 border border-slate-500/35 px-3 py-1.5 flex items-center justify-between shadow-sm";let g=document.createElement("div");g.className="flex items-center gap-3 min-w-0 h-[18px]";let e=document.createElement("span");e.className="inline-flex items-center justify-center shrink-0",e.textContent=S[n]||"\u2022",e.style.width="18px",e.style.minWidth="18px",e.style.height="18px",e.style.minHeight="18px",e.style.fontSize="14px",e.style.lineHeight="1",e.style.background="transparent",e.style.borderRadius="0",e.style.boxShadow="none",e.style.marginRight="0";let m=document.createElement("span");m.className="text-[13px] font-semibold tracking-tight text-slate-100/95 truncate",m.textContent=t;let _=document.createElement("span");_.className="text-[12px] font-medium text-slate-200/90 truncate",_.textContent="unknown",g.append(e,m),o.append(g,_),y.appendChild(o),i[n]={row:o,emoji:e,label:m,status:_}});let s="/events/ops",r=(typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0;function h(t){if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED)return"unknown";let n=(t||"").toLowerCase();return n?n.includes("error")||n.includes("failed")||n.includes("offline")?"error":n.includes("online")||n.includes("ready")||n.includes("ok")?"online":n.includes("queued")||n.includes("pending")||n.includes("init")||n.includes("running")?"pending":"unknown":"unknown"}function E(t,n){let o=i[t];if(!o)return;let g=h(n),e=n||"unknown";switch(o.status.textContent=e,o.row.className="w-full min-h-0 rounded-md border px-3 py-1.5 flex items-center justify-between shadow-sm",o.emoji.className="inline-flex items-center justify-center shrink-0",o.emoji.textContent=S[t]||"\u2022",o.emoji.style.display="inline-flex",o.emoji.style.width="18px",o.emoji.style.minWidth="18px",o.emoji.style.height="18px",o.emoji.style.minHeight="18px",o.emoji.style.fontSize="14px",o.emoji.style.lineHeight="1",o.emoji.style.background="transparent",o.emoji.style.borderRadius="0",o.emoji.style.boxShadow="none",o.emoji.style.marginRight="0",o.label.className="text-[13px] font-semibold tracking-tight truncate",o.status.className="text-[11px] font-medium truncate",g){case"online":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-emerald-300/90");break;case"error":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-rose-300/90");break;case"pending":o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-amber-200/90");break;default:o.row.classList.add("bg-gray-900","border-gray-700"),o.label.classList.add("text-slate-100"),o.status.classList.add("text-slate-300/75");break}}if(Object.keys(i).forEach(t=>E(t,"unknown")),r){console.warn("[agent-status-row] Optional SSE disabled:",s);return}function T(t){try{return JSON.parse(t)}catch{return null}}function A(t){return!t||typeof t!="object"?null:t.payload&&typeof t.payload=="object"?t.payload:t.data&&typeof t.data=="object"?t.data:t.state&&typeof t.state=="object"?t.state:t}function a(t){if(!t||typeof t!="object")return!1;let n=t.agents;if(!n||typeof n!="object")return!1;let o=!1;for(let[g,e]of Object.entries(n)){let m=String(g||"").toLowerCase();if(!i[m])continue;let _="unknown";if(typeof e=="string")_=e;else if(e&&typeof e=="object"){_=e.status??e.state??e.level??e.health??e.mode??"unknown";let C=e.at??e.ts??e.last_activity??e.lastActivity??e.last_seen??e.lastSeen??null;C!=null&&(c[m]=C)}E(m,String(_||"unknown")),o=!0}return I(),o}function d(t){if(!t||typeof t!="object")return!1;let n=(t.agent||t.actor||t.source||t.worker||t.name||"").toString().toLowerCase();if(!n||!i[n])return!1;let o=t.status??t.state??t.level??t.health??t.mode??"unknown",g=t.at??t.ts??t.last_activity??t.lastActivity??t.last_seen??t.lastSeen??null;return g!=null&&(c[n]=g),E(n,String(o||"unknown")),I(),!0}function x(t,n){let o=T(n.data);if(!o)return;let g=A(o);t==="ops.state"&&a(g)||a(o)||a(g)||(d(g),d(o))}let f;try{f=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(s)}catch(t){console.error("agent-status-row.js: Failed to open OPS SSE connection:",t);return}f&&(f.onmessage=t=>x("message",t),f.addEventListener("hello",t=>x("hello",t)),f.addEventListener("ops.state",t=>x("ops.state",t)),f.addEventListener("state",t=>x("state",t)),f.addEventListener("update",t=>x("update",t)),f.onerror=t=>{console.warn("agent-status-row.js: OPS SSE error:",t),Object.keys(i).forEach(n=>E(n,"unknown")),k(v,"\u2014")})})();var D="__broadcastVisualizationInited",j=["Matilda","Cade","Effie"];function R(){let p=document.getElementById("broadcast-visual");if(!p)return;let u=[];for(let b=0;b<j.length;b++){let S=j[b];u.push('<div class="node" id="node-'+S+'">'+S+"</div>"),b<j.length-1&&u.push('<div class="arrow">\u279C</div>')}p.innerHTML=u.join("")}function $(){let p=0;setInterval(()=>{document.querySelectorAll(".node").forEach(i=>i.classList.remove("active"));let b="node-"+j[p],S=document.getElementById(b);S&&S.classList.add("active"),p=(p+1)%j.length},1500)}function H(){if(typeof window>"u"||typeof document>"u"||window[D])return;window[D]=!0;let p=()=>{R(),$()};document.readyState==="loading"?window.addEventListener("DOMContentLoaded",p):p()}typeof window<"u"&&(window.initBroadcastVisualization=H);(function(){if(!(typeof document>"u")){var p=document.getElementById("ops-dashboard-pill");if(!p){var u=document.querySelector("[data-ops-pill]");u&&(u.id="ops-dashboard-pill")}}})();(()=>{if(typeof window<"u"&&window.__PHASE16_SSE_OWNER_STARTED||typeof window>"u"||typeof EventSource>"u"||window.__opsGlobalsBridgeInitialized)return;window.__opsGlobalsBridgeInitialized=!0,typeof window.lastOpsHeartbeat>"u"&&(window.lastOpsHeartbeat=null),typeof window.lastOpsStatusSnapshot>"u"&&(window.lastOpsStatusSnapshot=null);let p="/events/ops";if((typeof window<"u"&&window.__DISABLE_OPTIONAL_SSE)===!0){console.warn("[ops-globals-bridge] Optional SSE disabled (Phase 16 pending):",p);return}let b=S=>{try{let i=JSON.parse(S.data||"null");if(!i)return;window.lastOpsHeartbeat=Math.floor(Date.now()/1e3),window.lastOpsStatusSnapshot=i}catch(i){console.warn("[ops-globals-bridge] Failed to parse OPS event:",i)}try{window.dispatchEvent(new CustomEvent("mb:ops:update",{detail:{event:"message",state:window.lastOpsStatusSnapshot}}))}catch{}};try{let S=window.__PHASE16_SSE_OWNER_STARTED?null:new EventSource(p);if(!S)return null;if(S.onmessage=b,S.addEventListener("hello",b),!S)return;S.onerror=i=>{console.warn("[ops-globals-bridge] EventSource error:",i)}}catch(S){console.warn("[ops-globals-bridge] Failed to init EventSource:",S)}})();(function(){if(typeof window>"u"||typeof document>"u"||window.location.pathname!=="/dashboard")return;var p=5e3,u="ops-dashboard-pill";function b(){var i=document.getElementById(u);return i||(i=document.createElement("span"),i.id=u,i.className="ops-pill ops-pill-unknown",i.textContent="OPS: Unknown",i.style.display="inline-block",document.body.firstChild?document.body.insertBefore(i,document.body.firstChild):document.body.appendChild(i),i)}function S(){var i=document.getElementById("ops-status-pill");i&&(i.style.display="none");var c=b();if(c){var w=typeof window.lastOpsHeartbeat=="number",v=w?"OPS: Online":"OPS: Unknown",k=w?"ops-pill-online":"ops-pill-unknown";c.classList.remove("ops-pill-unknown","ops-pill-online","ops-pill-stale","ops-pill-error"),c.classList.add(k),c.textContent=v}}S(),setInterval(S,p)})();(function(){function p(i){console.log("[matilda-chat]",i)}function u(i,c,w){if(i){var v=document.createElement("p");v.className="mb-1 text-sm";var k=c?c+": ":"";v.textContent=k+w,i.appendChild(v),i.scrollTop=i.scrollHeight}}function b(i,c,w){i&&(i.disabled=w,i.classList.toggle("opacity-60",w),i.textContent=w?"Sending...":"Send"),c&&(c.disabled=w)}async function S(){var i=document.getElementById("matilda-chat-root");if(!i){p("No #matilda-chat-root found; skipping wiring.");return}var c=document.getElementById("matilda-chat-transcript"),w=document.getElementById("matilda-chat-input"),v=document.getElementById("matilda-chat-send");if(window.appendMessage=window.appendMessage||function(y){try{var s=y&&y.role?String(y.role):"system",r=y&&(y.content??y.text??y.message)?String(y.content??y.text??y.message):"",h=s==="user"?"You":s==="matilda"?"Matilda":"System";u(c,h,r)}catch{}},window.__appendMessage=window.__appendMessage||window.appendMessage,!c||!w||!v){p("Missing one or more Matilda chat elements; aborting wiring.");return}function k(y){return(y||"").toString().trim()}async function l(){var y=k(w.value);if(y){u(c,"You",y),w.value="",b(v,w,!0);try{var s=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:y,agent:"matilda"})});if(!s.ok){u(c,"Matilda","(error talking to /api/chat)");return}var r=await s.json(),h=r&&(r.reply||r.message||r.response)||"(no reply)";u(c,"Matilda",h)}catch(E){console.error(E),u(c,"Matilda","(network error)")}finally{b(v,w,!1)}}}v.addEventListener("click",l);var I=document.getElementById("matilda-chat-quick-check");I&&I.addEventListener("click",function(){w.value="Quick systems check from dashboard Phase 11.4.",l()}),w.addEventListener("keydown",function(y){y.key==="Enter"&&!y.shiftKey&&(y.preventDefault(),l())}),p("Matilda chat wiring complete.")}document.addEventListener("DOMContentLoaded",S)})();(function(){console.log("[dashboard-delegation] module loaded");function p(s){return document.getElementById(s)}function u(){var s=window.fetch,r=typeof s;return console.log("[dashboard-delegation] detected fetch type:",r),r!=="function"?(console.error("[dashboard-delegation] fetch is not a function; value:",s),null):s.bind(window)}function b(s){return String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;")}function S(s){return'<pre class="mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-black/20 p-3 text-xs text-gray-300">'+b(JSON.stringify(s,null,2))+"</pre>"}function i(s,r){var h=p("delegation-response"),E=p("delegation-status-panel");h&&(h.innerHTML=r,E&&(E.classList.remove("border-gray-700","border-teal-600","border-green-600","border-red-600","border-amber-500"),s==="sending"?E.classList.add("border-teal-600"):s==="success"?E.classList.add("border-green-600"):s==="error"?E.classList.add("border-red-600"):s==="waiting"?E.classList.add("border-amber-500"):E.classList.add("border-gray-700")))}function c(){i("idle","Awaiting operator input.<br>Results from delegation requests will appear here.")}function w(s){i("sending",'<div class="text-teal-300 font-medium">Sending delegation\u2026</div><div class="mt-2 text-gray-400">Preparing request for the orchestration layer.</div>'+(s?'<div class="mt-3 rounded-lg bg-black/20 p-3 text-xs text-gray-300 break-words">'+b(s)+"</div>":""))}function v(){i("waiting",'<div class="text-amber-300 font-medium">Still waiting on delegation response\u2026</div><div class="mt-2 text-gray-400">The request may still be processing.</div>')}function k(s){var r="Delegation accepted.";s&&typeof s=="object"&&(r=s.message||s.status||s.result||s.reply||s.ok&&"Delegation accepted."||r),i("success",'<div class="text-green-300 font-medium">'+b(r)+'</div><div class="mt-2 text-gray-400">Request completed successfully.</div>'+(s&&typeof s=="object"?S(s):""))}function l(s,r){i("error",'<div class="text-red-300 font-medium">Delegation failed.</div><div class="mt-2 text-gray-300">'+b(s||"Unknown error")+"</div>"+(r?'<div class="mt-3 text-xs text-gray-400 break-words">'+b(r)+"</div>":""))}async function I(){var s=p("delegation-input"),r=p("delegation-submit");if(!s){console.warn("[dashboard-delegation] delegation input not found at click time"),l("Delegation input field was not found.");return}var h=String(s.value||"").trim();if(!h){console.warn("[dashboard-delegation] empty delegation input; skipping"),l("Please enter a delegation request before submitting.");return}console.log("[dashboard-delegation] sending delegation:",h);var E=u();if(!E){console.error("[dashboard-delegation] aborting delegation because fetch is unavailable or invalid"),l("Browser fetch is unavailable.");return}var T=r&&r.textContent||"Submit Delegation",A=null;try{r&&(r.disabled=!0,r.textContent="Sending...",r.classList.add("opacity-70","cursor-not-allowed")),w(h),A=window.setTimeout(v,4e3);var a;try{a=await E("/api/delegate-task",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({prompt:h,message:h,text:h,task:h})})}catch(f){throw console.error("[dashboard-delegation] fetch threw before response:",f),f}console.log("[dashboard-delegation] fetch returned:",{ok:a&&a.ok,status:a&&a.status,statusText:a&&a.statusText});var d=null,x="";try{x=await a.text(),d=x?JSON.parse(x):{}}catch(f){console.error("[dashboard-delegation] error parsing JSON response:",f),d={error:"Non-JSON response from /api/delegate-task",raw:x||""}}if(console.log("[dashboard-delegation] delegation response:",d),!a.ok){l(d&&(d.error||d.message||d.statusText)||"HTTP "+a.status+" "+(a.statusText||""),x);return}k(d)}catch(f){l(f&&f.message?f.message:String(f))}finally{A&&window.clearTimeout(A),r&&(r.disabled=!1,r.textContent=T,r.classList.remove("opacity-70","cursor-not-allowed"))}}function y(){var s=p("delegation-submit"),r=p("delegation-input");if(!s||!r){console.warn("[dashboard-delegation] delegation button or input not found in init");return}if(c(),s.dataset.delegationWired==="true"){console.log("[dashboard-delegation] Task Delegation wiring already active");return}s.dataset.delegationWired="true",s.addEventListener("click",I),r.addEventListener("keydown",function(h){(h.metaKey||h.ctrlKey)&&h.key==="Enter"&&(h.preventDefault(),I())}),console.log("[dashboard-delegation] Task Delegation wiring active")}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();(()=>{let p="/events/task-events",u="mb-task-events-panel",b="mb-task-events-feed",S="mb-task-events-counts",i="mb-task-events-panel-anchor";function c(){let n=document.getElementById(i);return n||document.body}function w(){if(document.getElementById(u))return;let n=c(),o=document.createElement("div");o.id=u;let g=n&&n.id===i;o.style.width=g?"100%":"360px",o.style.maxWidth=g?"100%":"calc(100vw - 24px)",o.style.maxHeight=g?"260px":"40vh",o.style.overflow="hidden",o.style.zIndex="9999",o.style.border="1px solid rgba(255,255,255,0.12)",o.style.borderRadius="14px",o.style.background="rgba(10,10,14,0.92)",o.style.backdropFilter="blur(10px)",o.style.boxShadow="0 10px 30px rgba(0,0,0,0.35)",o.style.color="rgba(255,255,255,0.92)",o.style.fontFamily="ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial",g?o.style.marginTop="12px":(o.style.position="fixed",o.style.right="12px",o.style.bottom="12px");let e=document.createElement("div");e.style.display="flex",e.style.alignItems="center",e.style.justifyContent="space-between",e.style.gap="8px",e.style.padding="10px 12px",e.style.borderBottom="1px solid rgba(255,255,255,0.10)";let m=document.createElement("div");m.textContent="Task Events",m.style.fontSize="12px",m.style.letterSpacing="0.06em",m.style.opacity="0.9";let _=document.createElement("div");_.style.display="flex",_.style.alignItems="center",_.style.gap="10px";let C=document.createElement("div");C.id=S,C.textContent="created:0  completed:0  failed:0",C.style.fontSize="11px",C.style.opacity="0.85",C.style.fontVariantNumeric="tabular-nums";let L=document.createElement("span");L.setAttribute("aria-label","task-events connection"),L.title="task-events connection",L.style.display="inline-block",L.style.width="10px",L.style.height="10px",L.style.borderRadius="999px",L.style.background="rgba(255,255,255,0.25)",L.style.boxShadow="0 0 0 2px rgba(255,255,255,0.08) inset";let O=document.createElement("button");O.type="button",O.textContent="\xD7",O.title="hide",O.style.cursor="pointer",O.style.border="1px solid rgba(255,255,255,0.14)",O.style.background="transparent",O.style.color="rgba(255,255,255,0.85)",O.style.borderRadius="10px",O.style.width="28px",O.style.height="24px",O.style.lineHeight="22px",O.style.fontSize="14px",O.onclick=()=>o.remove(),_.appendChild(L),_.appendChild(C),_.appendChild(O),e.appendChild(m),e.appendChild(_);let N=document.createElement("div");N.id=b,N.style.padding="10px 12px",N.style.overflow="auto",N.style.maxHeight=g?"200px":"calc(40vh - 46px)",o.appendChild(e),o.appendChild(N),n.appendChild(o),window.__MB_TASK_EVENTS_PANEL={dot:L,feed:N,counts:C},console.log("[phase22] task-events panel mounted (anchored=%s)",g)}function v(n){w();let o=window.__MB_TASK_EVENTS_PANEL?.dot;o&&(n==="open"?o.style.background="rgba(80,200,120,0.85)":n==="error"?o.style.background="rgba(240,90,90,0.85)":o.style.background="rgba(255,255,255,0.25)")}let k=new Set,l={created:0,completed:0,failed:0},Q=new Set,F=document.getElementById("metric-running");function U(){F||(F=document.getElementById("metric-running")),F&&(F.textContent=String(Q.size))}function z(n){return n&&typeof n=="object"?String(n.task_id??n.taskId??n.meta?.task_id??n.meta?.taskId??"").trim():""}function G(n,o){let g=String(n?.kind??o??"").toLowerCase(),e=String(n?.status??n?.meta?.status??"").toLowerCase(),m=z(n);if(!m)return;/task\.created|task\.started|task\.running|task\.queued|task\.resumed/.test(g)||/created|started|running|queued|pending|active/.test(e)?Q.add(m):(/task\.completed|task\.failed|task\.cancelled|task\.canceled|task\.done/.test(g)||/completed|failed|cancelled|canceled|done|error/.test(e))&&Q.delete(m),U()}function I(n){n==="task.created"&&(l.created+=1),n==="task.completed"&&(l.completed+=1),n==="task.failed"&&(l.failed+=1);let o=document.getElementById(S);o&&(o.textContent=`created:${l.created}  completed:${l.completed}  failed:${l.failed}`),U()}function y(n){if(typeof n=="number")return new Date(n).toISOString();if(typeof n=="string"&&n.trim()){let o=new Date(n);return Number.isNaN(o.getTime())?n.trim():o.toISOString()}return new Date().toISOString()}function s(n,o,g){let e=`${n||""} ${o||""} ${g||""}`.toLowerCase();return/fail|error|cancel|timeout/.test(e)?"terminal-error":/complete|done|success/.test(e)?"terminal-success":/queue|pending|retry|wait|hold|sleep/.test(e)?"waiting":/open|start|run|resume|lease|dispatch|ack|progress|update|active|heartbeat/.test(e)?"active":"neutral"}function r(n){return n==="terminal-error"?"rgba(240,90,90,0.95)":n==="terminal-success"?"rgba(80,200,120,0.95)":n==="waiting"?"rgba(250,204,21,0.95)":n==="active"?"rgba(96,165,250,0.95)":"rgba(255,255,255,0.18)"}function h(n,o){let g=y(n.ts),e=String(n.kind??o??"event"),m=n.task_id??n.taskId??"unknown",_=n.run_id??n.runId??"",C=n.actor??n.meta?.actor??n.meta?.owner??"",L=n.status??n.meta?.status??"",O=typeof n.cursor=="number"||typeof n.cursor=="string"?String(n.cursor):"",N=String(n.msg??n.message??"").trim(),B=[];B.push(`task=${m}`),_&&B.push(`run=${_}`),C&&B.push(`actor=${C}`),L&&B.push(`status=${L}`),O&&B.push(`cursor=${O}`),N&&B.push(N);let M=B.join(" \u2022 "),P=s(e,N,L);return{ts:g,kind:e,detail:M,tone:P}}function E(n,o){w();let g=document.getElementById(b);if(!g)return;let e=h(n,o),m=document.createElement("div");m.className=`phase61-task-event phase61-task-event-${e.tone}`,m.dataset.eventKind=e.kind,m.dataset.eventTone=e.tone,m.style.position="relative",m.style.display="grid",m.style.gridTemplateColumns="minmax(112px, 132px) minmax(150px, 170px) 1fr",m.style.gap="10px",m.style.alignItems="start",m.style.padding="10px 12px 10px 14px",m.style.marginBottom="8px",m.style.border="1px solid rgba(255,255,255,0.10)",m.style.borderRadius="12px",m.style.background="rgba(255,255,255,0.03)",m.style.lineHeight="1.35",m.style.fontSize="12px";let _=document.createElement("span");_.setAttribute("aria-hidden","true"),_.style.position="absolute",_.style.left="0",_.style.top="10px",_.style.bottom="10px",_.style.width="3px",_.style.borderRadius="999px",_.style.background=r(e.tone);let C=document.createElement("div");C.textContent=e.ts,C.style.color="rgba(255,255,255,0.68)",C.style.fontVariantNumeric="tabular-nums",C.style.whiteSpace="nowrap";let L=document.createElement("div");L.textContent=e.kind,L.style.color="rgba(255,255,255,0.92)",L.style.fontWeight="600",L.style.letterSpacing="0.01em",L.style.wordBreak="break-word";let O=document.createElement("div");O.textContent=e.detail||"\u2014",O.style.color="rgba(255,255,255,0.78)",O.style.wordBreak="break-word",m.appendChild(_),m.appendChild(C),m.appendChild(L),m.appendChild(O),g.prepend(m);let N=Array.from(g.children);if(N.length>60)for(let B=60;B<N.length;B++)N[B].remove()}function T(n){try{window.dispatchEvent(new CustomEvent("mb.task.event",{detail:n}))}catch{}}function A(n){try{return JSON.parse(n)}catch{return null}}function a(n,o){let g=typeof o=="string"?A(o):o,e=g&&typeof g=="object"?g:{kind:n,raw:o};n==="task.event"&&(e.actor==null&&e.meta&&typeof e.meta=="object"&&(e.actor=e.meta.actor??e.meta.owner??null),!e.kind&&e.type&&(e.kind=e.type),e.kind==="task.event"&&e.type&&(e.kind=e.type),e.task_id==null&&e.taskId!=null&&(e.task_id=e.taskId),e.run_id==null&&e.runId!=null&&(e.run_id=e.runId),e&&e.meta&&typeof e.meta=="object"&&(e.task_id==null&&e.meta.task_id!=null&&(e.task_id=e.meta.task_id),e.run_id==null&&e.meta.run_id!=null&&(e.run_id=e.meta.run_id),e.actor==null&&e.meta.actor!=null&&(e.actor=e.meta.actor),e.actor==null&&e.meta.owner!=null&&(e.actor=e.meta.owner),e.status==null&&e.meta.status!=null&&(e.status=e.meta.status))),e.kind||(e.kind=n);let m=`${n}|${e.kind}|${e.ts??""}|${e.task_id??""}|${e.run_id??""}|${e.cursor??""}`;if(!k.has(m)){if(k.add(m),(e.kind==="task.created"||e.kind==="task.completed"||e.kind==="task.failed")&&I(String(e.kind)),G(e,n),E(e,n),window.__UI_DEBUG)try{console.log("[task-events] mb.task.event",e)}catch{}T(e)}}let d=null,x=0;function f(){if(w(),d){try{d.close()}catch{}d=null}let n=p;d=new EventSource(n),d.onopen=()=>{x=0,v("open"),E({ts:Date.now(),kind:"sse.open",message:`url=${n}`},"sse.open"),console.log("[phase22] task-events SSE open",n)},d.onerror=()=>{v("error");try{d.close()}catch{}d=null,x+=1;let g=Math.min(15e3,500*Math.pow(2,Math.min(6,x)));E({ts:Date.now(),kind:"sse.error",message:`reconnect_in=${g}ms`},"sse.error"),console.log("[phase22] task-events SSE error; reconnect in",g),setTimeout(f,g)},d.onmessage=g=>a("message",g.data);let o=["hello","heartbeat","task.event","task.created","task.completed","task.failed","task.updated","task.status"];for(let g of o)d.addEventListener(g,e=>a(g,e.data))}function t(){f(),setInterval(()=>{if(!document.getElementById(u))try{f()}catch{}},2e3)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",t,{once:!0}):t(),window.__MB_TASK_EVENTS={url:p,reconnect:()=>f()}})();(()=>{let p="mb.task.event",u=new Map,b={queued:"task-status-queued",done:"task-status-done",failed:"task-status-failed"};function S(s){let r=String(s??"").toLowerCase();return r==="queued"||r==="pending"?"queued":r==="done"||r==="complete"||r==="completed"?"done":r==="failed"||r==="error"?"failed":r||"unknown"}function i(s){return s?.task_id??s?.taskId??s?.task?.id??null}function c(s){let r=s?.task&&typeof s.task=="object"?s.task:null,h=i(s),E=s?.status??s?.payload?.status??r?.status??(s?.kind==="task.created"?"queued":null);return{id:h!=null?String(h):null,status:E!=null?S(E):null,title:r?.title??s?.title??null,agent:r?.agent??s?.agent??null,error:s?.error??s?.payload?.error??r?.error??null,updated_at:r?.updated_at??s?.ts??Date.now()}}function w(s,r){if(!s)return;let h=S(r);s.setAttribute("data-task-status",h),s.classList?.remove(...Object.values(b)),b[h]&&s.classList?.add(b[h]);let E=s.querySelector?.("[data-task-field='status']")||s.querySelector?.(".task-status")||s.querySelector?.(".status")||null;E&&(E.textContent=h)}function v(s){if(!s?.id)return;let r=String(s.id),h=[document.getElementById(`task-${r}`),document.getElementById(`task_${r}`),document.querySelector?.(`[data-task-id="${CSS.escape(r)}"]`),document.querySelector?.(`[data-taskid="${CSS.escape(r)}"]`)].filter(Boolean);for(let E of h)w(E,s.status)}function k(){let s=0,r=0,h=0;for(let T of u.values()){let A=S(T.status);A==="queued"?s++:A==="done"?r++:A==="failed"&&h++}let E=[["queued",s],["done",r],["failed",h]];for(let[T,A]of E){let a=document.getElementById(`task-count-${T}`)||document.getElementById(`tasks-${T}-count`)||document.querySelector?.(`[data-task-count="${T}"]`)||null;a&&(a.textContent=String(A))}}function l(s){if(!s?.id)return;let r=String(s.id),h=u.get(r)||{},E={...h,...s,id:r,status:s.status??h.status};u.set(r,E),v(E),k()}function I(s){let r=c(s);!r.id&&s?.kind&&(s.kind==="task.completed"&&(r.status="done"),s.kind==="task.failed"&&(r.status="failed")),r.id&&l(r)}function y(){window.__PHASE22_TASK_UI_BOUND||(window.__PHASE22_TASK_UI_BOUND=!0,window.addEventListener(p,s=>{try{(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&(window.__UI_DEBUG||window.__PHASE22_DEBUG)&&console.log("[phase22] mb.task.event",s.detail),I(s.detail)}catch{}}),window.__PHASE22_TASK_UI={tasks:u},console.log("[phase22] bindings attached"))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",y,{once:!0}):y()})();typeof window<"u"&&typeof window.__DISABLE_OPTIONAL_SSE>"u"&&(window.__DISABLE_OPTIONAL_SSE=!1);})();
42:    'queued',
100:      payload?.status ??
101:      payload?.state ??
234:      forcedEventName ?? parsed?.event ?? parsed?.type ?? parsed?.status,

FILE: ./public/dashboard-tabs.backup.js
23:      div.className = `task ${t.status}`;
24:      if (t.status === "complete" && t.endTime) {
27:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
107:      div.textContent = `${agent.name} — ${agent.status}`;
109:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./public/bundle-core.js
17:  // public/js/dashboard-status.js
23:    const healthStatus = document.getElementById("health-status");
31:      console.warn("dashboard-status.js: Core status elements not found in DOM.");
53:    function classifyHealthFromStatus(statusString) {
54:      const s = (statusString || "").toLowerCase();
92:      console.error("dashboard-status.js: Failed to open OPS SSE connection:", err);
105:      const statusString = (data.status || data.state || data.level || "").toString();
108:        agentStatusMap[agentName] = statusString || "unknown";
120:      const statusLower = statusString.toLowerCase();
121:      if (statusLower.includes("success") || statusLower.includes("completed") || statusLower.includes("ok")) {
123:      } else if (statusLower.includes("error") || statusLower.includes("failed")) {
135:      const healthState = classifyHealthFromStatus(statusString);
144:        if (statusString) labelParts.push(`\u2013 ${statusString}`);
145:        if (message && message !== statusString) labelParts.push(`\u2013 ${String(message).slice(0, 160)}`);
154:      console.warn("dashboard-status.js: OPS SSE error:", err);
170:      console.error("dashboard-status.js: Failed to open Reflections SSE connection:", err);
191:      console.warn("dashboard-status.js: Reflections SSE error:", err);
14676:    const completedData = tasks.map((t) => t.status === "completed" ? 1 : 0);
14677:    const failedData = tasks.map((t) => t.status === "failed" ? 1 : 0);
14721:      if (!res.ok) throw new Error(`HTTP ${res.status}`);

FILE: ./public/dashboard-tabs.js
24:      div.className = `task ${t.status}`;
25:      if (t.status === "complete" && t.endTime) {
28:        div.textContent = `${t.task} — ${t.agent} (${t.status}, started ${t.startTime})`;
109:      div.textContent = `${agent.name} — ${agent.status}`;
111:      const isOnline = agent.status.toLowerCase() === "online";

FILE: ./scripts/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./scripts/_local/route-loader/ops-sse-server.ts
18:  res.write(`data: {"status":"connected"}\n\n`);
31:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./scripts/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./scripts/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;

FILE: ./scripts/sql/invariants/phase55_terminal_event_precedence.sql
39:    RAISE EXCEPTION 'Phase55 invariant: cannot find events table (tried task_events/tasks_events/events/task_event)';
43:    RAISE EXCEPTION 'Phase55 invariant: cannot find tasks table (tried tasks/task)';
109:    AND c.table_name=tasks_tbl
117:    AND c.table_name=tasks_tbl
125:    AND c.table_name=tasks_tbl

FILE: ./run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./ts-backup/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./ts-backup/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./ts-backup/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./ts-backup/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./ts-backup/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./ts-backup/tasks.ts

FILE: ./tasks.ts

FILE: ./routes/api/tasks.ts
10:      "SELECT id, type, agent, status, payload, result, created_at FROM task_events ORDER BY created_at DESC LIMIT 20"
16:    res.status(500).json({ ok: false, error: "task_fetch_error" });

FILE: ./routes/tasks.ts

FILE: ./routes/routes/tasks.ts

FILE: ./scripts_backup_2/demo/run-demo-sequence.ts
15:      "Matilda broadcasting cycle status.",
29:      sqlite.prepare("INSERT INTO task_events (description, status) VALUES (?, 'completed')").run(t)

FILE: ./scripts_backup_2/_local/route-loader/ops-sse-server.ts
22:  res.write(`data: {"status":"connected"}\n\n`);
35:    .prepare("SELECT id, description, status, created_at FROM task_events WHERE id > ? ORDER BY id ASC")

FILE: ./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor_clean.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup_2/_local/agent-runtime/utils/effie_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
41:    if (task.status === "Pending" && task.agent === "Effie" && task.type === "run_shell") {
44:        task.status = "Completed";
47:        task.status = "Failed";

FILE: ./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor.bak.ts
4:const filePath = "./memory/agent_chain_state.json";
38:    if (task.status === "Pending") {
60:        task.status = "Completed";
64:        task.status = "Failed";

FILE: ./scripts_backup_2/_local/agent-runtime/utils/cade_task_processor.ts
5:const filePath = "./memory/agent_chain_state.json";
40:    if (task.status === "Pending" && task.agent === "Cade" && task.type === "run_shell") {
43:        task.status = "Completed";
46:        task.status = "Failed";

FILE: ./scripts_backup_2/_local/agent-runtime/utils/matilda_task_processor.ts
4:const chainPath = path.resolve('memory/agent_chain_state.json');
21:  const state = readChainState();
22:  const tasks = state?.tasks;


-- done --
Next: use output to confirm exact queued-state definition before wiring metric.
