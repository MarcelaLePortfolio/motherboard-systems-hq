services:
  worker:
    volumes:
      - ./scripts:/scripts
    entrypoint: ["/scripts/docker-entrypoint-guard.sh"]
    command: ["node", "server/worker/phase26_task_worker.mjs"]

  dashboard:
    volumes:
      - ./scripts:/scripts
    entrypoint: ["/scripts/docker-entrypoint-guard.sh"]
