# Phase 48: default local DB URL for docker compose (no per-run overrides)
# docker compose automatically loads .env from the project root
POSTGRES_URL=postgres://postgres:postgres@postgres:5432/postgres
