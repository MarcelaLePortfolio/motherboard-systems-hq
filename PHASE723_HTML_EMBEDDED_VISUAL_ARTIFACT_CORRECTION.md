
# Phase 723 Corridor Correction: HTML-Embedded Visual Artifact Target

## Correction

The intended end-state is not merely an execution summary.

The intended end-state is an HTML-embedded visual artifact of something built.

## Correct Final Operator Experience

At the end of this corridor, Preview should show:

- a rendered visual artifact

- HTML-safe embedded presentation

- operator-readable artifact context

- semantic metadata support

- markdown fallback preservation

- no raw envelope leakage

- no iframe/srcdoc instability reintroduced unless isolated and proven safe

## Correct Artifact Direction

The semantic summary layer is supportive context only.

The artifact itself should move toward:

- rendered build preview

- visual output card

- HTML artifact display

- safe embedded artifact surface

- operator-facing visual deliverable

## Preserved Constraints

Do not mutate:

- DB schema

- retry architecture

- SSE contracts

- task polling

- backend execution contracts

Do not remove:

- markdown fallback

- artifact preview route safety

- semantic envelope compatibility

## Next Safe Corridor

Phase 723 should realign around HTML-embedded visual artifact rendering.

Recommended next step:

Inspect current artifact preview rendering and decide whether the visual artifact should be represented as:

1. safe inline HTML subset rendering

2. generated HTML artifact file plus read-only preview route

3. markdown artifact with embedded sanitized HTML visual block

4. separate visual artifact metadata while preserving markdown fallback

## Immediate Priority

Stop treating the corridor as only "execution summary polish."

Reframe Phase 723 as:

HTML-embedded visual artifact corridor.

