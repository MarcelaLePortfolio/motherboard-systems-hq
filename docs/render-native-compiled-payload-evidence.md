
# Render-Native Compiled Payload Evidence

Status: VERIFIED

Verified source:

- scripts/render-native/generated/compiled-semantic-payload.json

Purpose:

Capture the exact deterministic payload structure emitted by the semantic compiler before renderer integration planning.

Verification goals:

- inspect payload schema stability

- inspect node hierarchy

- inspect token propagation

- inspect layout mapping

- inspect style mapping

- inspect render compatibility assumptions

Constraint:

This evidence is sandbox-only and does not mutate live Preview runtime.

