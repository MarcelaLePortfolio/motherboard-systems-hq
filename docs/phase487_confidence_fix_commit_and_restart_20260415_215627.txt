PHASE 487 — CONFIDENCE FIX COMMIT + RESTART
timestamp=20260415_215627
branch=phase119-dashboard-cognition-contract
commit_before=462867f1d96f9bec39cacf55f3cafed6548ba802

=== TARGET DIFF ===
diff --git a/src/cognition/operatorGuidanceConfidence.ts b/src/cognition/operatorGuidanceConfidence.ts
index 829ebd3e..7d23d812 100644
--- a/src/cognition/operatorGuidanceConfidence.ts
+++ b/src/cognition/operatorGuidanceConfidence.ts
@@ -25,7 +25,7 @@ export function assessGuidanceConfidence(
 
   if (signals.length === 0) {
     return {
-      confidence: "insufficient",
+      confidence: "limited",
       reason: "No signals available for interpretation.",
       signalCount: 0,
       conflictingSignals: false,

=== SOURCE CHECK ===
1:import type { OperationalConfidence } from "./confidence";
2-
3-export interface OperatorGuidanceConfidenceInput {
4-  operationalConfidence?: OperationalConfidence;
5-}
6-/*
7-PHASE 89 — CONFIDENCE FRAMING MODEL
8:Defines bounded confidence interpretation rules.
9-No execution authority. Cognition framing only.
10-*/
11-
--
13-import type { GuidanceSignalObservation } from "./operatorGuidanceMapping";
14-
15-export type ConfidenceAssessment = {
16:  confidence: GuidanceConfidence;
17-  reason: string;
18-  signalCount: number;
19-  conflictingSignals: boolean;
--
25-
26-  if (signals.length === 0) {
27-    return {
28:      confidence: "limited",
29-      reason: "No signals available for interpretation.",
30-      signalCount: 0,
31-      conflictingSignals: false,
--
42-
43-  if (stalled > 0) {
44-    return {
45:      confidence: "medium",
46-      reason: "Stalled signals observed but root cause not proven.",
47-      signalCount: signals.length,
48-      conflictingSignals,
--
51-
52-  if (degraded > 0) {
53-    return {
54:      confidence: "medium",
55-      reason: "Degraded signals observed without systemic failure.",
56-      signalCount: signals.length,
57-      conflictingSignals,
--
60-
61-  if (unknown > 0) {
62-    return {
63:      confidence: "low",
64:      reason: "Signal quality insufficient for strong interpretation.",
65-      signalCount: signals.length,
66-      conflictingSignals,
67-    };
68-  }
69-
70-  return {
71:    confidence: "high",
72-    reason: "Signals consistently indicate healthy state.",
73-    signalCount: signals.length,
74-    conflictingSignals,
--
80-
81-
82-export function mapOperationalConfidenceToGuidanceModifier(
83:  confidence?: OperationalConfidence,
84-): number {
85:  if (!confidence) return 0;
86-
87:  if (confidence.level === "HIGH") return 1;
88:  if (confidence.level === "MEDIUM") return 0;
89:  if (confidence.level === "LOW") return -1;
90-
91-  return 0;
92-}
93-
94-
95:export function confidencePriorityWeight(
96:  confidence?: OperationalConfidence,
97-): number {
98:  if (!confidence) {
99-    return 1;
100-  }
101-
102:  switch (confidence.level) {
103-    case "HIGH":
104-      return 1;
105-    case "MEDIUM":

=== INTENT ===
Commit the already-modified confidence mapping file and restart the local dashboard/runtime so the UI reflects the change.

