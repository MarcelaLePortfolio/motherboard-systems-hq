PHASE 486 — STEP 18
EXECUTION PANEL TRAVERSAL ORDER RENDER SPEC

────────────────────────────────

OBJECTIVE

Define the exact passive render spec for:

report.traversalOrder

inside the existing Execution panel in:

app/demo-runtime/page.tsx

This is a specification step only.
No UI mutation occurs here.

────────────────────────────────

INPUT BASIS

Existing available field:

• report.traversalOrder: string[]

Current Execution panel already renders:

• report.generatedRequest.tasks

Current Execution panel does NOT render:

• report.traversalOrder

────────────────────────────────

PANEL LOCATION

Inside existing:

Execution

panel

Placement:

• After the "Deterministic traversal order" label
• Before the existing rendered task list
• Inside the existing report-gated Runtime Result section

────────────────────────────────

EXACT CONTENT TO RENDER

New subsection title:

Traversal order

Value source:

report.traversalOrder

Render shape:

• Ordered list
• One item per entry
• Preserve exact string values
• Preserve exact backend order

────────────────────────────────

NO TRANSFORMATION RULE

UI must NOT:

• sort traversalOrder
• deduplicate traversalOrder
• infer missing task names
• map traversalOrder into alternative labels
• compare traversalOrder against tasks to derive conclusions

UI must only:

• render report.traversalOrder exactly as received

────────────────────────────────

NO DATA RULE

If report.traversalOrder.length === 0

Render exactly:

NO DATA

Do not fabricate fallback ordering.

────────────────────────────────

RELATIONSHIP TO EXISTING TASK LIST

The existing generatedRequest.tasks list remains unchanged.

Purpose separation:

• traversalOrder = reported traversal sequence
• generatedRequest.tasks = task definition list

Both may coexist.

UI must not imply they are identical unless backend already does so.

────────────────────────────────

PREFERRED DISPLAY SHAPE

Execution
• Deterministic traversal order:

Traversal order
1. entry
2. entry
3. entry

Task definitions
1. task.id: task.name
2. task.id: task.name

────────────────────────────────

COMPLETION CONDITION

This spec is complete when:

• exact field is named
• exact placement is named
• exact NO DATA behavior is defined
• exact no-transformation rule is defined
• implementation can proceed as one UI-only mutation

────────────────────────────────

CLASSIFICATION

EXECUTION PANEL PASSIVE RENDER SPEC COMPLETE

Next corridor:

Phase 486 Step 19 — implement passive traversalOrder render in existing Execution panel

